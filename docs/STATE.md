# Dispatch — Product State (current as of Day 51, 2026-05-09)

*Single canonical entry point for new Cowork chat sessions. Read this first; do NOT read the daily handoff history (it has been deleted — use git log if you need archaeology). Forward motion only.*

*Update this doc inline when items close. Add new entries to "Open Jennifer questions" and "Standing tabled" as discovered. Do not recreate the daily handoff narrative.*

---

## What the product is

Dispatch is a mobile-primary web app for boutique/independent hotels. Managers/admins create task cards (housekeeping turns, arrivals, stayovers, dailys, EOD, maintenance) and assign them to staff phones. Staff execute them.

Owner: Bryan Stauder. Co-founder: Wisconsin boutique hotel operator (Jennifer). Beta target: that hotel.

Stack: Next.js 16 App Router + React 19 + TypeScript strict + Supabase (Postgres + Auth + RLS + Storage). No Tailwind; plain CSS in `app/globals.css`. No tests. No middleware. Auth guarding is client-side in page components. Magic-link OTP only.

---

## What ships today

**Staff side:**
- Pre-Clock-In screen with greeting + "Start your day" CTA. Bucket deck takes over once clocked in.
- Six bucket cards on staff home (SOD / Departures / Stayovers / Arrivals / Dailys / EOD) with three-mode gating (Day 38): not-yet-reached blurred + hard-locked (pointer-events: none); already-done blurred + checkmark, clickable to view without un-completing (`.bcard.is-done`); current active sharp + clickable. Forward-unlock (`.bcard.is-unlockable`) on cards whose previous buckets are all in `done`. Late-Departures reshuffle: when past 11 AM Central weekday / 12 PM weekend AND Departures still has incomplete tasks, deck reorders to SOD → Stayovers → Arrivals → Departures → Dailys → EOD. Initial `active` advances past initially-done buckets so the deck doesn't render with a "completed but active" head bucket.
- All six X-430 staff card execution screens (D-430, A-430, S-430, Da-430, E-430, SOD-430), with Notes compose popout modal (Day 48 — triggered by `+` button in `topstrip-right`, cream dialog with overlay + Esc-close + scroll-lock + mobile bottom-sheet / desktop centered; replaces inline compose form pattern across all 6 cards), Maintenance compose drawer, status pills, checklists, pause/resume, need-help, image attachment. D-430 / A-430 / S-430 briefs fall back to live `reservations` table reads when `task.context.{outgoing,incoming,current}_guest` is missing (Day 37 V.A BR4).
- AddTaskModal `task.context.notes` (free-text manager note, written by the modal's Notes panel since Day 28) surfaces on every X-430 staff card via a bucket-agnostic `.manager-note` block (Day 47 plumb-through; Day 48 restyle). Day 48 visual aesthetic matches the existing `setstat-row` pattern: white container, mono uppercase label, mint-pill value via `var(--accent-soft)` so each bucket inherits its own tone (SOD-amber / Departures-red / Stayovers-mint / Arrivals-yellow / Dailys-orange / EOD-purple) without per-card CSS. Per-card placement: SOD / Arrivals / Stayovers above brief; Dailys / EOD above first section; **Departures specifically** has the manager-note relocated INTO the setstat container as a row between Room Spray and Status (per Bryan's Day 48 spec).
- Wrap Shift on E-430 clocks staff out + cross-staff EOD activation gate (locked until other on-shift staff are in their EOD).

**Admin side:**
- `/admin` home with live Activity Feed (filterable by severity + kind, dismissable per-browser via localStorage).
- `/admin/staff/[id]` with sky-blue hero card + 14-day segment block + per-shift summary + lifetime hours.
- `AddTaskModal` mounted on `/admin`, `/admin/tasks`, `/admin/staff/[id]`. Seven bucket chips selectable as of Day 44 (canonical six staff buckets including SOD + Maintenance card-type modifier), priority chips, assignee chips, room field, notes panel, pre-selected-staff lock mode.
- `/tasks/[id]` manager card view with editable assignee dropdown → fires reassignTask helper on save (Day 34).
- New `<StayoverStatusPanel/>` mounted on BOTH `/tasks/[id]` (manager card view) AND `/admin/tasks/[id]` (admin task view modal) Day 46 — discrete multi-select chip control gated on `card_type === "stayover"` writing `context.stayover_status` (`string[]` of keys: `dnd / guest_ok / desk_ok / sheet_change / done`) via merge-safe spread; emits new `stayover_status_overridden` audit event. Single source of writes today (orchestrator pre-set deferred until Jennifer's KB heuristic; staff-side toggle locked display-only Day 21). Populates the S-430 brief "Last status" row on subsequent stayover cards for the same room via Day 45's `lib/stayover-history.ts` lookup.
- `/admin/tasks` dashboard live (Day 36 chase #1 — single tasks fetch derives OPEN/DONE-TODAY/OVERDUE stats + HOUSEKEEPING/ADMIN/MAINTENANCE lane partitions client-side).
- `/admin/maintenance/[id]` live-data wired Day 41 (II.H — synthetic 8-char work-order display ID derived from row UUID, sage hero with severity chip row writing back to `maintenance_issues.severity`, Mark Resolved write-path stamping `resolved_at` + `resolved_by_user_id`, conditional photo panel rendering `image_url` when set, derived 1-2 row activity panel — Reported by + Resolved by — with resolver name resolved via `profiles.display_name`). Watchlist lane on `/admin` now links per item directly to `/admin/maintenance/{id}` (first inbound nav path).
- `/admin/maintenance` master-table index live (Day 42 — II.H Phase C). Sage cream-shell list view of unresolved `maintenance_issues` rows. Two flat dropdowns (Location + Type, sourced from `MAINTENANCE_LOCATIONS` + `MAINTENANCE_TYPES` static exports in `lib/maintenance.ts`) default to "All"; conditional CLEAR button when either filter is set. Severity desc client-side (High → Normal → Low) with created_at desc tiebreak (mirrors Watchlist convention). Each row Links to `/admin/maintenance/{id}` (live-wired Day 41). Two inbound paths from `/admin`: Watchlist lane "VIEW ALL OPEN ISSUES" / "VIEW ALL · +N MORE" caption (replaces Day 41's static `+N more` div) + Maintenance lane tile (re-targeted Day 42 from stale `/admin/tasks#maintenance` to `/admin/maintenance`). 5-step UI verification ran clean end-to-end Day 42 — populated list view, severity-ordered chips, filter compose, row click into per-issue page.
- `/admin/tasks/[id]` admin task view modal fully live (Day 36 chase #1) — full task fetch, schema-aligned 3-chip priority, editable admin notes textarea persisting to `context.admin_notes`, per-task `task_events` activity panel, Save & Deploy writing priority + merge-safe context atomically. ReassignPanel from Day 35 unchanged.
- `/admin` home four lanes live (Day 36 chase #1) — Watchlist (unresolved `maintenance_issues` severity-sorted), Scheduling (`public.staff` clock-in snapshot until II.K Calendar lands), Critical (priority='high' open + status='blocked' merged), Notes (recent `public.notes`). `BRIEF_STATS` + STAFF roster intentionally still static (out of chase #1 scope).
- `/admin/staff/[id]` profile metadata live (Day 36 chase #1) — slug → live `public.staff` row → derived hero/role/status/role-line/status-line. AVATAR_* + slug→fullName map stays (locked design tokens per master plan II.D/II.E). Metrics trio derives from a per-staff tasks fetch (rooms/jobs label swaps by role). Segment block already live (Day 32) and unchanged.

**Data layer:**
- Real `shift_start` / `shift_end` events written to `inbound_events` via SECURITY DEFINER Postgres trigger on `staff.clocked_in_at` flips.
- Three views: `staff_shifts_v` (LATERAL pairing of starts to ends), `staff_segments_v` (Wed-anchored 14-day buckets, reference Wed = 2026-01-07), `shift_summary_v` (per-shift task counts by card_type).
- `notes` table + 4 taxonomies (note_types × 11, note_statuses × 5, note_assigned_to × 5) + RLS + denormalize trigger.
- `maintenance_issues` table + 4 taxonomies (locations × 21, items × 11, types × 10, severities × 3) + RLS + denormalize trigger + 7 indexes.
- `task_events` append-only with 16 event types. `schema_version: 1` required on all writes via `withTaskEventSchema` from `lib/task-events.ts`. See `docs/TASK_EVENTS_CONTRACT.md`.
- Activity feed query helper at `lib/activity-feed.ts` unifies `task_events` + `notes` with severity boost ordering (criticals → warns → info, then reverse-chrono within each bucket).

**Auth + routing:**
- Supabase magic-link only. No passwords, no social.
- Role-based routing in `lib/profile.ts` (`shouldUseManagerHome`, `mayAccessStaffRoutes`).
- Localhost-only dev role-view switcher in `lib/dev-auth-bypass.ts`.

**Production deploy (Day 45):**
- Live at https://dispatch-app-iota.vercel.app/ on Vercel Hobby plan, auto-deploys from `main` branch pushes.
- 5 env vars set in Vercel dashboard: 3 Supabase keys + `AGENT_KILL=true` + `AGENT_DRY_RUN=true` (production safety). `NEXT_PUBLIC_DEV_BYPASS` deliberately not set — localhost-only role switcher.
- Supabase auth: Site URL set to prod domain; Redirect URLs allowlist gains `/auth/callback` + `/**` for the Vercel domain (localhost entries preserved for dev).

**Locked design tokens** in `app/globals.css`: six neon bucket palettes (SOD / Departures / Stayovers / Arrivals / Dailys / EOD) + Day 20 cream-surface tokens + sage green admin maintenance lane (`#BACBA0` / `#8A9B75` / `#2C3A1D`, master plan II.C) + per-staff hero gradients (CM peach, LL sky, AL coral, MP sage — 4-name fixed palette per master plan II.D/II.E).

---

## Schema in place

**Tables:**
- `tasks` — `card_type` ∈ {`housekeeping_turn`, `arrival`, `stayover`, `eod`, `dailys`, `start_of_day`, `maintenance`, `generic`}; `status` ∈ {`open`, `in_progress`, `paused`, `blocked`, `done`}; `source` ∈ {`manual`, `staff_report`, `pms`, `system`}; `priority` ∈ {`low`, `medium`, `high`} default `medium`. `context` JSONB carries per-card metadata; required subkey `staff_home_bucket`; conventional subkeys `incoming_guest` / `current_guest` / `outgoing_guest` (object-shaped per `wave_4d_context.sql` — each has `name`/`party_size`/`extras`/etc. depending on guest direction) + `notes` (card-level free text, role-agnostic, distinct from `public.notes` table) + `admin_notes` (Day 36 chase #1 — string, written by admin via `/admin/tasks/[id]` Save & Deploy; surfaces as the editable Admin Notes panel; role-distinct from `notes` so admin and staff don't stomp each other's content). Always merge-safe write: `{ ...current, <subkey>: { ...current?.<subkey>, <new> } }` (or for string subkeys, `{ ...current, <subkey>: <new> }`). `assignee_name` text mirrors `staff_id` for display fallback.
- `task_events` — append-only; `schema_version: 1` required via `withTaskEventSchema` from `lib/task-events.ts`. 16 event types; see `docs/TASK_EVENTS_CONTRACT.md`.
- `task_checklist_items` — staff can only check/uncheck (toggle `done`); `done_at` timestamptz auto-set on done flip via trigger; powers D-430 brow "Done · 1:18 PM" meta.
- ~~`task_comments`~~ — DROPPED Day 51 chase #5 via `drop_task_comments.sql`. Pre-beta dev data only; no readers post-Day-27.
- `task_drafts` — agent dry-run staging; mirrors `tasks` shape with FKs deliberately dropped (drafts survive deletion of referenced rows); RLS service-role-only. Written by orchestrator when `AGENT_DRY_RUN=true`; promoted via SQL helper (see Orchestrator dry-run pipeline below).
- `profiles` — `role` ∈ {`admin`, `manager`, `staff`} default `manager`; new `auth.users` get a default-`manager` profile via `handle_new_user_profile()` trigger. **Operational gotcha**: every new authed user starts as manager unless updated.
- `staff` — staff directory; `clocked_in_at` timestamptz column flips drive the clock-in/out lifecycle. Vestigial `staff_outcomes` companion table from early MVP.
- `reservations` — full schema in `reservations_br1.sql`. `status` ∈ {`confirmed`, `arrived`, `departed`, `cancelled`, `no_show`}; brief queries filter on `(confirmed, arrived)`. `source` ∈ {`resnexus`, `manual`, `walk_in`} (needs `cloudbeds` add per V.H — one-word change). `nights` is generated: `greatest(1, departure_date - arrival_date)`; same-day stays count as 1. 5 partial indexes tuned for the three brief queries. Powers daily brief counts on staff/admin home + X-430 guest fields. Day 14 ResNexus manual import bridge through `inbound_events` is the population path pre-Cloudbeds.
- `inbound_events` — append-only raw event queue; service-role-only RLS; `inbound_events_dedup` UNIQUE on `(source, external_id, event_type, event_date)`. Powers the orchestrator's read loop. `staff_clock_in_event_trigger` writes `shift_start` / `shift_end` here with `source='clock_in'`. Dedup constraint silently swallows same-day re-clocks (acceptable for beta single-property).
- `deep_clean_history` — per-room rolling history of completed deep-clean items; surfaces in future D-430 cards for the same room within 30 days. Spec D-430 R34-R36. Append-only by staff (must own `source_task_id`); admin/manager can correct.
- `notes` + 4 taxonomies (`note_types` × 11, `note_statuses` × 5, `note_assigned_to` × 5, plus the shared `maintenance_severities`).
- `maintenance_issues` + 4 taxonomies (`maintenance_locations` × 21, `maintenance_items` × 11, `maintenance_types` × 10, `maintenance_severities` × 3).

**Views:**
- `staff_shifts_v` — pairs `shift_start` + `shift_end` events from `inbound_events` (LATERAL by timestamp, not event_date — handles cross-midnight). `is_current = (shift_end_at IS NULL)`; `duration_minutes` is NULL for in-flight shifts.
- `staff_segments_v` — Wed-anchored 14-day buckets aggregated from `staff_shifts_v`. Reference Wednesday `2026-01-07`. Math: `segment_start = reference_wed + FLOOR((shift_date - reference_wed) / 14) * 14`. Excludes currently-clocked-in shifts (they appear once they end). Per-staff `shift_count` + `total_minutes` per (segment_start, segment_end = segment_start + 13).
- `shift_summary_v` — per-shift task counts by card_type. Six FILTER aggregates: `departures_completed` (housekeeping_turn) / `arrivals_completed` / `stayovers_completed` / `dailys_completed` / `eod_completed` / `maintenance_completed` + `total_tasks_completed`. Join window `[shift_start_at, effective_end_at]` where `effective_end_at = COALESCE(shift_end_at, now())` so in-flight shifts have a moving window. `staff_id::text` cast required (raw_payload field is text). Completion gate is `completed_at IS NOT NULL`, not `status='done'`.
- `team_roster_v` — per-staff current-state snapshot (Day 51, master plan VII.G). One row per `public.staff` row (active + inactive — consumer filters). Columns: `staff_id` / `staff_name` / `role` / `status` / `clocked_in_at` / `is_on_shift` (computed from `clocked_in_at IS NOT NULL`) / `current_task_id` / `current_card_type` / `current_task_title` / `current_task_room` / `current_task_started_at` (most-recent `in_progress` task via LATERAL `ORDER BY COALESCE(started_at, created_at) DESC LIMIT 1`) / `is_in_eod` (boolean — matches `lib/clock-in.ts:canWrapShift` definition exactly: any task `card_type='eod' AND status != 'open' AND created_at >= now() - interval '24 hours'`). Pre-positions I.H Da-430 + I.I E-430 team displays + I.I cross-staff EOD activation gate (allows `canWrapShift` to collapse the two-step staff+tasks fetch into a single `is_in_eod` query when a future chase migrates it). `tasks.staff_id` is `uuid` so no text cast needed (unlike `shift_summary_v`'s `inbound_events` text-typed `staff_id` join).

**Triggers:**
- `staff_clock_in_event_trigger` — `staff.clocked_in_at` flip → `inbound_events` row, `event_type` ∈ {`shift_start`, `shift_end`}, `source='clock_in'` (SECURITY DEFINER, `search_path` locked).
- `tasks_staff_field_guard()` — defense-in-depth alongside RLS for staff-write-blocked task fields. Permits status, timing, and `context` writes only.
- `task_checklist_staff_guard()` — second staff guard; blocks staff INSERT/DELETE on checklist items and any UPDATE that changes `task_id`/`title`/`sort_order`. Staff can only toggle `done`.
- `tasks_seed_default_checklist()` — AFTER INSERT on `tasks`; auto-inserts 7 canonical items (Open/Strip, Bed, Report/Doc, Prep, Clean, Close Out, Restock) when `card_type` IN (`housekeeping_turn`, `arrival`, `stayover`) AND `staff_id` is set AND `is_staff_report=false` (Day 49 hotfix — previously inserted 3 non-canonical items "Remove used linens" / "Replace sheets and pillowcases" / "Set up rollaway" on `housekeeping_turn` only, which mismatched the staff-card canonical UI list per `lib/staff-task-execution-checklist.ts` `DEPARTURES_CANONICAL_CHECKLIST` and silently no-op'd staff clicks on canonical rows without DB twins). **Operational gotcha**: AddTaskModal-created housekeeping/arrival/stayover tasks always get these 7. Legacy 3-item rows on pre-hotfix tasks remain in DB; they don't render in the UI (no canonical title match) but slightly bloat the row count.
- `task_checklist_set_done_at()` — sets `done_at = now()` on `done` flip false→true; clears on true→false.
- `handle_new_user_profile()` — AFTER INSERT on `auth.users`; creates default `role='manager'` profile.
- `reservations_set_updated_at()` — bumps `updated_at` on every write; stamps `cancelled_at` on status flip to `cancelled`.
- Denormalize triggers on `notes` + `maintenance_issues` — fill `room_number` + `card_type` from parent task on insert; may go stale if parent task is later edited (acceptable for beta).

**Storage:** `task-files` bucket — `public = true` (authenticated users can read by URL with no further auth check). Upload path convention: first folder must equal `auth.uid()::text` (enforced by `task_files_insert_own` policy). Compose-drawer wiring closed Day 40 (III.E + V.G chase #1, commit `84ad9c7`); upload helper at `lib/task-events.ts:52` (`uploadTaskFile`). On successful staff post with a photo attached, the helper's `publicUrl` flows to `addNote` / `addMaintenanceIssue` as `imageUrl`, and a `taskEventType.imageAttached` task_event row fires with `detail: { image_url, source: "note" | "maintenance" }`.

**Orchestrator dry-run pipeline:** the orchestrator runs in dry-run mode by default (`AGENT_DRY_RUN=true`). In dry-run, generated tasks land in `task_drafts` (service-role-only RLS) instead of `tasks` — staff home only reads `tasks`, so drafts are invisible to the UI. Manual promotion via SECURITY DEFINER helpers in `promote_drafts_to_tasks.sql`: `select promote_draft_to_task('<uuid>')` (single) or `select promote_all_drafts_from_source('<source>')` (bulk). Promotion copies the row into `tasks` and deletes the source draft (destructive). `AGENT_KILL=true` shuts the orchestrator off entirely. Both env vars default to `true` for the first Vercel deploy as a safety net (master plan VIII.A + VIII.E).

**RLS:** `auth_profile_role()` SECURITY DEFINER function avoids `profiles` policy recursion. `can_read_task(p_task_id uuid)` (defined in `cards_mvp.sql`) is the single source of truth for staff task visibility — manager/admin pass unconditionally; staff pass if the task's `staff_id` matches their `profiles.staff_id` OR they're `created_by_user_id`. Staff cannot update most task fields (title, description, priority, due_date, due_time, staff_id, assignee_name, card_type, source, template, room, location_label, expected_duration_minutes, require_checklist_complete, attachment_url, report_*); they CAN update `status`, timing fields, and `context` (the latter post-`allow_staff_context_update.sql` — D-430's departure_status chip needs it). Notes + maintenance_issues policies mirror — staff insert-self-only, admin/manager full CRUD, select gated by `can_read_task` or self-author.

**Migration files in `docs/supabase/`:** 29 total, applied via Supabase dashboard SQL editor; all idempotent except `reservations_seed.sql` (truncate to reset).

Pre-Day-24 base / early-MVP files (already applied; listed for completeness): `dispatch.sql` (vestigial `dispatch_day` daily-brief table from earliest MVP), `staff.sql` (staff + staff_outcomes table creation), `activity.sql` (created `activity_events`, dropped Day 29 by `drop_activity_events_table.sql`), `tasks.sql`, `tasks_staff_id.sql`, `tasks_priority.sql`, `cards_mvp.sql` (profiles + `auth_profile_role()` + `can_read_task()` + `tasks_staff_field_guard()` + checklist + task_events + task-files storage bucket), `milestone1_architecture_lock.sql` (paused status + tasks new columns + staff field guard rev + default-checklist-seed trigger), `fix_rls_profiles_recursion_manager_path.sql`, `inbound_events_and_task_drafts.sql` (Day 14 ResNexus manual import bridge + dry-run staging), `reservations_br1.sql`, `reservations_seed.sql` (3 arrivals / 2 departures / 4 stayovers / 1 cancelled — matches pre-BR1 hardcoded brief counts so live read renders identically to fallback), `promote_drafts_to_tasks.sql` (`promote_draft_to_task(uuid)` + `promote_all_drafts_from_source(text)` SECURITY DEFINER helpers), `add_card_type_sod_eod.sql` (8-value `tasks_card_type_check`), `allow_staff_context_update.sql` (removes `context` from staff field guard's blocked-field list), `taxonomy_tables.sql`, `notes_table.sql`, `maintenance_issues_table.sql`, `deep_clean_history.sql`, `wave_4d_context.sql` (`task_checklist_items.done_at` + `done_at` auto-set trigger + JSONB context subkey conventions documented).

Day-25-and-later files (clock-in lifecycle + view trio + Day 49 checklist seed alignment + Day 51 team roster view + Day 51 chase #5 cleanup pair): `staff_clocked_in_at.sql`, `staff_clock_in_event_trigger.sql`, `staff_shifts_view.sql`, `staff_segments_view.sql`, `shift_summary_view.sql`, `drop_activity_events_table.sql`, `checklist_seed_canonical_alignment.sql` (Day 49 hotfix — replaces 3-item legacy seed with 7-item canonical list, expands trigger firing to `housekeeping_turn` + `arrival` + `stayover`, idempotent backfill of existing open tasks via `not exists` guard), `team_roster_view.sql` (Day 51 — VII.G), `drop_task_comments.sql` (Day 51 chase #5 — destructive cleanup of pre-beta dev table; no readers post-Day-27), `cleanup_legacy_checklist_seeds.sql` (Day 51 chase #5 — destructive DELETE of pre-Day-49-hotfix 3-item legacy seed rows).

Apply order matters in two places: `taxonomy_tables.sql` BEFORE `notes_table.sql` + `maintenance_issues_table.sql` (FKs); `reservations_br1.sql` AFTER `profiles` + `auth_profile_role()` exist.

---

## Closure ledger — master plan items closed (Days 27-52)

The master plan at `docs/dispatch-master-plan.md` was authored Day 24 with State labels (BUILT / PARTIAL / UNBUILT / AUTHORING). Items below have closed since; the master plan inline labels haven't been re-annotated. This ledger overrides.

- ✓ **Day 52 chase #6 — Final wrap (V.C drop + VII.F/H/I formal-DEFER + VI.F authoritatively closed via Rules for Housekeeping match + IV.H Phase B Bryan-decisions LOCKED + 2 KB archives).** Five inventory closures + 2 doc archives + 1 small TS comment update. Driver: Bryan delivered (a) ResNexus channel-manager CSV showing the actual data shape (NOT Cloudbeds — Cloudbeds is dead), and (b) Jennifer's full **"Rules for Housekeeping"** doc which turned out to be the canonical spec the codebase was built against. **VI.F authoritatively closes** — Jennifer's Rules for Housekeeping line 106 has the exact 18-cell time-target matrix and matches `lib/dispatch-config.ts` Section 9 EXACTLY (12 non-ADA cells per Jennifer's verbatim text + 6 ADA cells confirmed Day 52 chase #4 per Q5). Jennifer's "please provide the grid for me to fill" answer to Q4 was a misunderstanding — her own doc already had the values, just nobody connected the dots until Day 52 chase #6. Closures + actions:
   - **V.C DROP** (Cloudbeds dead per Bryan; same shape as Section X.I ResNexus DROPPED Day 51 chase #7). Bryan's separate Cloudbeds quote thread killed; existing ResNexus is the actual channel manager and was always intended to be (CLAUDE.md "Future (post-beta): ResNexus channel-manager integration"). V.H pairs V.C — already in Day 51 chase #8 formal-DEFER subsection; reframe as DROP rather than DEFER.
   - **VII.F formal-DEFER as POST-BETA** (photo storage RLS tighten path). Day 40 closure already documented this as "kept as-is for beta; tighten requires switching to signed-URL async fetch with token rotation; meaningful client-side surgery for marginal beta benefit." Per Day 51 chase #7 precedent.
   - **VII.H formal-DEFER as POST-BETA** (KB versioning + change history). Already explicitly post-beta per master plan authoring; tied to II.J KB Editor (post-beta). Revisit when II.J ships.
   - **VII.I formal-DEFER as POST-BETA** (weather + holiday calendar tables). Already explicitly post-beta per master plan; optional caching, can defer to live API calls pre-beta. Revisit when V.D weather + V.E Google events APIs activate (both also post-beta).
   - **VI.F CLOSE** — `lib/dispatch-config.ts` Section 9 `DEPARTURE_TIME_TARGET_MATRIX` comment block updated with **CONFIRMED Day 52 chase #6 per Jennifer's "Rules for Housekeeping" doc line 106** preamble + verbatim quote of the 12-cell text + cross-reference to the archived doc. Removed the stale "placeholders pending her authoring pass" framing from both the explanatory comment block AND the Section 9 header comment. All 18 cells now authoritatively confirmed (12 from line 106 + 6 ADA from Q5). VI.F drops from active inventory.
   - **IV.H Phase B Bryan-decisions LOCKED** in master plan: total-room-count denominator = **21** (per ResNexus CSV row count: 9 rooms in 20s hall + 9 in 30s hall + 3 in 40s hall = 21); occupied-room-nights data source = **Adults/Children/Pets aggregate row** at the bottom of the ResNexus CSV import (much simpler than computing per-room overlap from the per-room rows). All four `DEEP_CLEAN_AUTO_TRIGGER` thresholds in `dispatch-config.ts` Section 12 (max_departures=5, min_occupancy_pct=40, lookback_days=45, max_recent_deep_items_completed=3) **validated** against Jennifer's Rules for Housekeeping line 97 — exact match. Phase B activation reframed: single-line code change once any ResNexus import path lands. Not closing IV.H Phase B inventory item (no item exists for it; Phase B is a queued single-line change), but locking the decisions removes future ambiguity.
   - **Q2 Long-term context implicit answer captured.** Jennifer's Rules for Housekeeping defines Long-term implicitly with two thresholds: **7 nights** triggers operational rules (Deep Clean elevation per line 97; Sheet Change scheduling per line 132 — "Sheet changes are scheduled for any guests staying longer than 7 nights"; modified stayover time targets per line 118 — "for guests staying longer than 7 days should take 3-8 minutes"); **14 days** triggers the "Longterm Prep" Setup notation per line 93. Bryan no longer needs to bounce back to Jennifer for Long-term context clarification (per Q2) — use 7-night for operational, 14-day for notation. VI.E Long-term scope is now resolved at the threshold level; only the actual Long-term variant CHECKLIST remains pending Jennifer's KB.
   - **Archived `docs/kb/Rules for HouseKeeping.docx.md`** (~52KB, 243 lines). This is the canonical spec the codebase was built against — most existing implementations validate exactly against it (visibility model, 14-day shift segments, hallway assignment, no-orphan rule, status percentages, Stayover time targets per status, R13 weekend rule, R12 standard load). Future sessions reference this doc for any spec ambiguity instead of waiting on Jennifer.
   - **Archived `docs/kb/channel-manager-data-shape.csv`** (ResNexus export sample). Schema-implication reference for future BR4 v2 work: reservation ID `#XXXXX` = natural primary key; multi-night stays = same ID across consecutive day columns; group bookings = same ID across multiple ROOMS (e.g., `#61604` spans 13 rooms — needs `(reservation_id, room_id)` composite or parent/child shape); `X` markers = "Future Reservation" placeholder (booked but detail-less); `B / R / C` = Block / Repair / Call (room out-of-service signals); bottom Adults/Children/Pets aggregate rows = direct occupancy data source (simpler than per-room overlap query for IV.H Phase B).
   - **One NEW post-beta item flagged from Jennifer's Rules for Housekeeping line 101** (NOT added to active inventory): "All of the items on the deep clean list should be completed at least once every 30 days when occupancy is over 50% — when they are not this should trigger a card for the admin." Currently not implemented. Same shape as VII.I residue. Logged here for future surface awareness; not chase-queue territory.

   **Inventory drops 8 → 3** (V.C + VII.F + VII.H + VII.I + VI.F = -5). Remaining 3: VI.E (Sheet Change/Pet/VIP/Long-term variant CHECKLISTS pending Jennifer KB; thresholds resolved Day 52 chase #6) + VI.G (per-task time estimates Jennifer KB ETA "within the week" per Q7) + Q21 (maintenance cascade tree Jennifer KB authoring). 5 files changed: `lib/dispatch-config.ts` + `docs/STATE.md` + `docs/dispatch-master-plan.md` + 2 new archives in `docs/kb/`. Build verify required (TS comment-only edit; TS-clean sandbox-side via `npx tsc --noEmit`); no schema, no SQL, no new deps.

- ✓ **Day 52 chase #5 — Rules-table xlsx mine (7 Q-REFERRED answers extracted; new doc `docs/kb/jennifer-rules-extraction.md` shipped).** Mined `docs/kb/Dispatch — Rules Table for Card and Section Governance.xlsx` (25 sheets, ~140 rows total) for the 7 Jennifer answers REFERRED to "the rules document" via openpyxl. New ~150-line consolidation doc at `docs/kb/jennifer-rules-extraction.md` with per-Q sections (rules-table source citation, engineering implications, closure verdict). Findings:
   - **Q6 D-430 tolerance** CONFIRMED strict-bounds (`lib/dispatch-config.ts` `DEPARTURE_TIME_TARGET_MATRIX` already uses `tolerance: 0` — matches rules-table per-cell convention; S-430 per-status tolerances also already encoded in `STAYOVER_STATUS_TIME_TARGETS`). No code change needed.
   - **Q14 daily distribution** CONFIRMED 80/20 admin-note threshold (Da-430 row 13: "If single user does ≥80% or ≤20% of team dailys → admin note"). Logged for IV.I post-beta activation; new constant slot for `dispatch-config.ts` Section 12 when IV.I lights up.
   - **Q15 housekeeper assignment policy** CONFIRMED two-tier system (Hallway+Assignment rows 6-12: primaries → stayovers/arrivals split 30s/20s with lighter-load also taking 40s; non-primaries → departures/dailys; departure within-hall priority status stack `Has Sheets > Odobanned > Stripped > Open > Checked Out`; cross-cutting bumps in 4-step order; standard load 5 dep / 10 stay / 15 daily per housekeeper). Pre-encodes IV.A + IV.B + IV.C; all three already formal-DEFER per Day 51 chase #8.
   - **Q16 hallway adjacency** CONFIRMED locked w/ admin override + audit event (Hallway+Assignment row 10). Validates existing `assignment_cross_hall_override` audit event taxonomy (Day 29 III.D Phase 1, severity `warn`). No engineering change needed.
   - **Q17 IV.H Phase B occupancy gate** THRESHOLDS CONFIRMED (D-430 row 26: 40% occupancy + 45-day window + ≤3 deep items completed — all four constants in `DEEP_CLEAN_AUTO_TRIGGER` match the rules-table values). The remaining two open questions (data source for occupied-room-nights + total-room-count denominator) are NOT in the rules table — REFRAME from Jennifer-blocked to **Bryan-actionable** (single-property single-line decision: `public.reservations` overlaps query + hardcoded room count = 21 likely). Phase B activates once Bryan picks both.
   - **Q19 Sheet Change skip** CONFIRMED two-case interpretation (S-430 row 16: "if assigned and skipped → admin note. If completed unassigned → skip next scheduled change for guest"). Forward-scheduling interpretation requires sheet-change cadence infrastructure (post-beta per Day 45). The "skipped" admin-note case pairs cleanly with the Day 52 chase #2 event vocabulary fork — query against `task_events` for `stayover_status_overridden` with `sheet_change` in `to[]` lacking subsequent `stayover_status_changed` is the skip signal.
   - **Q21 maintenance cascade tree** NOT IN RULES TABLE — Jennifer's KB authoring still owed. Existing flat dropdowns in `lib/maintenance.ts` (21 locations × 11 items × 10 types) shipped Day 33 for beta; cascade tree (per-Location → Item-set + per-Item → Type-set mappings) requires explicit per-row authoring not in the rules table. Open Jennifer #4 stays PENDING JENNIFER KB AUTHORING.

   **Inventory impact**: Open Jennifer questions 3 → 1 (Q6 + Q17 close; Q21 stays pending). Q17 reclassified from Jennifer-blocked to Bryan-actionable. Master plan inventory: 9 → 8 (the one remaining Open Jennifer is Q21 cascade tree). 1 file added: `docs/kb/jennifer-rules-extraction.md`. Markdown-only commit; no code, no SQL, no schema. **Engineering work surface remaining: ZERO non-Bryan-actionable + ZERO non-Jennifer-pending items.** All forward motion waits on (1) Bryan picks Q17 data source + denominator, (2) Jennifer delivers Q21 cascade tree, (3) Jennifer delivers VI.E variant lists + VI.F matrix + VI.G time estimates, (4) Cloudbeds sales quote unblocks V.C.

- ✓ **Day 52 chase #4 — Quick-wins bundle (4 Jennifer-answer ships; Deep Clean +1 deferred to Chase E).** Bundles four small Jennifer-answer ships from the To Do List delivery — closes 3 Open Jennifer questions + VI.E partial; the 5th sub-piece (Deep Clean +1 item per Q2) is meaningfully more complex (needs a conditional trigger seed against `context.outgoing_guest.clean_type='Deep'` at insert time + an existing-Deep-task backfill against orchestrator-elevated drafts) and gets its own chase (Chase E follow-up). **Ship 1 — Open Jennifer #9 ADA cells confirm (Q5)**: `lib/dispatch-config.ts` `DEPARTURE_TIME_TARGET_MATRIX` Section 9 — 6 `[ASSUMED] mirrors X` markers (3× ada_double, 3× ada_jacuzzi across Standard/Deep/Pet) replaced with `ADA equal to X per Jennifer Q5 (Day 52 chase #4)` per replace_all; explanatory comment block above the matrix updated to lead with **CONFIRMED Day 52 chase #4 per Jennifer Q5** and quote her answer verbatim ("ADA rooms do not take longer than their counterparts, they just have some different placement of items that need to be understood by the staff."). Cell values unchanged (already mirrored non-ADA pre-confirm); the change is the elevated `[ASSUMED]` → confirmed status. **Ship 2 — Open Jennifer #11 maintenance bucket flip (Q20)**: `components/admin/AddTaskModal.tsx` `buildContext` — maintenance card_type `staff_home_bucket` ternary changed `bucket === "maintenance" ? "start_of_day" : bucket` → `bucket === "maintenance" ? "dailys" : bucket` per Jennifer's beta interim ("if you want to assign these to Daily Tasks until then, that will work" — Phase 2 = own Maintenance section + room-located maintenance attaches to that room's departure card). Comment block expanded inline. The `card_type` itself stays `maintenance` (bucketToCardType unchanged); only the staff-home placement changes. **Ship 3 — Open Jennifer #16 Section 14 [ASK JENNIFER] flag promotion (Q25)**: `lib/dispatch-config.ts` Section 14 comment block rewritten — the two `[ASK JENNIFER]` + one `[ASK JENNIFER 2]` markers replaced with **CONFIRMED Day 52 chase #4 per Jennifer Q22 + Q23 + Q25** preamble quoting Jennifer ("Currently none of the real employees are in your system, these are all test users") plus the Phase 2 7-role schema (Admin / Management / Shift Lead / Front Desk / Housekeeping Manager / Housekeeper / Housekeeping Trainee). For beta: keep current Admin + Housekeeping bilateral with "Housekeeping Manager for now will operate the same as housekeeping other than the few and occasional adjustments outlined in the rules document" annotation. `STAFF_PRIMARY_NAMES` + `STAFF_PREFERRED_HALL` per-row comments converted from `[ASK JENNIFER]` → `test-data; Phase 2 reseats per real-staff hall`. **Ship 4 — VI.E partial VIP banner (Q2)**: New `task.context.vip: true` field per Jennifer's "VIP guests do not have specific elevated treatment, but should just be notated prominently at the top of any Arrival or Stayover cards." `components/admin/AddTaskModal.tsx`: new `vip: boolean` state slot + reset; new conditional VIP panel between NOTES and CTA pair (only renders when `bucket === "arrivals" || bucket === "stayovers"`); `buildContext` signature widened with `vip: boolean` param + sets `ctx.vip = true` only when checked AND bucket is arrivals/stayovers. New `.vipRow` CSS in `components/admin/AddTaskModal.module.css` (flex row with checkbox + label, accent-color on checkbox, disabled state). `app/staff/task/[id]/page.tsx`: extracts `vipGuest = task.context.vip === true` boolean + threads `vipGuest` prop into `<StayoversCard/>` + `<ArrivalsCard/>` (other 4 cards ignore the flag). `StayoversCard.tsx` + `ArrivalsCard.tsx`: optional `vipGuest?: boolean` prop with `= false` default; new `<div className="vip-banner">` block sits ABOVE the greeting block (first visual cue) when `vipGuest === true`; chip + text spans inside. New `.vip-banner` + `.vip-banner__chip` + `.vip-banner__text` CSS in `app/globals.css` — bucket-agnostic class, gold-on-cream gradient (`#FFF7E0` → `#FFEFC2` background + `#D4A41E` border + `#B8860B` chip with white text); 14px sans bold text in `#5C3A00`; 12px padding + 12px gap + 1.5px border + 12px radius. **Closes**: Open Jennifer #9 + #11 + #16 (3 of 6 remaining Open Jennifer questions) + VI.E partial (Sheet Change/Pet/Long-term variant lists still Jennifer-pending KB; Deep Clean +1 item deferred to Chase E). Master plan inventory: 13 → 9 (3 Open Jennifer closures + VI.E moves to "engineering-partial"). 8 files changed: `lib/dispatch-config.ts` + `components/admin/AddTaskModal.tsx` + `components/admin/AddTaskModal.module.css` + `app/staff/task/[id]/page.tsx` + `app/staff/task/[id]/StayoversCard.tsx` + `app/staff/task/[id]/ArrivalsCard.tsx` + `app/globals.css` + STATE.md + master plan. Build verify required (TS-clean sandbox-side via `npx tsc --noEmit`); no schema, no SQL, no new deps.

- ✓ **Day 52 chase #3 — VI.B canonical-list replacement (A-430 + S-430 forks per Jennifer Q3).** Day-49 hotfix `checklist_seed_canonical_alignment.sql` aliased A-430 + S-430 checklists to D-430's 7-item canonical (`Open/Strip / Bed / Report/Doc / Prep / Clean / Close Out / Restock`). Jennifer Q3 clarified this was wrong shape — A-430 + S-430 each have their own KB list distinct from departures. **`lib/staff-task-execution-checklist.ts`**: D-430 unchanged (Jennifer Q1 accepted as final); `ARRIVALS_CANONICAL_CHECKLIST` rewritten to Jennifer's 3-item list `[Open Room, Arrival Notes, Prep]`; `STAYOVERS_CANONICAL_CHECKLIST` rewritten to Jennifer's 8-item list `[Status, Open Room, Remove, Replace, Bed, Clean, Close, Card in App]` (the "Status" row asks staff to set the status pill — pill cluster rendered separately in S-430 statcard; DND / Guest Notes / What Each Status Means are KB sub-detail under Status). All three lists doc-commented to point at the matching SQL trigger arm per the Day 49 DB-trigger-vs-UI-canonical-list drift audit codification. **`docs/supabase/checklist_seed_per_card_type.sql`** (new migration, ~140 lines): `tasks_seed_default_checklist()` rewritten with per-card_type fork — housekeeping_turn arm seeds the unchanged 7 D-430 items; arrival arm seeds the 3 new A-430 items; stayover arm seeds the 8 new S-430 items. Destructive cleanup deletes the 7 D-430 canonical titles from existing arrival + stayover tasks (Day-49 hotfix wrong-shape rows; safe per Jennifer Q22+Q23 "all current data is test-user data"); housekeeping_turn rows untouched. Idempotent backfill inserts the new canonical items per card_type using `not exists` guard against case-insensitive title match. Two verification SELECTs commented at bottom (wrong-shape row count expected = 0; per-card_type item counts expected 7/3/8). Migration count 29 → 30. Closes VI.B (master plan Section VI 4 → 3 active items). Bryan applies via Supabase dashboard SQL editor; verify SELECTs confirm clean state. 2 files changed: `lib/staff-task-execution-checklist.ts` + `docs/supabase/checklist_seed_per_card_type.sql` (new) + STATE.md + master plan; build verify required (TS-clean sandbox-side via `npx tsc --noEmit`); no new deps.

- ✓ **Day 52 chase #2 — Day 21 lock REVERSAL: staff-set `stayover_status` setter restored + admin auto-complete behavior wired (closes I.G as ENGINEERING-COMPLETE).** Two-leg chase resolves the I.G chase queue (#1 + #2) end-to-end. Bryan plan-before-cut ratify via 2-question batched AskUserQuestion: (1) event-type fork = TWO separate types (`stayover_status_changed` for staff toggle + existing `stayover_status_overridden` for admin override) — cleanest filter for future percentages tracking per rules table line 88 ("staff-tracked percentages key off staff selections only — admin pre-selections do NOT count"); the staff-side handler at `StayoversCard.tsx:399` already used the literal string `"stayover_status_changed"` so this just promotes it to the typed registry. (2) Auto-complete trigger = ANY of `{dnd, desk_ok, guest_ok}` present (alone or co-selected with `sheet_change`/`done`) — matches the OR phrasing in line 88; admin co-selection error case wins (auto-complete fires anyway). **Leg 1 — staff-side toggle restored**: `app/staff/task/[id]/StayoversCard.tsx` — pills converted from `<span>` to `<button>` with `onClick={() => onToggleStayoverStatus(opt.value)}` + `disabled={!userId || statusBusy || taskDone}` + `aria-pressed={isActive}`; the void suppression block removed; the existing `onToggleStayoverStatus` useCallback handler (preserved void-suppressed since Day 21) now wires through; statcard "None active" sub-text gains "Saving…" busy state; logTaskEvent call switched from string literal `"stayover_status_changed"` to `taskEventType.stayoverStatusChanged`. CSS at `app/globals.css` `.preview-s-430 .status-pill` swapped `cursor: default; pointer-events: none;` for `cursor: pointer;` + button `appearance: none` resets + 120ms transitions on background/border/color/box-shadow + new `:hover:not(:disabled)` (border bumps to ink-soft, color to ink) + `:focus-visible` (2px status-on outline, 2px offset) + `:disabled` (cursor not-allowed, 0.55 opacity). Active pill state unchanged (sage status-on bg + box-shadow glow). **Leg 2 — admin auto-complete behavior**: `components/admin/StayoverStatusPanel.tsx` — new `AUTO_COMPLETE_KEYS = new Set(["dnd", "desk_ok", "guest_ok"])` + `intersectsAutoComplete(keys)` helper; onSubmit handler computes `autoComplete = intersectsAutoComplete(pending)`, conditionally adds `status: "done"` + `completed_at: new Date().toISOString()` to the update payload alongside the merge-safe context spread; `stayover_status_overridden` event detail gains new `auto_completed: boolean` field so future audit consumers can filter; component header doc-comment expanded to document the auto-complete behavior + the staff/admin event source distinction. **Event vocabulary**: `lib/task-events.ts` adds `stayoverStatusChanged: "stayover_status_changed"` to the `taskEventType` constant with comment block explaining the source distinction; `lib/activity-feed.ts` `TASK_EVENT_VERB` adds `[taskEventType.stayoverStatusChanged]: "updated stayover status"` (admin's existing verb stays "set stayover status" — distinguishes admin pre-set vs staff post-facto). **Contract**: `docs/TASK_EVENTS_CONTRACT.md` adds new row for `stayover_status_changed` (staff-toggle event with same from/to detail shape) + updates the `stayover_status_overridden` row to document the new `auto_completed` detail field. **Closes I.G** (master plan Section I now 0 active items): sub-item 1 closed Day 45, sub-items 2/3 close Day 52 chase #2 via auto-complete-on-admin-pre-set logic, sub-item 4 (KB-driven checklist variants for Sheet Change weekly + *** guest) pairs with VI.E and stays Jennifer-blocked. Per Day 51 chase #8 precedent, I.G formally accepted as ENGINEERING-COMPLETE-PENDING-VI.E. 7 files changed: `lib/task-events.ts` + `lib/activity-feed.ts` + `docs/TASK_EVENTS_CONTRACT.md` + `app/staff/task/[id]/StayoversCard.tsx` + `app/globals.css` + `components/admin/StayoverStatusPanel.tsx` + STATE.md + master plan. Build verify required (sandbox SWC binary missing for linux/arm64; CC handles host-side build). No schema, no SQL, no new deps.

- ✓ **Day 52 chase #1 — Jennifer "To Do List answers" delivery formal-close pass (7 closures).** Bryan delivered Jennifer's response to the 25-item Jennifer To Do List (`uploads/Jennifer To Do List answers.md`). Coordination pass against the master plan inventory + Open Jennifer questions surfaced 7 items closeable as pure inventory accounting (no code touched): **VI.A** (Q1: "That is the appropriate placeholder. This is not relevant to the product working." — `lib/checklists/variants/*.ts` placeholders formally accepted as final for beta), **VI.C** (Q8: "I will work on a list. This is not a high priority, for now use the existing placeholder." — Affirmations placeholder accepted), **VI.D** (Q9 + Q10: same pattern, placeholder accepted for SOD greetings + EOD wrap headlines), **VI.H** (Q11: "No. Staff knows what content goes in each bucket based on the category title alone." — Note Type writing examples formally declined), **Open Jennifer #13** (Q24: reference Wednesday `2026-01-07` anchor CONFIRMED — Jennifer's payroll-cadence pattern Wed Apr 29 → Tue May 12 → Wed May 13 → Tue May 26 → Wed May 27 → Tue Jun 9, walked backward by 14-day increments, lands on Wed Jan 7, 2026 exactly; Phase 2 admin override — admin sets anchor in settings — post-beta per Jennifer's same answer), **Open Jennifer #14** (Q23: "Currently none of the real employees are in your system, these are all test users" — "Courtney Manager" name format N/A as test-data), **Open Jennifer #15** (Q22: same Jennifer answer — Stray Lizzie orphan row `fc2c4280-...` no longer Jennifer-blocked; Bryan-actionable SQL DELETE remains optional hygiene). Markdown-only commit; no code, no SQL, no schema, no build verify per operating convention. Inventory 22 → 15 (7-item drop). **Engineering work surface activated:** chase queue #1/#2 (I.G) UNBLOCKED via Q18 ("This was a misunderstanding. This should not have any selected by default, and the housekeeper will select one while they are in each stayover card.") — REVERSES the Day 21 "staff cannot edit pills" lock that voided the staff-side `stayover_status` setter. Rules table (`docs/kb/Dispatch — Rules Table Handoff.md` line 88) pre-encoded this answer verbatim: "Stayover Status — staff-set, default unselected. Admin can pre-select. Pre-selection of DND, Desk OK, or Guest OK auto-completes + auto-archives the card off the staff queue. Pre-selection of Sheet Change or Done is standard admin override (staff still executes). Staff-tracked percentages key off staff selections only — admin pre-selections do NOT count." Day 21 lock contradicted the rules table; Q18 + line 88 align — Day 52 chase #2 ships the reversal. Plus 4 additional engineering chases lit up: chase #3 ships VI.B canonical-list replacement (Jennifer Q3 PROVIDED actual A-430 + S-430 lists incompatible with Day-49 hotfix D-430-aliasing); chase #4 ships quick-wins bundle (Q5 ADA cells confirm + Q20 maintenance bucket flip start_of_day → dailys + Q25 Section 14 role flags promotion + Q2 Deep Clean +1 item + Q2 VIP banner); chase #5 mines the rules table xlsx (`docs/kb/Dispatch — Rules Table for Card and Section Governance.xlsx`) for the 7 Jennifer answers REFERRED to "the rules document" (Q6 D-430 tolerance + Q14 daily distribution + Q15 housekeeper assignment + Q16 hallway adjacency + Q17 IV.H Phase B occupancy + Q19 Sheet Change skip + Q21 maintenance cascade tree). Jennifer-pending KB items (within-the-week ETA per Jennifer): VI.G + IV.F dailys.ts daily/weekly/monthly task list (Q7 + Q12 + Q13); VI.E partial Sheet Change/Pet/Long-term variant lists (Q2 — Long-term needs Bryan to clarify context to Jennifer); VI.F + IV.G + I.E D-430 18-cell time-target matrix (Q4 — Bryan-actionable: extract grid format from `lib/dispatch-config.ts` Section 9 and forward to Jennifer).

- ✓ **I.A** — Hard-lock sequential gating + drop staff-side quick-add (Day 30).
- ✓ **I.B** — Pre-Clock-In screen (Day 30).
- ✓ **I.C** — Clock-In + Wrap Shift end-to-end (4 phases, Days 30-32). Staff clocks in → `clocked_in_at` flips → trigger writes `shift_start` → orchestrator picks up → tasks generate. Wrap Shift on E-430 nulls `clocked_in_at` → trigger writes `shift_end`. Cross-staff EOD activation gate locks Wrap Shift until other on-shift housekeepers are in their EOD card.
- ✓ **III.A** — Notes compose drawer + 3-sink routing + dual-sink read pattern (Day 27).
- ✓ **III.B** — Maintenance compose drawer at staff side (Day 33, 4 phases) + activity feed source swap (Day 39 Phase 5, commit `7281b8f`). All five listed phases closed end-to-end. Phase 5 added a third parallel source to `lib/activity-feed.ts` (`fetchMaintenanceIssueItems` + `normalizeMaintenanceIssueRow`) paralleling the notes fetch — same `{ limit, authorUserId? }` shape, same reverse-chron LIMIT, same graceful-error pattern. Severity classifier `classifyMaintenanceIssueSeverity`: High → warn (matches Day 34's reassigned-as-warn precedent — bumping Normal would dominate the feed since most issues land Normal), Normal + Low → info. Message format `composeMaintenanceIssueMessage` — `{actor} reported: {type} {item} in {location}` with optional ` — {excerpt}` suffix when body is present (100-char truncation). Both `getActivityFeed` + `getActivityForUser` fan out three sources via `Promise.all`; `ActivityKind` union extended to `"task_event" \| "note" \| "maintenance_issue"`. `components/admin/ActivityFeed.tsx` gained a Maintenance option in the Kind dropdown and the row meta-badge ternary swapped to a 3-way switch (NOTE / MAINT / EVENT). The Day-33-vintage placeholder `// Future: maintenance_issues` comment block in `lib/activity-feed.ts` retired. 2 files changed, 146 insertions / 19 deletions; build clean; no schema, no deps, no CSS. Per-user feed scope: `getActivityForUser` filters maintenance by `author_user_id` (issues *the staff reported*), parallel to notes-they-authored — alternative is parent-task-assignee scope, surfaceable if Bryan wants the other read.
- ✓ **III.D** — Activity feed (admin), 7 phases end-to-end (Day 29). Three new audit event types (`assignment_cross_hall_override`, `assignment_above_standard_load`, `reshuffle_tier_changed`) + severity classification + day-grouped UI + filters + dismiss persistence.
- ✓ **III.H** — Reassignment closed end-to-end (Days 34-35). Scope A (Day 34): `reassignTask()` helper at `lib/orchestration/index.ts`. Scope B (Day 35): discrete `ReassignPanel` at `components/admin/ReassignPanel.tsx` (chip-row staff picker + required reason textarea + Reassign button). Mounted on BOTH `/tasks/[id]` (replacing the dropdown + inline reassign-on-save flow — dropdown fully removed) AND `/admin/tasks/[id]` (II.G admin task view, between Activity and CTA pair, backed by a small live assignee fetch). Single component, two surfaces. **II.G partial credit**: the Reassign admin action is wired; the rest of II.G's surface (priority chips, admin notes, activity feed, Save & Deploy) remains chase #1 territory.
- ✓ **III.J** — 14-day segment infrastructure via three views (Day 32). View-for-beta lean honored; no new tables.
- ✓ **VII.D** — Segments table-vs-view question (resolved as views, Day 32).

**Section IV:** the three Step-follow audit-event TODOs (Steps 5/6/7-follow) closed Day 29 as a side effect of III.D Phase 1.

- ✓ **II.B + II.E + II.F + II.G (non-Reassign surface)** — Section II live-data wirings cluster (Day 36, chase #1, commit `659fcc9`). All four admin pages swap hardcoded mocks for live Supabase queries. `app/admin/tasks/page.tsx` derives OPEN/DONE-TODAY/OVERDUE stats and HOUSEKEEPING/ADMIN/MAINTENANCE lane partitions from a single tasks fetch (limit 200) + property-TZ today/now helpers. `app/admin/page.tsx` fans out four parallel lane fetchers — Watchlist (unresolved `maintenance_issues` severity-sorted High→Normal→Low), Scheduling (clock-in snapshot from `public.staff`, on-shift first → off → inactive; honest substrate until II.K Calendar lands), Critical (priority='high' open + status='blocked' merged), Notes (recent `public.notes`). All four degrade gracefully on error. `app/admin/staff/[id]/page.tsx` swaps PROFILES const for live `public.staff` row; AVATAR_* + slug→fullName map stays (locked design tokens per master plan II.D/II.E); hero role/status/role-line all derive from `staff.role` + `staff.clocked_in_at` + `staff.status`; metrics trio derives from a per-staff tasks fetch with rooms/jobs label swap by role. `app/admin/tasks/[id]/page.tsx` adds full live task fetch + `staff(name)` embed + `created_by_user_id` → profile lookup, schema-aligned 3-chip priority (was UI-only 4-value Low/Normal/High/Critical that never matched the DB enum), editable admin notes textarea persisting to new `context.admin_notes` JSONB subkey, per-task `task_events` activity panel joined to profiles for actor names, Save & Deploy writing priority + merge-safe context atomically. `BUCKET_THEME` extended with SOD (`--sod-accent-pale` / `--sod-accent` fallback) + maintenance (sage tokens) so all 7 buckets render themed; master plan II.G "dulled-color tokens" remaining as a follow-up. ReassignPanel from Day 35 unchanged. No schema changes; no new dependencies; no CSS module changes. Build clean; 4 files changed, 1181 insertions / 456 deletions.

- ✓ **Day 38 chase #1** — SOD initial-active advance + re-touch + late-Departures reshuffle (Day 38, commits `7f80ee7` → `0dc1fb4` → `b362102` → `7173062`). Day 37's "deck never unlocks past SOD" report was misdiagnosed at the clock-in layer; root cause was at the deck-state layer. Probe-1 (`7f80ee7`) added `.select()` zero-row guards on `clockIn` / `clockOut` in `lib/clock-in.ts` to surface silent UPDATE-no-op failures; UI test confirmed clock-in works end-to-end (UPDATE lands, trigger fires, `shift_start` written to `inbound_events`). Probe-2 (`0dc1fb4`) added a visible debug strip exposing `active`, `done`, `tasks.length`, per-bucket counts; pinned the actual bug — `done` correctly contained `"sod"` on initial load (both SOD tasks done) but `active` stayed at hardcoded `useState("sod")` default, leaving SOD as both is-active + is-done with no obvious next step. Bundle commit (`b362102`) shipped three pieces: (a) `loadTasks` walks the dynamic bucket order after computing `initialDone` and `setActive`'s to the first non-done key; (b) re-touch on completed cards via `.bcard.is-done { pointer-events: auto; cursor: pointer; }`; (c) late-Departures reshuffle — new pure helpers `isPastReshuffleTime(now)` + `computeBucketOrder(now, departuresIncomplete)` re-derive a dynamic `bucketOrder` via `useMemo`, reused in deck render + `handleActionClick` advancement loop + `loadTasks` initial-active advancement. Reshuffle reuses existing `dispatch-config.ts` constants (`PRE_STAYOVER_RESHUFFLE_AT` 11:00 weekday / 12:00 weekend, `PROPERTY_TIMEZONE`, `WEEKEND_DAY_NUMBERS`) — no new config. Follow-up commit (`7173062`) added two product-rule pieces from Bryan's mid-test clarification: (d) `handleCardClick` is non-destructive — clicking a checkmarked card focuses it without removing from `done`; (e) `is-unlockable` class on bucket cards whose previous buckets in the dynamic order are all in `done`, CSS `pointer-events: auto` so the user can click directly forward to the next eligible bucket. Trade-off: explicit "re-open" / "un-complete" semantics no longer accessible via UI (view ≠ re-open per Day 38 product call; post-beta enhancement if staff needs explicit undo). UI-verified Day 38: re-touch back to SOD (focus, keep checkmark) ✓; forward to Departures via SOD action button ✓; forward to Departures via direct click on Departures (is-unlockable) ✓; advance to Stayovers after Departures action click ✓; back to Departures from Stayovers ✓. Reshuffle UI verification deferred to post-11 AM Central runtime check (Bryan completes solo). 4 commits, 3 files touched cumulative (`app/staff/page.tsx`, `app/globals.css`, `lib/clock-in.ts`); no schema changes; no new dependencies.

- ✓ **V.A BR4** — X-430 brief reservation fallback (Day 37, commit `1657144`). Per-card fallback to live reservation row when `task.context.{outgoing,incoming,current}_guest` is missing/empty. `app/staff/task/[id]/page.tsx` kicks off parallel reservation fetches in the existing notes/maintenance/checklist `Promise.all` (only when `task.room_number` is set AND `card_type` is one of housekeeping_turn / arrival / stayover); results threaded as optional props into DeparturesCard / ArrivalsCard / StayoversCard. Each card builds a derived guest record from the reservation row only when its context guest subkey is missing — `isAllNull` check on the parsed `GuestRecord` for D-430, plain `parsedGuest ?? fromReservation(...)` for A-430 + S-430. Mappings: name←guest_name (trimmed), party/guests←`${party_size} guest(s)`, nights←`${nights} night(s)`, notes←guest_notes ?? special_requests joined, checkin_time←arrival_time (HH:MM:SS slices fine through formatDueTime's HH:MM regex), nights_remaining←computed from `departure_date - todayInPropertyTz()` parsed at noon-local to avoid DST/zone rounding. `clean_type` stays null on D-430 fallback (no reservation-side source — KB-driven per-turn cleaning tier). Failures degrade to null silently via `.catch()` warn-and-return-null wrappers (briefs stay at em-dash placeholders). Two new state slots in page.tsx (`currentReservation` shared between D-430 outgoing + S-430 current; `incomingReservation` shared between D-430 incoming + A-430 incoming) — single fetch, two consumers each. 4 files changed, 218 insertions / 12 deletions. Build clean; no schema, no deps, no CSS. Unblocks I.E + I.F live guest data wirings; I.G Last Stayover Status lookup remains its own work item (sources from `tasks` history, not from `reservations`).

- ✓ **III.E + V.G** — Photo pipeline wired into staff compose drawers + read-side render (Day 40, commit `84ad9c7`). The `uploadTaskFile` helper at `lib/task-events.ts:52` (existed pre-Day-40, never UI-wired) now fires on post from both `NoteComposeForm` + `MaintenanceComposeForm`. File is OPTIONAL on both forms — submit gating unchanged. Compose forms gain a styled `<label>` button wrapping a visually-hidden `<input type="file" accept="image/*" capture="environment">`; iOS Safari surfaces "Take Photo / Choose from Library" sheet directly. State lives in `app/staff/task/[id]/page.tsx` (`noteFile` / `maintFile`) and threads through every X-430 card via props (parent owns state, cards stay pure presentational pass-throughs — same shape Day 33 used for the existing dropdown state). Upload happens BEFORE row insert: if `uploadTaskFile` returns null (auth/bucket/storage failure), fail-loud — abort post and surface "Photo upload failed. Try again, or remove the photo and post the note/issue without it." On successful upload, `publicUrl` flows to `addNote` / `addMaintenanceIssue` as `imageUrl`, and a `taskEventType.imageAttached` task_event row fires with `detail: { image_url, source: "note" | "maintenance" }` — first call site for `imageAttached` in v1 vocabulary; surfaces in admin per-task event panel on `/admin/tasks/[id]`. Read-side `<img>` thumbnail render added to note + maintenance row blocks in all 6 X-430 cards (Departures / Arrivals / Stayovers / Dailys / StartOfDay / EOD) + the legacy fallback execution surface in `page.tsx`. Thumbnail wrapped in `<a target="_blank">` so tapping opens full-size; max-width 200px so a 4032×3024 phone photo doesn't blow out the row layout. New CSS in `app/globals.css`: `.staff-attach-row` / `.staff-attach-btn` / `.staff-attach-input` (visually hidden) / `.staff-attach-name` / `.staff-attach-clear` for the file-input chrome, plus `.staff-photo-thumb-link` / `.staff-photo-thumb` for the row thumbnail. **VII.F audit decision logged** (storage RLS policies, `cards_mvp.sql:373-382`): kept as-is for beta. Existing two policies (`task_files_read` allows authenticated SELECT on any object; `task_files_insert_own` requires first folder = `auth.uid()::text` on INSERT) plus the public bucket are appropriate for single-property scope — note + maintenance photos are operational artifacts (broken TV, stained linen), not sensitive PII. Tighten path (scope SELECT through `can_read_task` so non-author non-admin can't enumerate URLs) requires switching from public-URL `<img src>` to signed-URL async fetch with token rotation + expiry + loading states; meaningful client-side surgery for marginal beta benefit. Logged in Standing tabled with the trade-off documented; revisit post-beta when tenancy boundaries get real. 10 files changed, 536 insertions / 9 deletions; build clean; no schema, no deps, no migration.

- ✓ **II.H** (non-master-table surface) — Admin Maintenance live-data wiring + Watchlist linking (Day 41, commit `92a68c5`). `app/admin/maintenance/[id]/page.tsx` rewritten to query `public.maintenance_issues` by `id` + resolver-name lookup via `profiles.display_name` (same pattern as `/admin/tasks/[id]`'s `created_by_user_id` resolution at line 358-372). The `ORDER` mock const dropped entirely. Five mock-vs-schema drifts reconciled: (a) 4-value `MaintPriority` (`low/normal/high/critical`) → 3-value `MaintSeverity` (`Low/Normal/High`) matching the `maintenance_severities` taxonomy seed (Critical chip dropped — no schema home); (b) 5-value `MaintStatus` (`new/in_progress/waiting_parts/waiting_vendor/resolved`) → 2-value derived from `resolved_at IS NULL` (Open / Resolved); (c) `assignee` field with name + avatar removed (no assignee column on `maintenance_issues` — issues are reports, not assignments) and the ASSIGNED hero cell replaced with ROOM (renders `room_number` or em-dash); (d) `workOrderId: "MO-0347"` invented synthetic ID replaced by `workOrderDisplayId(uuid)` returning `id.slice(0,8).toUpperCase()` — stable, derivable, no new column needed; (e) `parts: MaintPart[]` and `category: "HVAC"` fictional fields removed entirely (no parts table; `category` would have mapped to `${item} · ${type}` taxonomy values but the heroMeta line carries that already). Severity chips persist to `maintenance_issues.severity` on click via in-flight UPDATE + refetch (RLS already permits admin/manager UPDATE); chips are disabled when `isResolved` (defensive default — once resolved, severity is historical). Mark Resolved CTA writes `resolved_at = now()` + `resolved_by_user_id = authUserId` and refetches; idempotent guard re-checks `issue.resolved_at` so double-clicks no-op. Photo panel renders only when `image_url IS NOT NULL` — consumes the Day 40 III.E pipeline directly. Activity panel derives 1-2 rows from the row itself (Reported by `author_display_name` at `created_at`; Resolved by resolver at `resolved_at` if resolved) — richer `task_events` linkage deferred to v2 vocabulary widening (Day 40 `imageAttached` payload `{image_url, source}` has no maintenance row id, so a faithful join would need v2). Escalate CTA stays no-op for beta (live admin notification per master plan III.B last clause is post-beta; beta surfaces High via activity feed sort boost). New CSS `.photoLink` + `.photoImg` added to `page.module.css` (block-level link, max-height 320px image with object-fit cover + 1px border). Watchlist linking bundled: `LaneItem` type extended with optional `href?: string`; `fetchWatchlistItems` emits `href: \`/admin/maintenance/${r.id}\``; render conditionally `<Link>`-wraps when href present (preserves abstraction for future Critical/Notes lane linking). 3 files changed, 369 insertions / 175 deletions; build clean; no schema, no deps, no migration. **One TypeScript-strict cast fix landed during build verification**: `data as MaintIssueLive` → `data as unknown as MaintIssueLive` (line 179 of the new page.tsx) — the supabase-js GenericStringError workaround already used in `lib/maintenance.ts:138` and the Day 32 Phase 4d cast fix.

- ✓ **IV.H Phase A** — Wed-occupancy Deep Clean trigger (Day 43, commit `b611514`). Auto-elevates `housekeeping_turn` drafts from Standard to Deep on Wednesdays via a new pure module `lib/orchestration/deep-clean-elevation.ts` invoked from `run.ts` between `assignDrafts` and the bulk insert. Mutates `context.outgoing_guest.clean_type` Standard → Deep on qualifying drafts and emits a `deep_clean_triggered` audit event paired with the freshly-inserted row's `task_id` via the existing post-insert pairing loop. Threshold constants in `lib/dispatch-config.ts` Section 12 (`DEEP_CLEAN_AUTO_TRIGGER`: max_departures 5, min_occupancy_pct 40, lookback_days 45, max_recent_deep_items_completed 3). Spec interpretation Bryan-confirmed via AskUserQuestion: cond 4 = no prior `housekeeping_turn` task with `context->outgoing_guest->>clean_type='Deep'` for the room in lookback window (queried against `tasks`); cond 5 = ≤3 rows in `public.deep_clean_history` for the room in lookback window. Audit event name `deep_clean_triggered` (per Bryan); severity `warn` — added to `WARN_TASK_EVENT_TYPES` in `lib/activity-feed.ts` + `taskEventType.deepCleanTriggered` constant in `lib/task-events.ts` + `AuditEventKind` union extended in `lib/orchestration/audit-events.ts` + new contract row + severity classification update in `docs/TASK_EVENTS_CONTRACT.md`. **Phase A specifically: cond 3 (occupancy gate) intentionally inactive** with `[ASK JENNIFER]` block in code — data source + total room count denominator unresolved as of Day 43. Wednesdays auto-elevate at any occupancy until Phase B activates the gate. Phase B is a single-line replacement of the inline comment block plus a real query; queued behind Open Jennifer questions, not a separate chase queue slot. Idempotent: re-running elevation on already-Deep drafts is a no-op. Fail-closed: tasks lookup error blocks elevation; deep_clean_history count error returns MAX_SAFE_INTEGER (blocks). 6 files changed. Build verified clean against working tree pre-commit. No schema changes; no new dependencies; no CSS.

- ✓ **Day 43 hotfix** — checklist strike-through on `.brow`-shape staff cards (commit `fc53967`, mid-session demo prep). Pre-existing `.preview-{a,d,s}-430 .brow[data-checked="true"] .brow__label` rules applied `text-decoration: line-through` to a `display:flex` container, which doesn't render reliably across browsers when the line-through is expected to flow continuously through both flex item children (`.brow__num` number pill + bare title text node). Hotfix: wrap `{item.displayTitle}` in a `<span class="brow__title">` in ArrivalsCard / DeparturesCard / StayoversCard, then add a non-prefixed `globals.css` rule targeting `.brow__num` + `.brow__title` directly when the row is checked. Strike-through now renders consistently regardless of which preview-X-430 wrapper is in scope. Out-of-band relative to chase queue but operationally significant — staff couldn't visually confirm completed checklist items pre-fix. 4 files changed: `app/staff/task/[id]/{ArrivalsCard,DeparturesCard,StayoversCard}.tsx` + `app/globals.css`. Build clean.

- ✓ **II.A `AddTaskModal` confirmation pass** — Day 44, commit `4ad34f9`. Process pass over the modal (~95% built per Day 28 audit) plus a small bucket-model fix and a bundled `interpret.ts` cleanup. Modal previously exposed 6 chips (`arrivals/departures/stayovers/dailys/eod/maintenance`) with no separate SOD chip — Maintenance chip routed `card_type=maintenance` + `staff_home_bucket=start_of_day`, leaving managers with no path to create a vanilla SOD task without picking Maintenance and stamping the wrong card_type. Closure: prepended `start_of_day` to the `ModalBucket` type union, `BUCKET_CONFIG`, and `BUCKET_ORDER` (now 7 chips, SOD first); added a `bucketToCardType` branch returning `start_of_day` (constraint-valid per `add_card_type_sod_eod.sql`); SOD chip styled with the existing `--sod-accent` / `--sod-accent-ink` tokens to read distinct from the Arrivals yellow-amber chip without touching globals.css. `buildContext` line 110's existing maintenance-special-case logic (`bucket === "maintenance" ? "start_of_day" : bucket`) correctly resolves the new SOD chip → `staff_home_bucket: "start_of_day"` unchanged. Maintenance chip behavior preserved exactly. Bundled cleanup (Bryan-confirmed plan-before-cut via AskUserQuestion): dropped the two unreachable `general_report` entries from `lib/orchestration/interpret.ts` (CARD_TITLE_VERB + CARD_TYPE_TO_BUCKET) — `general_report` is not in the 8-value `tasks_card_type_check` constraint enum, so any orchestrator path emitting it would have failed at insert (surfaced Day 43 demo-seed SQL). Spec gaps Bryan resolved as already-correct via the same AskUserQuestion call: due_date hard-coded to today is intentional ("day-of"), null due_time is intentional ("ASAP" — card ordering rules do the work), `context.notes` is the right home for description (no separate `tasks.description` column needed). 2 files changed, 4 insertions / 2 deletions; build clean; no schema, no deps, no CSS. **Latent inconsistency NOT cut this pass:** `interpret.ts` line 42 maps orchestrator-emitted `card_type=maintenance` → `staff_home_bucket=departures` (legacy comment "maintenance lives in the departures bucket pre-beta"), while the modal routes manager-created maintenance → `start_of_day`. No active orchestrator rule file currently emits maintenance drafts, so the conflict is dead code today; flagging for future awareness, not a chase queue slot.

- ✓ **II.H Phase C** — Master-table index at `/admin/maintenance` (Day 42, commits `6f11c83` + `a20b09b`). Closes master plan §II.H second leg ("master tables: open issues by location, open issues by type"). New file `app/admin/maintenance/page.tsx` (admin-gated client component, single Supabase query against `maintenance_issues` with `.is("resolved_at", null)` always, conditional `.eq("location", X)` / `.eq("type", Y)` when filters set, `.order("created_at", { ascending: false }).limit(100)`, severity desc client-side mirroring the Watchlist convention) + sibling `page.module.css` (sage tokens + cream shell + grain noise SVG pattern reused from `[id]/page.module.css` so the two surfaces feel like one product; new blocks for filter row, row list, severity chip variants, empty/clear states). Two flat dropdowns sourced from `MAINTENANCE_LOCATIONS` + `MAINTENANCE_TYPES` static exports in `lib/maintenance.ts`, default to "All". Index alignment: `maintenance_issues_open_idx` (partial, `where resolved_at is null`) for unfiltered, `maintenance_issues_location_idx` / `maintenance_issues_type_idx` for filtered cases — all already tuned per `maintenance_issues_table.sql:60-83`. Inbound nav touches bundled: (a) `app/admin/page.tsx` Watchlist lane's conditional `+N more` static caption swapped to a `<Link href="/admin/maintenance">` ("VIEW ALL OPEN ISSUES" or "VIEW ALL · +N MORE", suppressed when watchlist is empty); (b) Maintenance lane tile on /admin home re-targeted from stale `/admin/tasks#maintenance` (the empty `card_type='maintenance'` lane partition on the tasks dashboard, a tasks-side concept distinct from `maintenance_issues` rows) to `/admin/maintenance`. The follow-up tile re-route landed in `a20b09b` after Bryan's UI verification step 2 surfaced the stale link mid-session. **5-step UI verification ran clean end-to-end Day 42** (unlike Day 41 [id] page verification which remains Bryan-solo carry-forward): Watchlist VIEW ALL link → Maintenance tile → row list with severity-ordered chips (red High → sage Normal → white Low) → filter dropdowns + CLEAR composing → row click into per-issue page. Cumulative across both commits: 4 files changed, 679 insertions / 3 deletions; build clean both passes; no schema, no deps, no migration, no globals.css additions.

- ✓ **I.G sub-item 1** — Last Stayover Status lookup + S-430 brief swap (Day 45, commit `28eb2d5`). New `lib/stayover-history.ts` with `getLastStayoverStatusForRoom(roomNumber, excludeTaskId?)` querying last 20 `stayover` / `housekeeping_turn` tasks for the room, filtering for non-empty `context.stayover_status` arrays, returning `{ statuses, recorded_at } | null`. Threaded through `app/staff/task/[id]/page.tsx` parallel fetch (gated on `isStayover && room`, mirrors V.A BR4 pattern) into `app/staff/task/[id]/StayoversCard.tsx` brief block; replaced hardcoded `"Type: —"` row with `"Last status: <formatLastStatus(lastStayoverStatus)>"`. Display helpers (`statusBriefLabel` switch keyed by stayover_status keys → short label e.g. `dnd → "DND"`; `formatRelativeDate` with today / yesterday / N-days-ago semantics; `formatLastStatus` composing `<label> · <relative>` with `"—"` fallback for null), new optional prop with default null. **Defensive forward-compat shape**: no setter writes `context.stayover_status` today (Day 21 lock holds: pills display-only, no orchestrator pre-set, no admin override), so helper returns null and brief renders `"—"` — same visible result as the prior hardcoded `"—"`. When the new prerequisite chase ships (orchestrator pre-set + admin override), data flows automatically with no further edits here. **Sub-items 2 + 3 + 4 deferred**: sub-item 2 (status-driven auto-complete) and sub-item 3 status-pill skip interpretation both need the setter path; sub-item 3 forward-scheduling interpretation (master-plan I.G line 87 Open Assumption #5 — "skip next scheduled change for guest") is post-beta (sheet-change cadence infra doesn't exist); sub-item 4 (checklist variants for Sheet Change weekly + * guest) still blocks on Jennifer's KB authoring + the new prerequisite chase. 3 files changed, 181 insertions / 23 deletions; build clean; no schema, no deps, no CSS, no new task_events vocabulary.

- ✓ **VIII.A** — Vercel deploy live at https://dispatch-app-iota.vercel.app/ (Day 45). Browser-only path via vercel.com Add New Project → Import `JBTheDispatchCobj/Dispatch-App` → Hobby plan (free tier). 5 env vars set (3 Supabase + AGENT_KILL=true + AGENT_DRY_RUN=true; `NEXT_PUBLIC_DEV_BYPASS` deliberately omitted — localhost-only). Supabase auth config: Site URL → prod domain, Redirect URLs allowlist gains `/auth/callback` + `/**` (existing localhost entries preserved for dev). Smoke test passed in incognito: magic-link round-trip → land on `/admin` (Bryan's admin role) → admin home + staff role view both render. CLI path in `docs/deployment/vercel-checklist.md` skipped mid-session in favor of web-UI flow (CLI tripped on the CC-vs-terminal OAuth + interactive-prompt boundary); checklist update to lead with web UI is a small post-beta follow-up Bryan can request. Vercel auto-redeploys from `main` pushes (verified Day 45 via the post-deploy hotfix `a287792` redeploying without manual intervention).

- ✓ **Day 45 hotfix** — dark-mode `--foreground` swap removal (commit `a287792`, mid-session post-deploy). Prod smoke test surfaced an Activity Feed contrast issue: Lizzie-event entry text rendered nearly invisible against cream background. Triage of `app/globals.css` lines 7-13 found a Next.js scaffold `@media (prefers-color-scheme: dark)` block flipping `--foreground: #171717` → `#ededed` (near-white). Admin Activity Feed text (`.activity-message`, `.activity-lede`, `.activity-muted`) inherits color from `--foreground`; admin home backgrounds are hardcoded cream (Day 20 design tokens) — near-white text on cream when the user's OS is in dark mode → invisible. Fix: deleted the dark-mode `@media` block at lines 7-13 entirely; added `color-scheme: light;` to `:root` as defense for browsers that respect the declarative property (tells the browser to use light-mode UA defaults for form controls / scrollbars). Block #2 at lines 144-148 (`.error` color override under dark mode → bright pink-red) left alone — single-property cosmetic, harmless on cream. Dispatch is now explicitly cream-locked light-mode-only at the CSS level (matches Day 20 design lock). 1 file changed, 8 insertions / 8 deletions (net +0 lines after explanatory comment block); Vercel auto-redeploys from main; build clean.

- ✓ **Day 46 chase #1 (admin override leg)** — `<StayoverStatusPanel/>` discrete-action multi-select chip panel mounted on BOTH `/tasks/[id]` (manager card view, commit `29173f2`) AND `/admin/tasks/[id]` (admin task view modal, follow-up commit `016d19a`), gated on `card_type === "stayover"`. Five chips matching `STAYOVER_STATUS_OPTIONS` from staff S-430 exactly (Do Not Disturb / Guest OK / Desk OK / Sheet Change / Done); save handler does merge-safe `{ ...context, stayover_status: pending }` spread + `logTaskEvent` with `withTaskEventSchema({ from: initial, to: pending })`. New audit event type `stayover_status_overridden` added to `taskEventType` registry in `lib/task-events.ts` + verb entry "set stayover status" in `lib/activity-feed.ts` `TASK_EVENT_VERB` + vocabulary row in `docs/TASK_EVENTS_CONTRACT.md`. Severity `info` default — admin overrides are routine, not warning signals; not added to `WARN_TASK_EVENT_TYPES`. Component CSS module mirrors `ReassignPanel.module.css` chip / panel / submit conventions verbatim minus the textarea styles (no reason field on this panel). Bryan-verified end-to-end Day 46 on localhost — both surfaces render the panel correctly (admin task view modal shows it between ACTIVITY and REASSIGN; manager card view shows it between ReassignPanel and the big card-edit form), save flows write the array shape, activity panel above refreshes inline via `onSuccess` calling both `loadTask()` + `loadActivity()`. Per Day 46 plan-before-cut decisions: orchestrator pre-set side intentionally deferred (heuristic undefined in Jennifer's KB; carry-forward yesterday's status was rejected at planning time because DND flips constantly with guest preference). Once orchestrator side ships, `lib/stayover-history.ts` (Day 45) reads from past stayover/housekeeping_turn rows that have non-empty `stayover_status` arrays so this panel's writes auto-populate the S-430 brief "Last status" row on subsequent stayover cards for the same room. **Surface-gap lesson:** the Day 35 ReassignPanel precedent established that discrete admin panels mount on BOTH `/tasks/[id]` AND `/admin/tasks/[id]`; Day 46 first commit `29173f2` only landed the first surface, follow-up commit `016d19a` added the second after Bryan's UI verification screenshot caught the gap. Forward rule for any future discrete-action admin panel: scan both `app/tasks/[id]/manager-card-detail.tsx` AND `app/admin/tasks/[id]/page.tsx` during the placement plan; the closure-ledger precedent line is the canonical answer. Cumulative across both commits: 8 files changed, 335 insertions / 2 deletions; build clean both passes; no schema, no deps, no globals.css.

- ✓ **Day 50 — Vercel checklist rewrite** (commit `2012674`). Closes the "Vercel checklist update" bullet from the master plan's Day 49 Current State Section VIII inventory; the Day-45-codified follow-up carried Days 46/47/48/49. `docs/deployment/vercel-checklist.md` rewritten to lead with the web UI flow Bryan actually used Day 45 (vercel.com → Add New Project → import `JBTheDispatchCobj/Dispatch-App` → Hobby plan → 5 env vars during configure → click Deploy); CLI flow demoted to a short pointer at the bottom documenting the CC-OAuth caveat (interactive `vercel login` tripped on Cursor's terminal). Step 5 spells out all four Supabase Redirect URLs entries explicitly — `http://localhost:3000/{auth/callback,**}` + `https://dispatch-app-iota.vercel.app/{auth/callback,**}` — with login-page `emailRedirectTo` rationale (login at `app/login/page.tsx:74` sends `${window.location.origin}/auth/callback` dynamically, so allowlist needs both for dev `npm run dev` + prod from the same codebase). Step 7 added staff-user provisioning SQL pattern matching STATE.md's architectural rule (`update public.profiles set role='staff', staff_id='<uuid>' where id='<auth_uid>'` after manual auth-user insert + default-manager profile trigger). Markdown-only commit; build verify skipped per operating convention. 1 file changed, 78 insertions / 73 deletions.

- ✓ **VII.G — Team roster derived view** (Day 51). New SQL migration `docs/supabase/team_roster_view.sql` defines `public.team_roster_v` — one row per `public.staff` row (active + inactive both; consumer filters), with `staff_id` / `staff_name` / `role` / `status` / `clocked_in_at` / `is_on_shift` (computed) / `current_task_id` / `current_card_type` / `current_task_title` / `current_task_room` / `current_task_started_at` (most-recent `in_progress` task via LATERAL `ORDER BY COALESCE(started_at, created_at) DESC LIMIT 1`) / `is_in_eod` (boolean — matches `lib/clock-in.ts:canWrapShift` definition exactly: any task `card_type='eod' AND status != 'open' AND created_at >= now() - interval '24 hours'`). Pattern + naming + verification SELECT + idempotent BEGIN/COMMIT block all mirror the Day-32 view trio (`staff_shifts_v` / `staff_segments_v` / `shift_summary_v`). `tasks.staff_id` is `uuid` so no text cast needed (unlike `shift_summary_v`'s `inbound_events` text-typed `staff_id` join). Pre-positions three consumers when they activate: I.H Da-430 Team Update section (currently a "Coming soon" placeholder per `EODCard.tsx:272-279`'s "no roster data available" comment — same shape will exist in `DailysCard.tsx`), I.I E-430 Team Updates section (same shape), and I.I cross-staff EOD activation gate (`lib/clock-in.ts:canWrapShift` can collapse its two-step `staff` + `tasks` fetch into a single `is_in_eod` query when a future chase migrates it). Optional consumer migration not in scope: `app/admin/page.tsx:fetchSchedulingItems` could swap its direct `public.staff` query for the view to share substrate. Build verify skipped per operating convention (SQL migration + STATE.md/master plan markdown only; build state inherits from `c4bbf06`'s last clean build). Bryan-applied via Supabase dashboard; verification SELECT returned 5 rows (orphan-Lizzie 'Ops' on-shift with stale eod task surfacing as `current_card_type='eod'` AND `is_in_eod=false` — view design validated end-to-end since the two columns earned their split: `current_*` is for UI visual signal regardless of staleness, `is_in_eod` is for the canWrapShift 24h-gate semantics).

- ✓ **VII.B — Maintenance issues table verification** (Day 51 chase #2, audit pass). Verified `docs/supabase/maintenance_issues_table.sql` + `docs/supabase/taxonomy_tables.sql` against the master plan III.B + Global Rules R12-R17 spec. Schema is complete and well-aligned: `severity` column (line 38-39) with FK to `maintenance_severities` taxonomy (3 values: Low/Normal/High, default 'Normal'); `location`/`item`/`type` FKs to their respective taxonomies (21/11/10 values seeded); 7 indexes including the partial `severity_high_idx WHERE severity='High'` (line 72-74) and partial `open_idx WHERE resolved_at IS NULL` (line 80-82) tuned for the master-table queries that landed Day 42; denormalize trigger `maintenance_issues_denormalize_trg` pulls `room_number` + `card_type` from parent task on insert (line 89-109); 4 RLS policies (insert/select/update/delete) mirroring `notes` policies exactly (line 117-143). Consumer track record validates: Day 33 maintenance compose drawer + Day 39 Phase 5 activity-feed source swap + Day 41 II.H non-master-table surface + Day 42 II.H Phase C master-table index all shipped against this schema with zero friction. The "+ extension" half of the master plan item is post-beta polish (e.g., `assignee` column when assigned-vendor workflow lands; a `parts` table when work-order-grade tracking is needed) — both explicitly out of beta scope per Day 41 closure note's "MaintPart[] / parts table fictional fields removed entirely (no parts table)" cleanup. No code, no SQL, no schema changes. Closes as VERIFIED for beta.

- ✓ **VII.E — Activity feed events table-or-view decision** (Day 51 chase #2, decision formalized). Master plan framing was "table-or-view decision; view probably wins for beta," but the actual Day-29 decision was a third option: a runtime fan-out helper at `lib/activity-feed.ts` that fires three parallel fetches (`task_events` + `notes` + `maintenance_issues`) via `Promise.all`, merges in TS, derives severity from the `docs/TASK_EVENTS_CONTRACT.md` "Severity classification" block (not from a stored column), sorts severity-boost-then-reverse-chrono, and slices. Lines 24-27 of `lib/activity-feed.ts` already document this implicitly ("A server-side view UNIONing the sources [master plan VII.E] is the cleaner long-term shape; deferred to post-beta per the Day 28 handoff. Refactor here if profiling shows the merge is a hot spot."); VII.E formalizes the decision explicitly. Reasons view didn't win at beta scale: (1) severity is DERIVED per-row in code per the contract, not a column on any source row — a view would have to either inline the same classification logic per-row in SQL or skip severity entirely; (2) display-name resolution joins through `profiles`, whose RLS uses `auth_profile_role()` SECURITY DEFINER — fine in TS, awkward in a view; (3) the three sources have asymmetric per-row shapes; UNION would force a wide nullable projection or careful per-source coalesce; (4) profiling shows the merge is NOT a hot spot at beta scale (4-staff, 21-room hotel); (5) adding a future fourth source (e.g., `audit_log` per the comment block at `lib/activity-feed.ts:16`) is a single fetch + merger update with no schema change. Day 29 dropped `activity_events` (the only candidate table) per `docs/supabase/drop_activity_events_table.sql` since it had zero readers and was structurally inadequate (no FK to tasks, no user_id, no detail jsonb, no severity column). Revisit triggers (post-beta): profiling shows merge as hot spot (≥10 staff, multi-property, sustained ≥1 Hz refresh) OR severity contract moves to DB-stored OR multi-property tenancy needs property-scoped indexes JS-side filtering can't satisfy. No code, no SQL, no schema changes. Closes as DECIDED — runtime fan-out helper, neither table nor DB view; revisit when profiling/scale demands it.

- ✓ **VII.A — Supply needs table-or-view decision** (Day 51 chase #3, decision formalized). Master plan framed as "Supply needs table (or derived view from `notes` where `note_type='Supply'`). Decision before adding." Decision: NO separate `supply_needs` table for beta — derived from `public.notes WHERE note_type='Supply'`. Confirmed `docs/supabase/notes_table.sql:23` has `note_type text not null references public.note_types(name)`, and `docs/supabase/taxonomy_tables.sql` seeds `('Supply', 60)` as one of the 11 note_types. Existing `notes_type_status_idx (note_type, note_status, created_at desc)` at `notes_table.sql:53-54` already covers the query path. The R25 dual-sink pattern (per-staff log + category card grouping) gives consumers free filtering with no fan-out write. Future view-vs-inline-query call (does I.I E-430 Supply Needs sink wrap the filter in a `supply_needs_v` view, or query inline?) deferred to when the I.I consumer activates — small refactor either way, no architectural commitment needed today. No code, no SQL, no schema changes. Closes as DECIDED — derived from notes; no separate table.

- ✓ **Day 51 chase #9 — VIII.G backup/restore docs ship + IX.E idempotency audit close (2 closures).** New `docs/deployment/backup-restore.md` (~95 lines) documents Supabase Hobby auto-backup coverage + restore procedure + test-restore lane + env var manual restore + post-beta Pro upgrade considerations — closes master plan VIII.G as engineering-actionable shipped. IX.E formally closed as AUDIT-COMPLETE-PENDING-CLOUDBEDS — existing idempotency footprint surveyed (`inbound_events_dedup` UNIQUE for reservations + clock-in events, `task_events` append-only, photo storage uid-prefix paths, photo upload fail-loud per Day 40, GitHub Actions cron retry); new Cloudbeds webhook idempotency-key work pairs with V.C chase queue #3. Markdown-only commit. Closes Section VIII (1 → 0 active items) and Section IX (1 → 0 active items). Headline 24 → 22. **Engineering work surface remaining: ZERO non-external-blocked items.** All forward motion now waits on Jennifer (KB authoring lane + 9 Open Jennifer questions) OR Cloudbeds (sales quote → V.C → V.H → IX.E webhook idempotency work).

- ✓ **Day 51 chase #8 — Mass formal-DEFER pass on Sections I + II + III + IV + V (30 closures).** Largest single-chase formal-DEFER pass on Dispatch — clears the post-beta polish + KB-blocked + CLAUDE.md-cut items across five master plan sections in one markdown commit. Section I (5 of 6 deferred; I.G stays in chase queue): I.D/E/F/H/I all formally accepted as ENGINEERING-COMPLETE-PENDING-KB-AND-EXTERNAL — the engineering shells shipped Days 33-49 (`app/staff/task/[id]/{StartOfDay,Departures,Arrivals,Dailys,EOD}Card.tsx`); remaining work is content/data-layer wirings blocked on Jennifer's KB authoring lane + V.D/V.E external APIs + downstream sub-blockers (II.F admin master, IV.F rule files, VI Jennifer content). Section II (4 of 4 deferred): II.I admin category cards as POST-BETA POLISH (10-card pattern copy from Maintenance), II.J/K/L all explicitly post-beta per master plan authoring intent. Section III (6 of 6 deferred): III.C blocked on II.J, III.F needs III.I + new design, III.G within-card section gating is post-beta polish (bucket-level was beta-essential, shipped Day 30), III.H/IV.D card rotation post-beta enhancement, III.I/IV.J meta-trigger post-beta engine work, III.K audit/archive UI post-beta (Day 51 chase #6 shipped `/admin/maintenance/resolved` covering the most-pressing case). Section IV (10 of 10 deferred): IV.A auto-assignment policies XL post-beta-foundational (beta uses AddTaskModal manual creation), IV.B/C/D pair IV.A, IV.E/G/H-Phase-B Jennifer-blocked, IV.F dailys.ts+eod.ts shells doable but blocked on Jennifer's content, IV.I CLAUDE.md beta-cut "dynamic daily reassignment", IV.J pairs III.I. Section V (5 of 6 deferred; V.C stays in chase queue #3): V.B BR5 reservation edges post-beta robustness, V.D/V.E weather + Google events APIs post-beta data-layer integrations, V.F holiday calendar Bryan-leans-defer, V.H Cloudbeds payload one-word enum pairs V.C. **Master plan inventory drops 45 → 15 master plan items** (the 15 remaining: I.G chase queue, V.C chase queue, VI 8 Jennifer items, VII 3 post-beta residue, VIII.G writeable, IX.E audit-active). Combined with chase #7, Day 51 has formally accepted 41 items across Sections I-V + VIII + IX + X — the inventory now honestly reflects the beta-scope reality (CLAUDE.md's 5 ruthless beta items all shipped per Day 45 deploy; everything else is post-beta polish or external-blocked). No code, no SQL, markdown-only commit. Headline 54 → 24 (30-item drop).

- ✓ **Day 51 chase #7 — Sections X + IX + VIII formal-DEFER pass (11 closures).** Mass formal-acceptance pass on the explicitly-post-beta master plan items. Section X (6 → 0): A in-app KB-driven card generation agent (gated on Jennifer's KB stability + II.J KB Editor), B multi-property support (beta is single-property), F advanced photo pipeline (beta single-photo via Day 40 III.E), G @mention autocomplete (beta uses note_assigned_to taxonomy), H multi-tenant/SaaS-mode (beta single-tenant), I ResNexus integration (DROPPED — dead, replaced by Cloudbeds tracked under V.C/V.H). Section IX (5 → 1): IX.A formally accepted as ONGOING OPERATIONAL PRACTICE (RLS hardening per table-add already enforced by CLAUDE.md operating convention; new `team_roster_v` Day 51 view inherits RLS from underlying tables — latest example), IX.B perf tuning POST-BETA DEFERRED (revisit at ≥10 staff or multi-property), IX.C multi-property timezone POST-BETA DEFERRED (revisit when second property comes online), IX.D error reporting SDK POST-BETA DEFERRED (CLAUDE.md explicitly disallows for beta); IX.E idempotency remains active as audit pass tied to V.C Cloudbeds. Section VIII (2 → 1): VIII.F pg_cron / Vercel cron migration POST-BETA DEFERRED (GitHub Actions works for beta; revisit when Vercel cron quotas fit OR pg_cron benefits at multi-property scale OR GitHub Actions exhibits drift); VIII.G backup/restore docs remains as engineering-actionable small markdown deliverable. No code, no SQL, markdown-only commit. Section X reaches zero items for active inventory tracking. Headline 65 → 54 (11-item drop).

- ✓ **Day 51 chase #6 — UI polish bundle (3 standing-tabled SHIP-NOW closures).** Severity chip "Saving" indicator on `/admin/maintenance/[id]` (new `panelHeadSaving` CSS class with sage-text color + `severityPulse` keyframe animation; new `chipBusy` class applied to the active chip while `severityBusy` is true; replaces the previously-invisible `panelHeadRight` SAVING text — admins now see an unmistakable pulsing indicator that the click registered). New `/admin/maintenance/resolved/page.tsx` index view (~280 lines) — mirrors the open-list master-table shape with `not("resolved_at", "is", null)` filter and `resolved_at` desc ordering; imports parent `page.module.css` via relative path so visual treatment stays identical without CSS duplication; cross-nav checkmark glyph link added to `/admin/maintenance` topbar pointing to `/resolved`; back arrow on `/resolved` returns to `/admin/maintenance` (one level up makes more sense than `/admin` for this surface). Admin-side photo thumbnail on `/admin/tasks/[id]` activity panel — extends `ActivityPanelRow` type with optional `imageUrl` field; `loadActivity` mapper extracts `detail.image_url` when `event_type === imageAttached` (matching Day 40 III.E pipeline payload shape); render block conditionally surfaces `<img>` thumbnail (max-width 180px / max-height 140px, anchored block-level link to open full-size in new tab) below the event text — parity with staff-side note/maintenance row thumbnails Day 40 shipped. 6 files changed (1 new page + 5 edits): `app/admin/maintenance/[id]/page.tsx` + `page.module.css`, `app/admin/maintenance/page.tsx`, `app/admin/maintenance/resolved/page.tsx` (new), `app/admin/tasks/[id]/page.tsx` + `page.module.css`. Standing-tabled SHIP-NOW queue 3 → 0 — all 6 SHIP-NOW items from chase #4 triage closed across chases #5 + #6. Build verify required.

- ✓ **Day 51 chase #5 — Quick-wins bundle (3 standing-tabled SHIP-NOW closures).** package.json `"type":"module"` add (eliminates `MODULE_TYPELESS_PACKAGE_JSON` Node warning on every npm command + every `node --experimental-strip-types` orchestrator run; safe because no `.js` config files use `module.exports` — `next.config.ts` is TypeScript, no other config files exist per glob); `docs/supabase/drop_task_comments.sql` (DESTRUCTIVE — drops legacy `public.task_comments` table; pre-beta dev data only with no readers post-Day-27); `docs/supabase/cleanup_legacy_checklist_seeds.sql` (DESTRUCTIVE — DELETEs the 3 non-canonical seed rows ('Remove used linens' / 'Replace sheets and pillowcases' / 'Set up rollaway') left over from the pre-Day-49-hotfix `tasks_seed_default_checklist()` trigger; rows don't render in UI today since `buildDisplayChecklist` matches by canonical title, but cleanup removes the row-count noise from `task_checklist_items`). 3 files changed (1 edit + 2 new SQL); migration count 27 → 29; standing-tabled SHIP-NOW queue 6 → 3. Bryan applies the 2 SQL migrations via Supabase dashboard. Build verify covers the package.json change.

- ✓ **Day 51 chase #4 — Standing-tabled triage** (16 closures: 15 FORMAL-DEFER + 1 DROP). Audited all 22 standing-tabled items in STATE.md and classified each as SHIP-NOW (chase #5 candidate, 6 items kept), FORMAL-DEFER (won't ship for beta, rationale + revisit trigger documented inline, 15 items moved to new "Formally deferred for beta" subsection), or DROP (1 item — verification kit data persistence already explicitly marked "acceptable; representative of production data," not a closure target). Inventory headline drops standing-tabled count 22 → 6 (only SHIP-NOW items remain in active carry-forward); chase #5 will close those 6. New STATE.md structure: three subsections under "Standing tabled" — SHIP-NOW (chase #5 queue), FORMALLY DEFERRED (accepted state with revisit triggers), surface-map reference (orientation aids that aren't closures). The 15 FORMAL-DEFER items + 1 DROP count as Day 51 chase #4 closures because they remove ambiguity from the inventory: they were "still on our radar?" before, they're "explicitly accepted as won't-ship-for-beta with revisit conditions" after. No code, no SQL, no schema changes. Markdown-only commit.

- ✓ **VII.C — Audit/archive search index strategy** (Day 51 chase #3, decision formalized). Master plan framed as "Index existing tables + `archived_at` flag, vs dedicated `audit_log`." Decision: index existing tables + leverage `task_events` for the audit trail dimension. NO dedicated `audit_log` table for beta. Reasoning: (1) `public.task_events` is already append-only with 16 event types and `schema_version: 1` per `docs/TASK_EVENTS_CONTRACT.md` — it IS the audit log; (2) the existing tables (`tasks` / `notes` / `maintenance_issues`) already carry the data III.K's UI needs to search by (date / guest / type / location / user); (3) a dedicated `audit_log` would duplicate `task_events` work; (4) III.K is read-only forever per master plan spec, so query strategy is the entire surface — no write-side concerns. III.K UI consumer (when it ships) joins the existing tables with `task_events` (already exposes per-task audit via the `/admin/tasks/[id]` activity panel pattern shipped Day 36 + Day 39) and filters in TS. `archived_at` flag on `tasks` (and possibly `notes` / `maintenance_issues`) is a small ALTER deferrable until soft-delete semantics actually land — beta uses hard-delete via the `done` terminal state + `cascade` FKs. No code, no SQL, no schema changes. Closes as DECIDED — index existing tables + task_events covers append-only audit; dedicated audit_log unnecessary for beta. III.K UI lands when authoring lane unblocks.

**Section II:** II.A (`AddTaskModal`) ~95% built per Day 28 audit + confirmation pass + SOD chip add + interpret.ts `general_report` cleanup CLOSED Day 44. II.B / II.E / II.F / II.G non-Reassign surface CLOSED Day 36. II.H non-master-table surface CLOSED Day 41; II.H Phase C master-table index CLOSED Day 42. **Section II 100% closed end-to-end.** Post-beta items II.J KB Editor + II.K Calendar + II.L Recap stay explicitly deferred to v2 lane per master plan footer.

**Section IV:** the three Step-follow audit-event TODOs (Steps 5/6/7-follow) closed Day 29 as a side effect of III.D Phase 1. IV.H Phase A Wed-occupancy Deep Clean trigger CLOSED Day 43 with cond 3 occupancy gate parked behind a new Open Jennifer question (Phase B activation).

---

## Recommended next chases (priority-ordered)

When you start a session, pick the highest-priority chase that's unblocked. Update this list when items close — move closed items to the closure ledger above.

*Day 52 chases #1-#6 closed 6 chases + 19 inventory items (cumulative 22 → 3). Cloudbeds DROPPED (V.C + V.H both moot; ResNexus is the actual channel manager and was always intended). VII.F/H/I formal-DEFER. VI.F authoritatively closed via Jennifer's Rules for Housekeeping doc match. IV.H Phase B Bryan-decisions LOCKED. KB substrate doubled — 2 new canonical archives in `docs/kb/`. Engineering work surface remaining: ZERO active. The 3 remaining items are all Jennifer-KB-pending.*

1. **VI.E variant CHECKLISTS** — pending Jennifer KB. Sheet Change / Pet / VIP / Long-term variant lists (the actual checklist content; thresholds + scope resolved Day 52). Per Q2 Jennifer is "still building these." Long-term thresholds confirmed (7 nights operational + 14 days notation per Rules for Housekeeping lines 93/97/118/132). Engineering implementation queues: `lib/staff-task-execution-checklist.ts` adds variant maps keyed by `clean_type` + `stayover_status` + `vip` flag; trigger fork conditional on context.

2. **VI.G per-task time estimates** — pending Jennifer KB ETA "within the week" per Q7 + paired Q12 + Q13. Daily/Weekly/Monthly recurring task list with per-task time targets. Pairs IV.F `dailys.ts` rule file activation when content lands.

3. **Q21 maintenance cascade tree** — pending Jennifer KB authoring (per-Location → Item-set + per-Item → Type-set mappings). Confirmed Day 52 chase #5 NOT IN rules-table xlsx; Jennifer still owes the per-Location tree. Engineering implementation queues: `lib/maintenance.ts` swap flat exports for runtime cascade filter once tree authored.

**Bryan-actionable engineering follow-ups (no longer chase-queue items, but worth tracking):**

- **Chase E — Deep Clean +1 item** (Q2 deferred from Day 52 chase #4) — needs conditional trigger seed against `context.outgoing_guest.clean_type='Deep'` at insert time + existing-Deep-task backfill. Engineering-actionable when desired.
- **IV.H Phase B activation** (Day 52 chase #6 LOCKED both decisions: total-rooms denominator = 21; data source = ResNexus CSV Adults/Children/Pets aggregate row). Single-line code change in `deep-clean-elevation.ts` once any ResNexus import path lands.
- **ResNexus CSV import path** — new chase territory if Bryan wants beta-realistic data flowing. Schema-implication reference archived at `docs/kb/channel-manager-data-shape.csv`. Group-booking shape (same reservation ID across multiple rooms) is the non-trivial piece.

---

## Reading paths by chase type

When picking up a chase, load only what you need. Total chase-specific load should fit in ~15-20K tokens — substantive product knowledge, not narrative redundancy. Tier 1 always; Tier 2 by chase type. If a chase doesn't fit cleanly, lean toward the LIGHTER load and surface the gap.

### Tier 1 — always load (every session)

- `docs/STATE.md` (this file — orientation, closure ledger, conventions)
- `CLAUDE.md` (operating manual)

That's it for orientation. Everything else is chase-specific.

### Tier 2 — load by chase type

**Section I staff card chase** (X-430 card UI, status pills, checklist variants, gating, time targets)
- `docs/dispatch-master-plan.md` §I.A-I.I (only the relevant item — skim, don't full-read)
- `app/staff/page.tsx` + `app/staff/task/[id]/page.tsx`
- The target card component: `app/staff/task/[id]/{Departures,Stayovers,StartOfDay,Arrivals,Dailys,EOD}Card.tsx`
- `lib/staff-task-execution-checklist.ts` if checklist-related
- `lib/dispatch-config.ts` Sections 9 + 12 for time targets / status time targets / triggers
- For KB-driven content: `docs/kb-spreadsheet-index.md` + the relevant tab

**Section II admin surfaces chase** (admin live-data wirings, AddTaskModal, manager card view, admin staff profile, admin tasks dashboard, admin maintenance)
- `docs/dispatch-master-plan.md` §II.A-II.I (only the relevant item — skim)
- The target page file under `app/admin/*` + any sibling `*.module.css`
- Live-data reference: `app/admin/staff/[id]/page.tsx` (already wired to the three Day 32 views — mirror this pattern for new live-data wirings)
- For data layer: `lib/notes.ts`, `lib/maintenance.ts`, `lib/activity-feed.ts`, or `lib/orchestration/index.ts` depending on what the page consumes
- For AddTaskModal: `components/admin/AddTaskModal.tsx` + `lib/assignable-staff.ts`
- Skip orchestrator code, KB docs, Section I card files — irrelevant to admin UI

**Section III cross-cutter chase** (notes, maintenance, activity feed, photo pipeline, reassignment, segments, audit/archive)
- `docs/dispatch-master-plan.md` §III item being chased
- `docs/TASK_EVENTS_CONTRACT.md` (event vocabulary contract)
- Lib pair for the cross-cutter:
  - Notes/maintenance → `lib/notes.ts` + `lib/maintenance.ts` + `lib/activity-feed.ts`
  - Per-task lifecycle (reassign, pause, complete) → `lib/orchestration/index.ts` + `lib/task-events.ts`
  - Photo → `lib/task-events.ts` (`uploadTaskFile` at line 52)
- Compose form if UI work: `app/staff/task/[id]/{NoteComposeForm,MaintenanceComposeForm}.tsx`
- Schema reference: `docs/supabase/{notes_table,maintenance_issues_table,taxonomy_tables}.sql`

**Section IV rule engine chase** (assignment policies, reshuffle, dailys/eod rule files, Wed-occupancy Deep Clean trigger, realtime dailys reassignment, repeated-instance meta-trigger)
- `docs/dispatch-master-plan.md` §IV item being chased
- `lib/orchestration/{run,interpret,assignment-policies,reshuffle,audit-events}.ts`
- The relevant rule file: `lib/orchestration/rules/{dailys,eod,arrivals,departures,stayovers,maintenance}.ts`
- `lib/dispatch-config.ts` (constants live here — Sections 9 + 12 + 14)
- `docs/TASK_EVENTS_CONTRACT.md` if adding new audit event types
- **WARNING:** orchestrator-side modules use `.ts` extensions and inline string literals to be Node-safe (Day 29 caveat). Anything in `lib/orchestration/` that imports a browser-coupled module (one that imports `lib/supabase`) compiles but fails at orchestrator runtime. Exception: `lib/orchestration/index.ts` is intentionally browser-side.

**Section V data-layer chase** (BR4 reservation fallbacks, BR5 cancellation edges, Cloudbeds payload, weather, Google events, holiday calendar)
- `docs/dispatch-master-plan.md` §V item being chased
- `lib/reservations.ts` (BR4/BR5 helpers exist here)
- `docs/supabase/cards_mvp.sql` for reservations + inbound_events schema
- The relevant card file under `app/staff/task/[id]/*Card.tsx` if wiring fallback to UI

**Section VI KB authoring chase** (Jennifer's content lane — checklist trees, affirmations, variant lists, time-target matrix, daily-task estimates)
- `docs/kb-spreadsheet-index.md`
- `docs/kb/README.md` + `docs/kb/Dispatch — Rules Table Handoff.md`
- The specific tab(s) in the governance spreadsheet at `docs/kb/Dispatch — Rules Table for Card and Section Governance.xlsx`
- `lib/checklists/variants/*.ts` if converting authored content to runtime config
- `lib/dispatch-config.ts` if updating constants from authored values
- Skip code unless converting authored content to runtime config

**Section VII schema chase** (new tables, RLS, derived views)
- `docs/dispatch-master-plan.md` §VII item being chased
- All existing migrations in `docs/supabase/*.sql` for naming conventions + RLS patterns
- `docs/supabase/fix_rls_profiles_recursion_manager_path.sql` for SECURITY DEFINER pattern reference
- `docs/supabase/notes_table.sql` or `maintenance_issues_table.sql` for the dual-sink + denormalize-trigger pattern
- The lib data-access file for the new table follows the `lib/notes.ts` / `lib/maintenance.ts` pattern

**Section VIII deploy chase** (Vercel deploy, magic-link redirect URL config, smoke test, env vars)
- `docs/deployment/vercel-checklist.md`
- Skip everything else

**Section IX quality / non-functional chase** (RLS hardening per new table, perf tuning, multi-property timezone, idempotency)
- `docs/dispatch-master-plan.md` §IX item being chased
- The specific table or module being hardened
- Reference patterns from existing RLS / idempotency in `docs/supabase/*.sql`

### When STATE.md is missing something

If you're orienting and find a load-bearing fact that's not in STATE.md (an architectural rule, Jennifer caveat, schema convention, gotcha), **add it inline before continuing the chase**. STATE.md is meant to be living, not a one-shot snapshot. Drift between STATE.md and the bedrock (master plan + schema migrations + code) is the failure mode to prevent — surfacing gaps and filling them is how that gets prevented.

If a specific question requires a back-reference into deleted handoff history, the 21 deleted handoff docs are preserved in git history. `git show HEAD~N:docs/handoff-day-X.md` retrieves any of them. Targeted retrieval, not blanket re-reading.

---

## Critical operating conventions

These are hard rules. Don't relearn them each session.

### Workflow

- **Cowork-Claude direct-writes ALL code.** CC handles only build verification + git operations + commits. This is the standing convention; do not deviate.
- **Bryan is non-developer.** Plain English, exact copy-paste prompts in fenced blocks. No placeholders unless explicitly called out.
- **Single fenced code block per CC prompt + per SQL block.** Bryan's standing preference.
- **CC build-verify chain: NO `tail` pipe.** It swallows the failure exit code and ships broken commits. Pattern: `cd ~/dispatch-app && git add . && git status && npm run build && git commit -m "..." && git push origin main && git log --oneline -3`. Use full output. If you must truncate, use `set -o pipefail && npm run build 2>&1 | tail -25 && ...`.
- **Markdown-only commits skip the build verify** — no code touched, build state inherits from prior commit.
- **Bash output is ground truth.** CC editorial commentary stays unreliable. Use `git log -3` + `git status` for ground-truth checks.
- **Migrations** live in `docs/supabase/*.sql`, applied manually via Supabase dashboard SQL editor. Idempotent.
- **When you finish work**: (1) plain English summary, (2) files touched, (3) how to verify from UI — click here, expect this, (4) any Supabase migration warning if applicable.

### Code conventions

- **Boring code wins.** No clever abstractions. No new dependencies without asking Bryan. Current deps: `@supabase/supabase-js`, `@supabase/ssr`, `next`, `react`, `react-dom`. That's it.
- **TypeScript strict.** No `any`. If tempted, stop and solve it.
- **Mobile-first CSS** at 390px viewport. Desktop is a bonus.
- **Client components** (`"use client"`) for anything interactive. Server components for static layouts.
- **Supabase client** lives at `lib/supabase.ts`. Always import from there.
- **Error handling:** show the raw Supabase error message in a `.error` element. Do not hide errors behind generic text.
- **No emojis** in code or UI unless explicitly requested.

### Module / import rules

- **Imports under `lib/orchestration/`** that target other `lib/orchestration/` modules use `.ts` extensions (Node `--experimental-strip-types` orchestrator). Imports from outside that folder use plain extensionless paths.
- **WARNING (Day 29 caveat):** anything in `lib/orchestration/` that imports a browser-coupled module (one that imports `lib/supabase`) compiles but fails at orchestrator runtime. Inline constants or extract to a Node-safe shared module. Pattern at `lib/orchestration/audit-events.ts` shows how (inlined `TASK_EVENT_SCHEMA_VERSION` + `AuditEventKind` string literals).
- **Exception:** `lib/orchestration/index.ts` IS browser-side intentionally. It's the per-task lifecycle boundary (`openCard` / `pauseCard` / `resumeCard` / `completeCard` / `requestHelp` / `addTaskComment` / `toggleChecklistItem` / `reassignTask`). It imports `lib/task-events` extensionless, which transitively imports `lib/supabase`. The orchestrator-cron-side modules (`run.ts` / `assignment-policies.ts` / `reshuffle.ts` / `interpret.ts` / `rules/*` / `audit-events.ts`) use `.ts` extensions and inline string literals to be Node-safe.

### Architecture rules (do not violate)

- **`profiles` RLS depends on `auth_profile_role()`** SECURITY DEFINER function. Never query `profiles` inside a `profiles` policy body directly.
- **Staff cannot update most task fields** (assignee, priority, due date, title, description, room, card_type, source, template, attachment, report_*). They CAN update `status`, timing fields, and `context` (the last permitted by `allow_staff_context_update.sql` — D-430's departure_status chip needs it). Defense-in-depth: RLS + Postgres triggers `tasks_staff_field_guard()` + `task_checklist_staff_guard()`. If you add a field that only managers should edit, update both the RLS policy and the relevant trigger.
- **`task_events` is append-only.** No updates, no deletes. Schema is versioned at `schema_version: 1`. Don't mutate v1; bump to v2 if you need to evolve.
- **`tasks.context` is JSONB.** The one field that absolutely must be set for staff UX is `context.staff_home_bucket`. Enforce in UI; DB default is sloppy. Conventional subkeys (`incoming_guest` / `current_guest` / `outgoing_guest` / `notes`) follow merge-safe write pattern; see `wave_4d_context.sql` comment block for the contract.
- **`tasks.card_type` constraint enumerates 8 values:** `housekeeping_turn`, `arrival`, `stayover`, `eod`, `dailys`, `start_of_day`, `maintenance`, `generic`. AddTaskModal + `lib/orchestration/interpret.ts` must respect this enum.
- **Default checklist seed** auto-inserts 7 canonical items (Open/Strip / Bed / Report/Doc / Prep / Clean / Close Out / Restock) on every new `housekeeping_turn` / `arrival` / `stayover` task with non-null `staff_id` and `is_staff_report=false` (`tasks_seed_default_checklist()` trigger; canonical list mirrors `DEPARTURES_CANONICAL_CHECKLIST` in `lib/staff-task-execution-checklist.ts` exactly so the staff-card match-by-title in `buildDisplayChecklist` finds DB twins for every canonical row). AddTaskModal-created housekeeping/arrival/stayover tasks always get these 7 — if you need a different starting set, delete + replace, don't expect a clean slate. (Day 49 hotfix — previously seeded 3 non-canonical items on `housekeeping_turn` only, see Day 49 close ledger entry.)
- **New `auth.users` default to `role='manager'`** via `handle_new_user_profile()` trigger. To add a staff user: insert auth user → trigger creates default-manager profile → manually `update public.profiles set role='staff', staff_id='<uuid>' where id='<auth_uid>'`.
- **`AGENT_KILL` + `AGENT_DRY_RUN` env vars** gate the orchestrator's live writes. Both default to `true` in production until explicitly flipped — the orchestrator writes to `task_drafts` (invisible to staff UI) until promoted via SQL helper. See "Orchestrator dry-run pipeline" under Schema in place.
- **`resolveAuthUser(session)`** returns the real Supabase user — never a forged or dev user.
- **Role-based routing** lives in `lib/profile.ts`. Managers/admins → `/`, staff → `/staff`.

### Process

- **Audit-derived items merit a grep-first sanity check.** When something is "ONE-FUNCTION-AWAY," grep the relevant `taskEventType.X` literal or function name first to find existing call sites before assuming green-field.
- **Carry-forward items can go stale.** Sanity-check carry-forward sub-items older than ~5 days against the actual code before chasing. Saves time when entries have shipped.
- **Plan-before-cut for multi-file tasks.** Propose a plan before cutting code if the task touches more than one file or is otherwise non-trivial.
- **Plan-before-cut for per-card patterns (Day 47).** Any change touching all 6 X-430 staff card files (`Departures` / `Arrivals` / `Stayovers` / `Dailys` / `EOD` / `StartOfDay`Card.tsx) merits an upstream placement question via AskUserQuestion BEFORE composing edits. Generalization of plan-before-cut for per-card pattern shape — placement decisions (above brief vs above first section vs into setstat container) compound across 6 cards if guessed wrong. Day 47 `task.context.notes` plumb-through used this rule; one question round avoided full rework on 8 files.
- **AddTaskModal-to-staff-card data-flow audit (Day 47).** Any new top-level `task.context.<field>` written by `AddTaskModal` needs an explicit plumb-through plan covering `app/staff/task/[id]/page.tsx` (extract + thread to cards) + each of the 6 staff cards (prop + render slot) BEFORE considering the field "done." Bucket-specific fields document which card surfaces them; generic fields plan all six. The `task.context.notes` field had been silently written for ~19 days before Day 47 surfaced that no card was reading it.
- **Per-bucket-token collision class (Day 47).** When a button or surface derives CSS vars from per-bucket palette tokens (e.g., `--dispatch-bg` from `header`, `--dispatch-color` from `text`), audit that the source tokens for that bucket resolve to **distinct** colors before merging. SOD's `BUCKET_CONFIG` had `header=text=accent-ink`, producing a hollowed-out dark-on-dark Dispatch button. Fix pattern: add an optional override field (e.g., `buttonText?: string`) on the config type, apply only for buckets with the collision. Same root cause as the stayovers cream-on-cream Save & Deploy bug that surfaced again as the first Day 47 hotfix.
- **Bucket-agnostic class restyle scales the cheap way (Day 48).** When a multi-card UI change is purely visual (no structural moves), restyling a single bucket-agnostic class (like `.manager-note`) propagates across all 6 cards without per-card edits. Per-card edits stay reserved for structural moves (Departures relocating `.manager-note` into setstat). One shared class + per-card structural deltas = visual cohesion at minimum file churn.
- **Multi-question AskUserQuestion batching (Day 48).** If a chase has >1 independent decision Bryan should make, batch them into one AskUserQuestion call with multiple `questions[]` entries. Same single round; constrains all downstream work in one Bryan response cycle. Day 48 used 2 questions (scope + button placement) in one call to constrain 11 files of work.
- **DB-trigger-vs-UI-canonical-list drift audit (Day 49).** When a UI canonical list (like `DEPARTURES_CANONICAL_CHECKLIST` in `lib/staff-task-execution-checklist.ts`) is matched by title against DB-seeded rows from a trigger (like `tasks_seed_default_checklist()`), audit that the trigger's seed list and the canonical list are byte-for-byte aligned BEFORE shipping the UI. Day 49 surfaced a 19-day-old gap where the trigger seeded 3 non-canonical items ("Remove used linens" etc.) while the staff cards rendered 7 canonical titles, producing silent click no-op on every row whose canonical title had no DB twin. Forward rule: any UI list that match-by-title against DB rows needs a comment pointer in both files (UI canonical → trigger location, trigger → UI canonical location), and any change to one MUST update the other in the same commit.
- **Mid-verification schema-change hotfix pivot (Day 49).** When a Bryan-fingers verification surfaces a real bug requiring a schema change, pivot from verification → diagnosis → SQL migration → Supabase apply → re-verify within the same session. Don't defer to a separate chase. The Day 49 checklist seed hotfix took ~25 min from "Restock didn't work" to confirmed working post-migration. Single SQL block + saved to `docs/supabase/`. Resume the verification chase immediately after.

### Documentation precedence

- **This `docs/STATE.md` is the canonical product state entry point.** New sessions read this first. Do NOT recreate the daily handoff series.
- **`docs/dispatch-master-plan.md`** is THE inventory ("no cuts, all of it"). State labels there are stale post-Day-24; this STATE.md's closure ledger overrides.
- **`docs/TASK_EVENTS_CONTRACT.md`** is the event vocabulary contract.
- **`docs/kb/`** is canonical for governance + Jennifer's KB authoring.
- **`docs/supabase/*.sql`** is the schema source of truth.
- **`CLAUDE.md`** is the operating manual for Cowork-Claude. `AGENTS.md` is older and superseded; `dispatch-ui-rules.md` is merged into CLAUDE.md.

---

## Open Jennifer questions

*Day 52 chases #1 + #4 + #5 + #6 closed 8 of the 9 prior questions (see Day 52 ledger for the per-Q closure trail). Day 53 chase #1 narrowed Q21 (lists landed via new VI.I substrate) and added 2 narrower questions from the Day 53 Jennifer batch ask. Current state: 3 items remaining, all authoring-shape narrower than the prior pre-Day-53 superset. Net Day 53 effect: -1 wide question, +3 narrow questions = +2 net inventory but +(2-question Bryan-can-batch-into-one-message) ask velocity.*

- **Maintenance compose drawer — flat-vs-cascade behavior** (Day 53 ask Q4). Q21 SUBSTRATE LANDED Day 53 chase #1 via VI.I (`docs/kb/Maintenance_Dropdowns.md`). Cascade-vs-flat picker behavior still pending Jennifer; recommend stay flat 3-axis pre-beta unless Jennifer chooses cascade. `lib/maintenance.ts` Day 33 ship stays as-is for beta; reconciliation against Jennifer's parent-child shape is post-beta.
- **Pet Clean per-item how-to text** (Day 53 ask Q3). Pet Clean +2 item names (Bedding + Floors) confirmed Day 52 chase #5 mining of Rules Table xlsx D-430 row 30; substrate authored Day 53 chase #1 at `docs/kb/Departure_PetClean_variant.md` with placeholder how-to scaffolding. Per-item how-to descriptions pending Jennifer.
- **Three variant Stayover checklists** — Sheet Change / \*\*\* (modified discounted) / Long-term Maintenance Stayover (Day 53 ask Q2). Standard Stayover sections authored Day 52 chase #3 (8-item canonical). Each of the three variants needs its own per-section authoring vs the standard. Long-term Maintenance Stayover note for Jennifer: Day 53 ask carries the framing context she requested in original Q2 (reduced-cadence weekly service for guests staying 14+ days — once-a-week shape vs daily refresh).

**Section VI authoring lane** (cross-reference): 2 items remaining active (VI.E + VI.G) plus **VI.I closed Day 53 chase #1**. See master plan Section VI for per-item Jennifer status. Day 53 chase #1 partial-progress on VI.E (Deep Clean + Pet Clean substrate landed in `docs/kb/`; Sheet Change/\*\*\*/Long-term variant CHECKLISTS still pending Jennifer per Day 53 ask Q2). VI.F Bryan-actionable externally (extract grid format from `lib/dispatch-config.ts` Section 9 and forward to Jennifer per Q4 — closed Day 52 chase #6). VI.G Jennifer-pending within-the-week ETA per Q7.

*Closed Day 52 chase #1: VI.A (Q1 placeholder accepted) + VI.C (Q8 placeholder accepted) + VI.D (Q9 + Q10 placeholders accepted) + VI.H (Q11 declined) + reference Wednesday `2026-01-07` anchor CONFIRMED per Q24 (matches Jennifer's payroll-cadence pattern walked backward through Wed Apr 29 → Tue May 12 → +14 → ... → Wed Jan 7 exactly) + "Courtney Manager" + Stray Lizzie orphan row both N/A per Q22 + Q23 ("Currently none of the real employees are in your system, these are all test users"). Bryan-actionable Lizzie SQL DELETE remains optional hygiene (no longer Jennifer-blocked).*

---

## Standing tabled — SHIP-NOW

*Day 51 chases #5 + #6 closed all 6 SHIP-NOW items from the chase #4 triage queue. Standing-tabled SHIP-NOW count: 0. If new items get added during future chases, they accumulate here.*

## Standing tabled — FORMALLY DEFERRED for beta (Day 51 chase #4 triage, 15 items)

Each entry below is a documented decision NOT to ship for beta, with rationale + revisit trigger. These are no longer "active carry-forward" — they are accepted state.

- **Re-key `dispatch-config.ts` Section 14 maps from full names to UUIDs.** DEFERRED — gated on Jennifer's primary-staff identity confirmation (Open Jennifer question, Section 14 [ASK JENNIFER] flags). Revisit when Jennifer's standing assignment policy lands.
- **`lib/task-event-types.ts` extraction.** DEFERRED — current duplication is 5 lines × 3 files = 15 lines, working correctly. Revisit when ≥2 new orchestrator-side modules need the same constants OR when v2 task_events vocabulary widening forces extraction anyway.
- **v1 → v2 `task_events` vocabulary widening** (admin Save & Deploy `priority`/`admin_notes` edits in activity feed). DEFERRED — admin can see the change in the next view of the card; the gap is audit-trail visibility, not data loss. Revisit if admins report missing audit trail on priority/admin_notes edits or when `audit_log` consumer (deferred per VII.C) becomes real.
- **II.G dulled-color tokens (`--sod-dull-*`, `--maintenance-dull-*`).** DEFERRED — current SOD/maintenance fallback (`sod-accent-pale` + `sage-body`) is recognizable per Day 36 closure. Revisit if Bryan + Jennifer want stricter dulled-color signature.
- **Same-day re-clock pair accuracy in `staff_clock_in_event_trigger`.** DEFERRED — `inbound_events_dedup` UNIQUE blocks second pair same day; acceptable for single-property typical-shift. Revisit when multi-shift / overnight scenarios become real (master plan IX.C multi-property tied).
- **24h `created_at` window for cross-staff EOD activation gate.** DEFERRED — acceptable for typical 7-3 / 3-11 shifts at beta hotel. Revisit when multi-shift scheduling lands.
- **`onImDone` `clockOut` is fire-and-forget on failure.** DEFERRED — admin SQL recovery is one statement (`update public.staff set clocked_in_at = null where name = '<name>'`). Revisit if recovery rate exceeds ~1/month.
- **High-severity push notification for maintenance issues.** DEFERRED — beta surfaces High via activity feed sort boost only per master plan III.B Phase 5. Revisit when push infrastructure lands (deferred under master plan IX.D error reporting).
- **E-430 not a Maintenance host (defensive prop block stays).** DEFERRED — explicit master plan I.I decision. Revisit only if Jennifer changes the spec.
- **No automatic `tasks` row on maintenance issue insert.** DEFERRED — `maintenance_issues` row IS the third sink per Day 24/33 KISS design. Revisit only if a workflow needs maintenance-as-task surface.
- **No same-day-shift dedup on maintenance.** DEFERRED — admin resolves duplicates manually via existing master-table view (Day 42 II.H Phase C).
- **Per-target-staff activity feed query** (filter on `detail->>from_staff_id` / `to_staff_id` for III.H reassign rows). DEFERRED — property-wide feed at `/admin` carries entry visibly enough for beta. Revisit when per-staff-detail audit views become required.
- **`/staff` page `now` set once at mount.** DEFERRED — acceptable for beta single-property single-shift; late-Departures reshuffle won't fire mid-session without refresh. Revisit when staff report mid-shift reshuffle gaps.
- **Explicit "re-open" / "un-complete" semantics post-Day-38.** DEFERRED — `done` state is one-way; long-press undo or "Mark not done" from card detail is post-beta enhancement.
- **`/admin/maintenance/[id]` back-arrow always points to `/admin`.** DEFERRED — Bryan-confirmed OK as-is during Day 42 5-step UI verification. Revisit if context-aware return-to-list becomes required.

## Standing tabled — surface-map reference (kept for orientation)

- Surface map: `/tasks/[id]` (manager card view, II.B) and `/admin/tasks/[id]` (admin task view modal, II.G) intentionally remain separate routes — different roles, different shells, both share `ReassignPanel` from Day 35. Reference for any future chase that touches either route.
- `PROPERTY_TIMEZONE` hardcoded `'America/Chicago'` in `staff_clock_in_event_trigger` + `dispatch-config.ts`. Master plan IX.C tracks moving to per-property column post-beta. Reference for any future timezone-related work.
- VII.F storage RLS tighten path (post-beta). Day 40 III.E + V.G chase audited the two policies in `cards_mvp.sql:373-382` and kept them as-is. Master plan VII.F is the post-beta revisit point. Reference for any future tenancy/storage hardening.

---

## Bryan working style

- Bryan pastes prompts to CC in Cursor terminal and SQL in Supabase dashboard.
- Bryan is non-developer — never assume he can read diffs or debug stack traces.
- Q4 (Jennifer's KB authoring) is "an ongoing battle." Engineering doesn't block on it; cards render shells until KB lands.
- Cloudbeds is the channel-manager direction (ResNexus dead). Bryan's separate thread.
- "Live and die by the master plan": walk it top-down per priority.
- "No cuts, all of it" by default. Items deferred go to a v2 lane; deferring is not skipping.
- Context-capacity rule: end work + write the rolling close at ~70-85% session capacity. Past that, output quality degrades.

---

## Path to "well below 3-5 weeks"

The master plan footer (Day 24) estimated 6-10 weeks of focused engineering. Day 28 audit revised to ~4-6 weeks. Days 29-45 closures (III.A + III.B all five phases + III.D + III.E + III.H both scopes + I.A + I.B + I.C + III.J + II.G Reassign + Day 36's II.B/E/F/G non-Reassign surface + Day 37's V.A BR4 + Day 38's deck-state cluster: SOD initial-active + re-touch + late-Departures reshuffle + forward-unlock + Day 39's III.B Phase 5 maintenance source swap + Day 40's III.E + V.G photo pipeline + VII.F audit pass + Day 41's II.H non-master-table surface + Watchlist linking + Day 42's II.H Phase C master-table index + Maintenance tile re-route + Watchlist VIEW ALL nav touch — Section II 100% closed end-to-end + Day 43's IV.H Phase A Wed-occupancy Deep Clean trigger (cond 3 occupancy gate parked behind Open Jennifer question) + Day 43 hotfix `.brow`-shape strike-through + Day 44's II.A `AddTaskModal` confirmation pass + SOD chip add + interpret.ts `general_report` cleanup + Day 45's I.G sub-item 1 Last Stayover Status lookup + VIII.A Vercel deploy live at https://dispatch-app-iota.vercel.app/ + Day 45 hotfix dark-mode `--foreground` swap removal) trim further. **Item I/§VIII.A Vercel deploy closed Day 45 — Dispatch is live on the public internet.** Practical path remains: chase the 3 items above back-to-back across ~1 focused week (queue held at 3 Day 45: Item I closed and replaced with the new prerequisite chase that unblocks I.G sub-items 2/3/4 — chase #1 SOD/deck-advancement Day 38, chase #1 III.B Phase 5 Day 39, chase #1 III.E + V.G Day 40, chase #1 II.H non-master-table Day 41, chase #1 II.H Phase C Day 42, chase #1 IV.H Phase A Day 43, chase #1 II.A confirmation pass Day 44, chase #1 I.G sub-item 1 + Item I Vercel deploy + dark-mode hotfix Day 45), then post-beta items (II.J KB Editor, II.K Calendar, II.L Recap, VII.H KB versioning, VIII.F-G ops) push off the critical path. Section II.J + II.K are post-beta per the spreadsheet's own marking — formal `[DEFER]` to a v2 lane satisfies "no cuts" without violating the promise.

Three multipliers held throughout: (1) batching cross-cutters that unblock multiple Section I items at once (III.B closure unblocked I.D / I.E / I.F / I.G simultaneously), (2) Section VI Jennifer authoring as a parallel non-blocking lane, (3) hour density — single focused 4-5 hour sessions can close multiple items.

---

## Last session close (Day 53, 2026-05-09)

**1 chase, 3 KB-substrate ships, +1 inventory item closed (VI.I), +2 narrower Open Jennifer questions surfaced.** Substrate-authoring session, not an inventory-collapse session like Day 51 / Day 52. Net active inventory: 3 → 5 (VI.E + VI.G + Q21 narrowed → VI.E + VI.G + Q21-narrowed-cascade + Pet-how-to + three-Stayover-variants). The +2 net is the cost of converting wide Jennifer asks into narrower routable questions she can actually answer. Cowork-Claude direct-write of three KB docs into `docs/kb/`; no code changes; no SQL.

- **Chase #1 (`e6e7f7b`) — Day 52 chase #6 follow-up: Deep Clean / Pet Clean / Maintenance Dropdowns substrate authoring + Jennifer batch ask drafted.** Chase scope was Bryan's Day-53-pre-orientation surfacing of three "shippable now" items (Deep Clean variant, Pet Clean variant, Room Spray seasonal Setup notation, Maintenance Dropdowns landed via session upload) + a single Jennifer ask consolidating remaining open questions + master plan inventory delta. Orientation-against-canonical-repo-docs revealed: (a) Room Spray already fully wired in `lib/dispatch-config.ts:244-249` (`SEASONAL_SCENTS` + `lookupSeasonalScent`) AND in `lib/checklists/variants/single_queen.ts:136-139`, AND canonical spec in `docs/kb/Rules for HouseKeeping.docx.md` line 89 — no new substrate needed, just inventory accounting; (b) Pet Clean +2 names ("Bedding, Floors") sourced from Day 52 chase #5 mining of Rules Table xlsx D-430 row 30 (per `docs/kb/jennifer-rules-extraction.md`), NOT scaffolding — Bryan's earlier "scaffolding not content" framing meant "names exist but per-item how-to text doesn't yet"; (c) Q21 is the LAST remaining Open Jennifer question per STATE.md Day 52 close — Maintenance Dropdowns substrate landing closes the content gap and narrows Q21 to flat-vs-cascade behavior only. **Three KB substrates direct-written:** `docs/kb/Departure_DeepClean_variant.md` (8-section variant shape + 7-item Deep Clean detail sub-tree with verbatim how-to from Bryan's Deep Clean Admin List CSV; closes the per-item how-to gap on Deep Clean); `docs/kb/Departure_PetClean_variant.md` (+2 item names + placeholder how-to scaffolding marked clearly as Cowork-Claude-best-guess; explicit reminder NOT to surface to staff until Jennifer-confirmed text replaces); `docs/kb/Maintenance_Dropdowns.md` (Jennifer's three lists: Location 12-parent / Item 11-parent / Type 10-flat + reconciliation map vs `lib/maintenance.ts` Day 33 ship + cascade-vs-flat behavior question routing). **Master plan delta:** VI.E text rewritten to capture Day 53 partial-progress (Deep + Pet substrate landed; Sheet Change/\*\*\*/Long-term still pending) + new VI.I closed entry (Maintenance Dropdowns substrate); STATE.md Open Jennifer questions snapshot refreshed (was stale at "9 items," refreshed to current 3 with Day 53 narrowing). **Jennifer batch ask drafted in 4 questions** (Day 53 ask Q1: Arrival Status 4th state name per Rules.md line 95 blank + Open Assumption #2; Q2: Sheet Change / \*\*\* / Long-term Maintenance Stayover variant checklist content with Long-term framing-context Jennifer requested in her original Q2; Q3: Pet Clean per-item how-to text for Bedding + Floors; Q4: Maintenance dropdowns flat-vs-cascade picker behavior). Closes VI.I same-chase via substrate landing; partial-progresses VI.E; narrows Q21; surfaces 2 new narrower Open Jennifer questions (Pet how-to + three Stayover variants). 5 files changed (3 new in `docs/kb/`, master plan + STATE.md edits); no schema, no SQL, no code, no new deps. Inventory 3 → 5 (substrate-authoring chase pattern: ships substrate + surfaces narrower follow-ups). Net velocity gain: Bryan can batch-send 4 narrow questions in one Jennifer message vs the prior shape of 1 wide unanswerable Q21 + 3 implicit-undocumented gaps.

**Day 53 process codifications:**

- **Pre-orientation-fact-check codified.** Pre-orientation Cowork-Claude artificial-context drafts can drift from actual canonical state when pre-orientation source materials are upload-snapshots ("Master Plan.md" 12kb cut vs canonical `docs/dispatch-master-plan.md` 65kb). When connecting to repo mid-session, **always re-read canonical files before applying drafted patches**. Day 53 chase #1 caught three load-bearing drifts via re-orientation: (1) Open Jennifer questions count was 9 in upload, 1 in canonical STATE.md; (2) Pet Clean +2 names sourced from chase #5 mining doc, not scaffolding-only as briefly framed mid-session; (3) Room Spray fully wired in code — no substrate needed. All three drifts would have shipped wrong content if drafted patches had been applied verbatim.
- **Substrate-authoring-as-distinct-chase-pattern codified.** Most prior chases collapsed inventory (closed items). Day 53 chase #1 is the first formal substrate-authoring chase — adds inventory in net (+2) but converts wide unanswerable questions into narrow Jennifer-can-answer-in-one-message questions. Forward rule: substrate-authoring chases are evaluated on Bryan-Jennifer-ask velocity (questions-per-batch and answer-per-question), not inventory contraction. Both metrics matter; conflating them mis-prices substrate work.
- **KB-substrate-vs-code-substrate distinction codified.** When Jennifer's KB content lands (variant lists, dropdowns, scent rotation, etc.), the right ship vector is **`docs/kb/` markdown substrate** — NOT immediate code wiring. Code wiring follows a separate orchestration chase (e.g. "Chase E" for Deep Clean conditional trigger) and depends on (a) substrate completeness + (b) Jennifer-confirm-shape + (c) Bryan-pick-implementation-shape. Day 53 chase #1 keeps `lib/checklists/variants/*.ts` and `lib/maintenance.ts` UNTOUCHED; substrate lands as KB-only. Forward rule: substrate ≠ code wiring; ship them in separate chases.
- **Never-amend-after-substituting-own-SHA codified (post-mortem from Day 53 chase #1).** First version of the Cowork-Claude → CC handoff bash block (a) committed Day 53 work with `<HEAD pending>` placeholder, (b) captured SHA1 (`7fcab6a`), (c) sed-substituted SHA1 into STATE.md, (d) ran `git commit --amend --no-edit` to fold the substitution into the same commit. Step (d) rewrote the SHA — final origin commit was `e6e7f7b`, not `7fcab6a`. STATE.md ended up referencing a SHA that doesn't exist on origin. Fix-up commit corrected the two references (the post-mortem chose not to reference its own SHA, to avoid the same trap). Forward rule: **never `git commit --amend` after substituting the original commit's own SHA into a tracked file** — the amend rewrites the SHA and creates the very drift you were trying to avoid. Two-commit pattern instead: (1) first commit ships work with `<HEAD pending>` placeholder; (2) capture SHA1 from first commit; (3) sed-substitute SHA1 into the placeholder; (4) second commit "STATE.md SHA backfill for chase #N (SHA1)" pushed as a separate non-amend commit. Adds one commit per chase but eliminates the drift class entirely.

**Carry-forward still pending Bryan-solo:**

- Same as Day 52 close (Stray-Lizzie SQL DELETE optional cleanup; Day 52 chase #3 SQL apply still pending in Supabase dashboard — see Day 52 close section for details; UI verifications for Day 52 Chases #2 + #3 + #4 unchanged).
- **Day 53 specific: send the Jennifer batch ask** — drafted at end of session (4 questions). Bryan picks delivery channel (text / email / shared doc).

**Bryan apply steps (Day 53):**

- Chase #1: no apply step (markdown-only commit; 3 new KB docs + master plan + STATE.md edits; Vercel auto-redeploys but no UI change since no code touched).

**Followups for next session:** Active inventory holds at 5 (VI.E + VI.G + Q21-narrowed-cascade + Pet-how-to + three-Stayover-variants). All 5 are Jennifer-pending KB authoring or Bryan-Jennifer-batch-ask routing. Engineering work surface remains ZERO active. Next chase tier: (a) Jennifer answers any of the 4 batch-ask questions → corresponding KB substrate lands (Pet how-to fill, Stayover variant authoring, Maintenance cascade map if she picks cascade, Arrival Status 4th state name); (b) Jennifer delivers Q7 daily/weekly/monthly task list within-the-week → VI.G + IV.F unblock together; (c) Bryan picks ResNexus CSV import path → IV.H Phase B + V.H one-line code-change activations both light up. Substrate work continues in parallel non-blocking lane.

**Honest read on beta status (Day 53 final):** Code-side beta unchanged from Day 52 — no code shipped this session. Substrate-side beta substantially advanced — three new KB substrates land; one Open Jennifer question narrows from "wide unanswerable" to "narrow flat-vs-cascade pick"; two new Open Jennifer questions are explicit-routable rather than implicit-undocumented. **Production live unchanged at https://dispatch-app-iota.vercel.app/.** Substrate-authoring path from 5 to 0 runs through Bryan-sends-batch-ask + Jennifer-answers + 2nd-substrate-authoring-chase to land her answers; ~1-2 sessions to substrate-zero pending Jennifer's response speed.

📊 Inventory: 3 → 5 | Section VI: 2 → 2 active (+1 new VI.I closed) | Open Jennifer Qs: 1 → 3 | Day 53 chase #1 sealed | HEAD `e6e7f7b`

---

## Last session close (Day 52, 2026-05-09)

**6 chases, 19 closures, inventory 22 → 3.** Eclipses Day 51's 67-item single-day record on a percentage basis (86% inventory contraction in one session vs Day 51's 75%). Drivers across the session: (a) Bryan delivered Jennifer's response to the 25-item To Do List early in the session, lighting up 5 engineering chases that had been parked for weeks; (b) Cloudbeds-is-dead clarification mid-session DROPPED V.C + V.H from inventory; (c) Bryan's late-session upload of the ResNexus channel-manager CSV + Jennifer's full **"Rules for Housekeeping"** doc validated the existing codebase against the canonical spec, authoritatively closed VI.F (the values were Jennifer's all along — Q4 was a misunderstanding), LOCKED IV.H Phase B Bryan-decisions (denominator = 21 + data source = CSV aggregate row), and shipped 2 KB archives that double the canonical-spec substrate available to future sessions. **Final state: 3 items remaining, all Jennifer-KB-pending. Engineering work surface = ZERO active.**

 Driver: Bryan delivered Jennifer's response to the 25-item Jennifer To Do List (`uploads/Jennifer To Do List answers.md`) — a single coordination pass against the master plan + Open Jennifer questions surfaces lit up the entire chase queue + 4 additional engineering chases that had been parked for weeks. Sequence:

- **Chase #1 (`15380e2`) — Jennifer "To Do List answers" formal-close pass (7 closures).** Markdown-only inventory accounting commit against Jennifer's response to the 25-item To Do List. **VI.A** (Q1 — "That is the appropriate placeholder. This is not relevant to the product working." — `lib/checklists/variants/*.ts` placeholders accepted as final), **VI.C** (Q8 — Affirmations placeholder accepted), **VI.D** (Q9 + Q10 — SOD greetings + EOD wrap headlines placeholders accepted), **VI.H** (Q11 — Note Type writing examples formally declined: "Staff knows what content goes in each bucket based on the category title alone"), **Open Jennifer #13** (Q24 — reference Wednesday `2026-01-07` anchor CONFIRMED via Jennifer's payroll-cadence pattern walked back through Wed Apr 29 → Tue May 12 → +14 → +14 → ... → Wed Jan 7 exactly), **Open Jennifer #14** (Q23 — "Courtney Manager" N/A as test-user data per "Currently none of the real employees are in your system, these are all test users"), **Open Jennifer #15** (Q22 — Stray Lizzie orphan N/A same answer; Bryan-actionable SQL DELETE remains optional hygiene). **Largest single load-bearing finding from the cross-reference**: Jennifer Q18 ("This was a misunderstanding. This should not have any selected by default, and the housekeeper will select one while they are in each stayover card") REVERSES the Day 21 "staff cannot edit pills" lock that voided the staff-side `stayover_status` setter. Rules table line 88 (`docs/kb/Dispatch — Rules Table Handoff.md`) pre-encoded this answer verbatim — Day 21 lock contradicted our own governance doc. Lit up the entire I.G chase queue + 4 additional engineering chases. Inventory 22 → 15.

- **Chase #2 (`604d669`) — Day 21 lock REVERSAL + admin auto-complete behavior (closes I.G end-to-end).** Two-leg chase resolves the I.G chase queue (sub-items 2/3) and closes I.G as ENGINEERING-COMPLETE-PENDING-VI.E (sub-item 4 KB-blocked). **Plan-before-cut via 2-question batched AskUserQuestion** (Day 48 codification): event-type fork = TWO separate types (`stayover_status_changed` for staff toggle + existing `stayover_status_overridden` for admin override), cleanest filter for future percentages tracking per rules table line 88; auto-complete trigger = ANY of `{dnd, desk_ok, guest_ok}` present (matches OR phrasing in line 88). **Leg 1 (staff-side)**: `app/staff/task/[id]/StayoversCard.tsx` pills converted from `<span>` to `<button>` with `onClick + disabled + aria-pressed`; void-suppression block removed; existing `onToggleStayoverStatus` handler (preserved void-suppressed since Day 21) wires through; statcard sub gains "Saving…" busy state. CSS at `app/globals.css` `.preview-s-430 .status-pill` — `cursor: default; pointer-events: none;` swapped for `cursor: pointer` + `appearance: none` reset + 120ms transitions + hover/focus/disabled states. **Leg 2 (admin-side)**: `components/admin/StayoverStatusPanel.tsx` — new `AUTO_COMPLETE_KEYS` set + `intersectsAutoComplete` helper; onSubmit conditionally adds `status: "done" + completed_at: now()` to the update payload when pending intersects `{dnd, desk_ok, guest_ok}`; event detail gains `auto_completed: boolean` field. **Event vocabulary**: `lib/task-events.ts` adds `stayoverStatusChanged: "stayover_status_changed"` to the registry; `lib/activity-feed.ts` `TASK_EVENT_VERB` adds `[taskEventType.stayoverStatusChanged]: "updated stayover status"` (admin's existing "set stayover status" stays — distinguishes admin pre-set vs staff post-facto). `docs/TASK_EVENTS_CONTRACT.md` adds new contract row + documents the `auto_completed` detail field. **Closes I.G** (master plan Section I 1 → 0 active items). 8 files changed; no schema, no SQL, no new deps. Inventory 15 → 14.

- **Chase #3 (`aef1866`) — VI.B canonical-list replacement (A-430 + S-430 forks per Jennifer Q3).** Day-49 hotfix `checklist_seed_canonical_alignment.sql` aliased A-430 + S-430 checklists to D-430's 7-item canonical (`Open/Strip / Bed / Report/Doc / Prep / Clean / Close Out / Restock`). Jennifer Q3 clarified this was wrong shape — A-430 + S-430 each have their own KB list distinct from departures. **`lib/staff-task-execution-checklist.ts`**: D-430 unchanged (Jennifer Q1 accepted as final); `ARRIVALS_CANONICAL_CHECKLIST` rewritten to Jennifer's 3-item list `[Open Room, Arrival Notes, Prep]`; `STAYOVERS_CANONICAL_CHECKLIST` rewritten to Jennifer's 8-item list `[Status, Open Room, Remove, Replace, Bed, Clean, Close, Card in App]`. All three lists doc-commented to point at the matching SQL trigger arm per Day 49 codification. **`docs/supabase/checklist_seed_per_card_type.sql`** (new ~140-line migration): `tasks_seed_default_checklist()` rewritten with per-card_type fork — housekeeping_turn arm seeds the unchanged 7 D-430 items; arrival arm seeds the 3 new A-430 items; stayover arm seeds the 8 new S-430 items. Destructive cleanup deletes the 7 D-430 canonical titles from existing arrival + stayover tasks (safe per Jennifer Q22+Q23 test-user-only data); housekeeping_turn rows untouched. Idempotent backfill inserts new canonical items per card_type using not-exists guard against case-insensitive title match. Two verification SELECTs commented at bottom (wrong-shape row count expected = 0; per-card_type item counts expected 7/3/8). Migration count 30 → 31. Closes VI.B (master plan Section VI 4 → 3 active items). 4 files changed (1 new SQL + 1 code + STATE + master plan); no new deps. Inventory 14 → 13.

- **Chase #4 + #5 (`a14500a`) — Quick-wins bundle (4 Jennifer-answer ships) + rules-table xlsx mine (7 Q-REFERRED answers extracted) bundled in one commit.** Bundled per file-overlap (STATE.md + master plan touched by both); Bryan's `git add .` captured both in one push. Original split was: Chase #4 quick-wins, Chase #5 xlsx mine — STATE.md closure ledger keeps both as separate ledger entries even though the commit message names only #4.
   - **Chase #4 — Quick-wins bundle (4 ships).** Ship 1 — Open Jennifer #9 ADA cells confirm (Q5): `lib/dispatch-config.ts` `DEPARTURE_TIME_TARGET_MATRIX` Section 9 — 6 `[ASSUMED]` markers replaced with `ADA equal to X per Jennifer Q5`; comment block above matrix updated to lead with **CONFIRMED** + Jennifer's verbatim answer ("ADA rooms do not take longer than their counterparts, they just have some different placement of items that need to be understood by the staff"); cell values unchanged (already mirrored non-ADA pre-confirm). Ship 2 — Open Jennifer #11 maintenance bucket flip (Q20): `components/admin/AddTaskModal.tsx` `buildContext` — maintenance card_type `staff_home_bucket` flipped `"start_of_day"` → `"dailys"` per Jennifer's beta interim ("if you want to assign these to Daily Tasks until then, that will work"); Phase 2 = own Maintenance section + room-located maintenance attaches to that room's departure card; `card_type` itself stays `"maintenance"`. Ship 3 — Open Jennifer #16 Section 14 [ASK JENNIFER] flag promotion (Q25): `lib/dispatch-config.ts` Section 14 comment block rewritten with **CONFIRMED Day 52 chase #4 per Jennifer Q22 + Q23 + Q25** preamble + Phase 2 7-role schema (Admin / Management / Shift Lead / Front Desk / Housekeeping Manager / Housekeeper / Housekeeping Trainee); for beta keep current Admin + Housekeeping bilateral with "Housekeeping Manager for now will operate the same as housekeeping" annotation. Ship 4 — VI.E partial VIP banner (Q2): new `task.context.vip: true` field. `AddTaskModal.tsx` — new `vip: boolean` state slot + reset; conditional VIP panel between NOTES and CTA (only renders when `bucket === "arrivals" || bucket === "stayovers"`); `buildContext` widened with `vip` param + sets `ctx.vip = true` only when checked AND bucket is arrivals/stayovers. New `.vipRow` CSS in `AddTaskModal.module.css`. `app/staff/task/[id]/page.tsx` extracts `vipGuest = task.context.vip === true` boolean + threads prop into `<StayoversCard/>` + `<ArrivalsCard/>` (other 4 cards ignore). Both cards gain optional `vipGuest?: boolean` prop with `= false` default; new `<div className="vip-banner">` block sits ABOVE the greeting block when `vipGuest === true`. Bucket-agnostic `.vip-banner` + `.vip-banner__chip` + `.vip-banner__text` CSS in `app/globals.css` — gold-on-cream gradient (`#FFF7E0` → `#FFEFC2` background + `#D4A41E` border + `#B8860B` chip with white text). **Deferred to Chase E follow-up**: Q2 Deep Clean +1 item — needs a conditional trigger seed against `context.outgoing_guest.clean_type='Deep'` at insert time + an existing-Deep-task backfill, meaningfully more complex than the other 4 quick-wins. Closes Open Jennifer #9 + #11 + #16 + VI.E partial. Inventory 13 → 9.
   - **Chase #5 — Rules-table xlsx mine (7 Q-REFERRED answers extracted).** Mined `docs/kb/Dispatch — Rules Table for Card and Section Governance.xlsx` (25 sheets) via openpyxl for the 7 Jennifer answers REFERRED to "the rules document." New ~150-line consolidation doc at `docs/kb/jennifer-rules-extraction.md` with per-Q sections (rules-table source citation, engineering implications, closure verdict). Findings: **Q6 D-430 tolerance** CONFIRMED strict-bounds (existing `tolerance: 0` matches rules-table per-cell convention); **Q14 daily distribution** CONFIRMED 80/20 admin-note threshold (Da-430 row 13: "If single user does ≥80% or ≤20% of team dailys → admin note") — logged for IV.I post-beta activation; **Q15 housekeeper assignment** CONFIRMED two-tier system (Hallway+Assignment rows 6-12: primaries → stayovers/arrivals split 30s/20s with lighter-load also taking 40s; non-primaries → departures/dailys; standard load 5 dep / 10 stay / 15 daily per housekeeper); **Q16 hallway adjacency** CONFIRMED locked w/ admin override + audit event (validates existing `assignment_cross_hall_override` taxonomy); **Q17 IV.H Phase B** THRESHOLDS CONFIRMED (D-430 row 26: 40% / 45 days / ≤3 deep items — all four constants in `DEEP_CLEAN_AUTO_TRIGGER` match) — data-source decisions (occupied-room-nights query + total-rooms denominator) reframed from Jennifer-blocked to **Bryan-actionable**; **Q19 Sheet Change skip** CONFIRMED two-case interpretation (S-430 row 16: "if assigned and skipped → admin note. If completed unassigned → skip next scheduled change for guest"); **Q21 maintenance cascade tree** NOT IN RULES TABLE — Jennifer's KB authoring still owed (per-Location → Item-set + per-Item → Type-set mappings not specified). Closes Open Jennifer #6 + #17. Inventory 9 → 8.

**Day 52 process codifications:**

- **Multi-question AskUserQuestion batching, applied 2× this session** (Day 48 codification — first applied Day 50, three rounds Day 51, two this session). Chase #2 batched 2 plan-before-cut decisions (event-type fork + auto-complete trigger semantics) into one Bryan response cycle. Forward rule unchanged: any chase with >1 independent decision Bryan should make → batch into one AskUserQuestion call.

- **Rules-table-mining-as-a-closure-mechanism** (Day 51 chase #4 standing-tabled triage precedent generalized to Jennifer-pending items). When Jennifer responds "see the rules document" to multiple questions, mine the xlsx in batch via openpyxl + write a per-Q findings doc rather than re-asking Jennifer. Day 52 chase #5 closed 2 Open Jennifer questions outright via the mine + reframed 1 from Jennifer-blocked to Bryan-actionable. Forward rule: when ≥3 Jennifer answers point at the same governance doc, mine in one chase rather than asking Jennifer for clarifications-of-clarifications.

- **Day-21-lock-reversal-via-rules-table-archaeology codified.** Jennifer Q18 + rules table line 88 aligned exactly; Day 21 "staff cannot edit pills" lock contradicted our own governance doc and had been carried forward through 5+ closures (Days 21, 45, 46, 49) before Jennifer's Q18 surfaced the contradiction. Forward rule: when codifying a new lock or product call mid-session, cross-reference `docs/kb/Dispatch — Rules Table Handoff.md` + the governance xlsx BEFORE treating the lock as canonical. Locks contradicting governance are technical debt waiting to surface.

- **Combined-commit-when-files-overlap** (Day 52 chase #4 + #5 landed in `a14500a`). When two adjacent chases both touch STATE.md + master plan in overlapping ways, the cleanest split is to bundle into one commit with both ledger entries inline rather than forcing a per-chase commit split that would require selective-add and risk inconsistent ledger state. Pragmatic accounting forward rule: if `git status` after chase N shows files mid-edit by chase N+1, bundle into one commit and let the closure ledger carry the per-chase narrative. Commit message names the dominant chase; STATE.md tells the full story.

- **Inventory-tracker tail rule continued** (Day 51 codification). Every closure message appended `📊 Inventory: NN → MM | Section X: Y → Z | Day N chase #M sealed | HEAD <sha>`. Made the count drop visible across the long session without explicit "where are we" check-ins from Bryan.

**Carry-forward still pending Bryan-solo:**

- **Stray-Lizzie data hygiene** — no longer Jennifer-blocked (closed Day 52 chase #1 as test-user N/A per Q22). Bryan-actionable SQL DELETE in Supabase dashboard remains optional cleanup if desired; orphan still surfaces as top row of `team_roster_v` until deleted.
- **Day 52 chase #3 SQL apply** — `docs/supabase/checklist_seed_per_card_type.sql` needs to run in Supabase dashboard SQL editor. Migration is destructive (DELETEs Day-49 hotfix wrong-shape D-430 rows from existing arrival + stayover tasks; safe per Jennifer Q22+Q23). After applying, run the two verification SELECTs commented at bottom in a separate query — expect `wrong_shape_rows = 0` and per-card_type item counts of 7 / 3 / 8.
- **UI verifications** for Chases #2 + #3 + #4: (1) S-430 stayover pills now `<button>`s with hover/focus + "Saving…" busy state; (2) admin StayoverStatusPanel save with DND/DeskOK/GuestOK auto-flips task to done + auto-archives off staff queue; (3) post-SQL-apply A-430 + S-430 fresh cards render Jennifer's lists (3-item Arrival or 8-item Stayover); (4) AddTaskModal VIP panel surfaces on arrivals/stayovers buckets only + checked banner renders gold-on-cream at top of staff card; (5) AddTaskModal maintenance bucket dispatches to Dailys (not SOD).
- **Forward Jennifer the D-430 grid format** for Q4 (extract the 18-cell grid from `lib/dispatch-config.ts` Section 9 in a format Jennifer can fill — Cowork-Claude can prep on request).
- **Clarify "Long-term stays" context** to Jennifer (per Q2 — needed for VI.E variant lists completion).
- **Pick IV.H Phase B data source + denominator** — reframed from Jennifer-blocked to Bryan-actionable Day 52 chase #5. Likely picks: `public.reservations` overlap query for occupied-room-nights (Node-safe equivalent inside `deep-clean-elevation.ts` since `lib/reservations.ts` is browser-coupled) + hardcoded `21` for total-rooms denominator (single-property beta). Once both picked, single-line code change unlocks Phase B.

**Bryan apply steps (Day 52):**

- Chase #1: no apply step (markdown-only commit).
- Chase #2: no apply step (TS + CSS only; Vercel auto-redeploys from main).
- Chase #3: **`docs/supabase/checklist_seed_per_card_type.sql` PENDING** — destructive SQL migration; apply via Supabase dashboard SQL editor + run the two verification SELECTs commented at bottom.
- Chase #4 + #5 (`a14500a`): no apply step (TS/CSS + markdown-only changes; Vercel auto-redeploys).

**Followups for next session:** Chase queue holds at 1 active engineering chase (V.C Cloudbeds, external-blocked on sales quote). Chase E (Deep Clean +1 item per Q2) sits as a Bryan-actionable follow-up. Plus the IV.H Phase B activation as Bryan-actionable single-line change once data source + denominator picked. The remaining 8 inventory items: 7 master plan (V.C + VI.E + VI.F + VI.G + VII.F + VII.H + VII.I) + 1 Open Jennifer (Q21 maintenance cascade tree). All Jennifer-pending KB or external-blocked or Bryan-decision territory.

**Honest read on beta status (Day 52 final):** code-side beta has been shipped per CLAUDE.md's 5 ruthless beta-scope items since Day 45. Day 51 cleared the post-beta polish backlog from 89 active inventory items down to 22; Day 52 cleared another 14 down to 8. The remaining 8 are entirely (a) Jennifer's authoring lane content (4 items), (b) Cloudbeds external (1 item), or (c) post-beta residue (3 items). **Engineering-side, Dispatch is in steady state pending Jennifer + Cloudbeds + the Bryan-decision Phase B activation.** Production live at https://dispatch-app-iota.vercel.app/. The path from 8 to 0 runs through Bryan-Jennifer + Bryan-Cloudbeds + the single Bryan-IV.H-Phase-B picks.

---

## Last session close (Day 51, 2026-05-09)

**Crush-mode session: 9 chases, 67 inventory items closed (89 → 22), zero hotfixes.** Largest single-day inventory contraction in Dispatch history. Driver: Bryan's "knock out the rest of the 89 tonight" directive after upgrading account usage; one-chase-verify-repeat pace held throughout; mid-session operating-rule add of in-chat inventory-tracker tail (`📊 Inventory: NN → MM | Section X: Y → Z | Day N chase #M sealed | HEAD <sha>`) so the count drop stayed visible across the long session. Sequence:

- **Chase #1 (`ddfe66d`) — VII.G Team roster derived view.** New `docs/supabase/team_roster_view.sql` defines `public.team_roster_v` — one row per `public.staff` with on-shift flag, current `in_progress` task (LATERAL), `is_in_eod` boolean matching `canWrapShift` definition byte-for-byte. Pattern mirrors Day-32 view trio. Bryan applied; verification SELECT returned 5 rows (orphan-Lizzie surfaced as on-shift with `current_card_type='eod'` AND `is_in_eod=false` — design validated end-to-end since the two columns earned their split). Close commit `9c23fff`.
- **Chase #2 (`9013354`) — VII.B + VII.E.** VII.B schema audit pass: `maintenance_issues_table.sql` verified complete vs III.B + Global Rules R12-R17 spec, no extension needed for beta. VII.E formalized: runtime fan-out helper at `lib/activity-feed.ts` won (neither table nor DB view) — already implicit in lines 24-27 of that file's comment block; chase made it explicit.
- **Chase #3 (commit between 9013354 and 1289719) — VII.A + VII.C.** VII.A: derived from `notes WHERE note_type='Supply'` — no separate table for beta; existing `notes_type_status_idx` covers the query. VII.C: index existing tables + `task_events` covers append-only audit trail — no dedicated `audit_log` for beta. **Section VII reached its post-beta-only residue (8 → 3 items: VII.F / VII.H / VII.I — all deferred or external-blocked).**
- **Chase #4 (`1289719`) — Standing-tabled triage (16 closures).** Audited all 22 STATE.md standing-tabled items; classified each as SHIP-NOW (6 items kept for chases #5/#6), FORMAL-DEFER (15 items moved to new "Formally deferred for beta" subsection with documented rationale + revisit triggers), or DROP (1 item — verification kit data persistence already accepted as production data). Restructured "Standing tabled" section into three subsections (SHIP-NOW / FORMALLY DEFERRED / surface-map reference). Forward rule established: standing-tabled triage is a clean way to close inventory ambiguity without writing code.
- **Chase #5 (`6683c9c`) — Quick-wins bundle (3 standing-tabled SHIP-NOW closures).** package.json `"type":"module"` add (eliminates `MODULE_TYPELESS_PACKAGE_JSON` Node warning on every npm + orchestrator run; safe because no `.js` config files exist — `next.config.ts` is TypeScript). `docs/supabase/drop_task_comments.sql` (DESTRUCTIVE — drops legacy pre-beta dev table; Bryan applied, verification returned 0 rows). `docs/supabase/cleanup_legacy_checklist_seeds.sql` (DESTRUCTIVE — DELETEs 3 non-canonical seed rows from pre-Day-49-hotfix tasks; Bryan applied, verification returned `legacy_rows_after_delete = 0`). Migration count 27 → 29. Build verify clean.
- **Chase #6 (`0a15ea7`) — UI polish bundle (3 standing-tabled SHIP-NOW closures).** Severity chip "Saving" indicator on `/admin/maintenance/[id]` — new `panelHeadSaving` CSS class with sage-text color + `severityPulse` keyframe + `chipBusy` class on active chip while busy (replaces previously-invisible `panelHeadRight` SAVING text). New `/admin/maintenance/resolved/page.tsx` index view (~280 lines) — mirrors open-list shape with `not("resolved_at", "is", null)` filter + `resolved_at` desc ordering; imports parent `page.module.css` via relative path so visual stays identical without CSS duplication; cross-nav checkmark glyph link added to `/admin/maintenance` topbar. Admin-side photo thumbnail on `/admin/tasks/[id]` activity panel — extends `ActivityPanelRow` with optional `imageUrl`; loadActivity mapper extracts `detail.image_url` when `event_type === imageAttached` (Day 40 III.E payload shape); render block conditionally surfaces `<img>` thumbnail (180×140 max, anchored block-level link to open full-size). 6 files changed. Standing-tabled SHIP-NOW queue 3 → 0 — all 6 SHIP-NOW items from chase #4 triage closed across chases #5 + #6. Build verify clean (23 routes total post-resolve-page).
- **Chase #7 (`b8b6c47`) — Sections X + IX + VIII formal-DEFER pass (11 closures).** Section X (6 → 0): A in-app KB-driven card generation agent + B multi-property + F advanced photo pipeline + G @mention autocomplete + H multi-tenant/SaaS-mode all formally accepted POST-BETA DEFERRED with revisit triggers; I ResNexus dropped (already tracked under V.C/V.H Cloudbeds). Section IX (5 → 1): IX.A formally accepted as ONGOING OPERATIONAL PRACTICE (RLS hardening per table-add already enforced by CLAUDE.md operating convention), IX.B/C/D POST-BETA DEFERRED. Section VIII (2 → 1): VIII.F pg_cron migration POST-BETA DEFERRED. Section X reaches zero items remaining for active inventory tracking.
- **Chase #8 (`e68592f`) — Mass formal-DEFER pass on Sections I + II + III + IV + V (30 closures).** Largest single-chase formal-DEFER pass in Dispatch history. Section I (6 → 1): I.D/E/F/H/I formally accepted as ENGINEERING-COMPLETE-PENDING-KB-AND-EXTERNAL (engineering shells shipped Days 33-49; remaining work is content/data-layer wirings blocked on Jennifer + V.D/V.E external APIs); I.G stays in chase queue. Section II (4 → 0): II.I admin category cards as POST-BETA POLISH; II.J/K/L explicitly post-beta per master plan authoring intent. Section III (6 → 0): III.C/F/G/H/I/K all post-beta polish or downstream-blocked. Section IV (10 → 0): IV.A foundational but not beta-essential (AddTaskModal handles manual assignment); IV.B/C/D/F/I/J post-beta orchestrator polish; IV.E/G/H Phase B Jennifer-blocked. Section V (6 → 1): V.B/D/E/F/H all post-beta data-layer integrations or Cloudbeds-paired; V.C stays in chase queue. **Master plan inventory drops 45 → 15 master plan items.**
- **Chase #9 (`974606c`) — VIII.G backup/restore docs ship + IX.E idempotency audit close (2 closures).** New `docs/deployment/backup-restore.md` (~95 lines) documents Supabase Hobby auto-backup coverage + restore procedure + test-restore lane + env var manual restore + post-beta Pro upgrade considerations — closes VIII.G as engineering-actionable shipped. IX.E formally closed as AUDIT-COMPLETE-PENDING-CLOUDBEDS — existing idempotency footprint surveyed (`inbound_events_dedup` UNIQUE for reservations + clock-in events, `task_events` append-only, photo storage uid-prefix paths, photo upload fail-loud per Day 40, GitHub Actions cron retry); new Cloudbeds webhook idempotency-key work pairs with V.C chase queue #3. Section VIII (1 → 0) and Section IX (1 → 0). **Engineering work surface remaining: ZERO non-external-blocked items.**

**Day 51 process codifications:**

- **Inventory-tracker tail rule.** When operating in crush-mode (multi-chase session pushing for inventory closure), append `📊 Inventory: NN → MM | Section X: Y → Z | Day N chase #M sealed | HEAD <sha>` to every closure message. Keeps the count drop visible across long sessions; lets Bryan see throughput without asking. Pairs with the existing context-capacity rule (write rolling close at 70-85% session capacity).
- **Multi-question AskUserQuestion batching, applied 3× this session** (Day 48 codification — first applied Day 50, three rounds Day 51 chases #1, #2, #3). Each round constrained chase target + chase pace in one Bryan response cycle.
- **Standing-tabled triage as a closure mechanism.** Day 51 chase #4 demonstrated that a deliberate triage pass over carry-forward items — classifying each as SHIP-NOW / FORMAL-DEFER / DROP with documented rationale + revisit triggers — legitimately closes inventory items by removing ambiguity. Items move from "still on our radar?" to "explicitly accepted state with revisit conditions." Forward rule: when standing-tabled grows past ~15 items, run a triage pass to bring it back under 5.
- **Mass formal-DEFER pass when beta-scope is essentially complete** (chases #7 + #8 + #9). When CLAUDE.md's beta-scope items are shipped and remaining inventory is post-beta polish + KB-blocked + external-blocked, applying formal-DEFER acceptance with revisit triggers is the honest accounting move. Cleared 41 items today (11 in chase #7, 30 in chase #8) by recognizing the beta-shipped-already reality. Chase #9 closed the last 2 actively engineering-actionable items.
- **Paired audit/decision items batched per chase** (Day 51 chases #2, #3, #7, #8, #9). When existing implementation already encodes the answer (e.g., `lib/activity-feed.ts:24-27` already documents the runtime fan-out choice for VII.E; `notes_table.sql` already has `note_type='Supply'` taxonomy for VII.A), the chase is a documentation pass — pair with another similar-shape item to double throughput at no extra session cost.
- **Forward operating model for next-session orientation.** STATE.md is the canonical handoff substrate: read this file first, walk the closure ledger top-down for context, check the chase queue for what's actively prioritized, check Open Jennifer questions for what's externally blocked. The next chat does NOT need to read all 9 Day-51 commit diffs — STATE.md + master plan + this close stack entry covers the orientation. Reading paths by chase type (under "Tier 2 — load by chase type") tell the next-Cowork-Claude exactly what to read for any specific chase pickup.

**Carry-forward still pending Bryan-solo (multi-day):**

- **Stray-Lizzie data hygiene** unresolved — Open Jennifer question; orphan `fc2c4280-2be4-4ef8-a1ea-3a0b3dfbe3bc` 'Ops' vs canonical `8fb2f515-4df3-4835-b2e9-e01f2eff993d` 'Front of House'. Last remaining multi-day carry. Bryan-actionable externally; once Jennifer confirms via text/email, single SQL DELETE in Supabase dashboard. Visibility increased Day 51 — orphan is the literal top row of `team_roster_v` output.

**Bryan apply steps (Day 51):**

- ✓ **Chase #1 done.** `docs/supabase/team_roster_view.sql` applied via Supabase dashboard SQL editor; verification SELECT returned 5 rows.
- ✓ **Chase #5 done.** `docs/supabase/drop_task_comments.sql` applied (verification returned 0 rows = table dropped). `docs/supabase/cleanup_legacy_checklist_seeds.sql` applied (verification returned `legacy_rows_after_delete = 0`).
- **Chases #2/#3/#4/#6/#7/#8/#9: no Bryan apply step.** Either UI changes verified by build (chase #6) or markdown-only documentation changes.

**Followups for next session:** **Chase queue still holds at 3, all external-blocked.** Day 52 has zero engineering-actionable inventory remaining (verified end of chase #9). Day 52's productive surface = (a) Bryan-externally chase Jennifer for Open Jennifer answers + KB authoring deliverables, (b) Bryan-externally chase Cloudbeds sales quote, (c) when Jennifer/Cloudbeds unblock, the chase queue + paired-deferral items light up automatically. The 22 remaining inventory items: I.G chase queue #1/#2 (1) + V.C chase queue #3 (1) + VI.A-H Jennifer authoring lane (8) + VII.F/H/I post-beta residue (3) + 9 Open Jennifer questions = 22. Stray-Lizzie cleanup is the only externally-actionable Bryan-solo item remaining.

**Honest read on beta status (Day 51 final):** code-side beta has been shipped per CLAUDE.md's 5 ruthless beta-scope items since Day 45. Day 51 cleared the post-beta polish backlog from 89 active inventory items down to 22 — and of those 22, every single one is either chase-queue-active-but-externally-blocked OR Jennifer's authoring lane OR post-beta residue. **Engineering-side, Dispatch is in a steady state pending Jennifer + Cloudbeds.** The path from 22 to 0 runs entirely through Bryan-Jennifer + Bryan-Cloudbeds external coordination. Production remains live at https://dispatch-app-iota.vercel.app/.

---

## Last session close (Day 50, 2026-05-08)

One commit `2012674` on top of `65440b7` rewrote `docs/deployment/vercel-checklist.md` to lead with the web UI flow Bryan actually used Day 45 (vercel.com → Add New Project → import `JBTheDispatchCobj/Dispatch-App` → Hobby plan → 5 env vars during configure → click Deploy), demoting the CLI to a short pointer that documents the CC-OAuth caveat. Day 45-codified follow-up carried Days 46/47/48/49 closes here. Markdown-only commit; build verify skipped per operating convention. 1 file changed, 78 insertions / 73 deletions. **No master plan letter closures** — closes the "Vercel checklist update" bullet from the master plan's Day 49 Current State Section VIII inventory; chase queue still holds at 3 (all external-blocked: chase #1 `stayover_status` orchestrator pre-set on Jennifer's KB heuristic; chase #2 I.G sub-items 2+3 on chase #1; chase #3 V.C Cloudbeds on sales quote).

**Day 50 process notes:**

- **Localhost-completeness clarification mid-rewrite.** First draft of Step 5 (Supabase magic-link redirect) listed only the two prod Redirect URLs entries with a parenthetical "keep your existing localhost entries — dev still needs them." Bryan flagged "I need it to be the local as well." Step 5 expanded to spell out all four entries (`http://localhost:3000/{auth/callback,**}` + `https://dispatch-app-iota.vercel.app/{auth/callback,**}`) with the rationale documented inline: the login page (`app/login/page.tsx:74`) sends a dynamic `emailRedirectTo` based on `window.location.origin`, so Supabase's allowlist needs both origins to handle dev (`npm run dev`) + prod (Jennifer's phones) from the same codebase. Lightweight forward-applicable rule: when documenting Supabase URL Configuration in deploy docs, list the complete Redirect URLs allowlist explicitly rather than relying on "keep your existing entries" parentheticals — `app/login/page.tsx`'s dynamic `emailRedirectTo` behavior is what makes both entries necessary. Logged inline rather than codified in Critical operating conventions because it's documentation-completeness, not architectural process.

**Carry-forward still pending Bryan-solo (multiple-day):**

- **Stray-Lizzie data hygiene** unresolved — Open Jennifer question; orphan `fc2c4280-2be4-4ef8-a1ea-3a0b3dfbe3bc` 'Ops' vs canonical `8fb2f515-4df3-4835-b2e9-e01f2eff993d` 'Front of House'. Last remaining multi-day carry. Bryan-actionable externally; once Jennifer confirms via text/email, single SQL DELETE in Supabase dashboard.

**Followups for next session:** chase queue still holds at 3 (all external-blocked: chase #1 stayover_status orchestrator pre-set on Jennifer's KB heuristic; chase #2 I.G sub-items 2+3 on chase #1; chase #3 V.C Cloudbeds on sales quote). **Stray-Lizzie data hygiene** via Jennifer text/email is the only externally-actionable item Bryan can move on his own. No new chases queued.

**Honest read on beta status (Day 50):** unchanged from Day 49 — code-side beta essentially shipped per CLAUDE.md's 5 ruthless beta-scope items; production live at https://dispatch-app-iota.vercel.app/, dark-mode-readable. Wisconsin hotel can hit URL once staff users provisioned in Supabase (Step 7 of the rewritten checklist now documents that provisioning SQL pattern explicitly). Day 50 closed the documentation polish item from the carry-forward queue; the deploy doc now matches the actual Day 45 reality and includes the localhost+prod Redirect URLs spelled out so future deploys (or anyone reading the doc) doesn't have to infer.

---

## Last session close (Day 49, 2026-05-08)

Three commits + 4 verification closes + 2 new codifications + 1 schema-change hotfix. **No master plan items closed** — all polish + verification + bug fix. Chase queue still holds at 3 (all three remain external-blocked: chase #1 stayover_status orchestrator pre-set on Jennifer's KB, chase #2 I.G sub-items 2+3 on chase #1, chase #3 V.C Cloudbeds on sales quote).

**Chase #1 STATE.md catch-up (`17af576`).** Day 47 + Day 48 close ledger entries inserted; "What ships today" staff side bullets refreshed for Day 48 note compose modal pattern + Day 47/48 manager-note plumb-through with bucket-agnostic setstat-row aesthetic + Departures-specific relocation into setstat container; 5 codifications appended to Critical operating conventions → Process (per-bucket-token collision class, AddTaskModal-to-staff-card data-flow audit, plan-before-cut for per-card patterns, bucket-agnostic class restyle, multi-question AskUserQuestion batching); header date bumped Day 46 → Day 48. Markdown-only commit; skipped build verify per operating convention. 1 file changed, 60 insertions / 2 deletions.

**Day 49 hotfix — checklist seed canonical alignment (`3e6d864` + `docs/supabase/checklist_seed_canonical_alignment.sql` + Supabase dashboard apply).** Surfaced mid-Day-40-verification when Bryan reported "restock didn't work" on a Departures card. Diagnosis: the `tasks_seed_default_checklist()` trigger had been seeding 3 non-canonical items ("Remove used linens" / "Replace sheets and pillowcases" / "Set up rollaway") on `housekeeping_turn` tasks only, while the staff cards' canonical UI list (`DEPARTURES_CANONICAL_CHECKLIST` in `lib/staff-task-execution-checklist.ts`) is 7 items (Open/Strip, Bed, Report/Doc, Prep, Clean, Close Out, Restock) and is aliased identically for `ARRIVALS_CANONICAL_CHECKLIST` + `STAYOVERS_CANONICAL_CHECKLIST`. Match-by-title in `buildDisplayChecklist` returned `dbItem === null` for any canonical row without a DB twin, and the click handler at `DeparturesCard.tsx:514` (`if (item.dbItem && !stepsLocked && !taskDone) onToggleItem(...)`) silently no-op'd. Arrivals + Stayovers tasks weren't seeded at all (trigger card_type filter excluded them), so 100% of those rows would have been unclickable on truly fresh tasks; Bryan's test data had been manually populated with some canonical-titled rows over the months, masking the gap intermittently. Fix: SQL migration in `docs/supabase/checklist_seed_canonical_alignment.sql` updates the trigger function via `create or replace` to seed the 7 canonical items and fire on all three card_types, then runs an idempotent backfill via `not exists` guard against case-insensitive title match. Applied via Supabase dashboard SQL editor; Bryan confirmed Restock click now toggles done with strikethrough + green bar fill on a Departures card after hard-refresh. SQL file committed alongside; build verify ran clean (no .ts/.tsx/.css changes — file is doc/archive only). 1 file added, 78 lines.

**Verifications closed end-to-end this session:**

- **Day 41 II.H per-issue page UI verification.** `/admin/maintenance/{id}` page renders correctly on Open + Resolved states; severity chip writes through to `maintenance_issues.severity` (visible after navigate-back since the in-flight indicator doesn't render visibly — small UX gap noted, not a blocker); Mark Resolved CTA flips status pill Open (blue dot) → Resolved (green dot), button label "Mark Resolved" → "Saving…" → "Resolved" (gray-disabled), per-issue Activity panel header flips "1 EVENT" → "2 EVENTS" with new "Bryan marked resolved" row populated with display name + timestamp; Watchlist drops the resolved row (filter `resolved_at IS NULL` honored); back-arrow goes to `/admin` (Day 42 standing-tabled OK). Bryan-confirmed via screenshot of Resolved-state per-issue page showing all flips. Bryan explicit product call during verification: admin doesn't need a visual disabled state on severity chips when the card is resolved — admin can figure it out from the status pill + button state. Drop that polish item.

- **Day 40 III.E + V.G photo pipeline UI verification.** Note compose popout modal (Day 48 pattern) opens via `+` button in `topstrip-right`; Add photo button surfaces file picker; selected file uploads to Supabase storage at `task-files/{auth.uid()}/...` BEFORE row insert (fail-loud on upload failure with the documented error string); on success, modal closes, note row appears in card with `<img>` thumbnail (max-width 200px, wrapped in `<a target="_blank">`); `image_attached` task_event row fires with `detail = { image_url, source: "note", schema_version: 1 }`. Maintenance compose form (inline at bottom of staff card, not modal) repeats the same flow with `source: "maintenance"`. Bryan-confirmed end-to-end via Supabase SQL query against `task_events` returning 3 rows (1 note + 2 maintenance) with correct detail shape, storage URL pattern matching `task-files/{user-uid}/{timestamp}-{random}.jpg` (confirms the `task_files_insert_own` RLS policy let the staff UID write into its own folder + the public bucket serves URLs back). The Day 49 checklist seed hotfix landed mid-this-verification per the codified pivot pattern.

- **Day 38 reshuffle UI verification.** Verified Day 48 (post-11 AM Central runtime check), surfaced retroactively Day 49 in chat. Late-Departures reshuffle reorders the staff bucket deck from `SOD → Departures → Stayovers → Arrivals → Dailys → EOD` to `SOD → Stayovers → Arrivals → Departures → Dailys → EOD` when past 11 AM Central weekday AND Departures still has incomplete tasks. Multi-day carry-forward from Day 38 closes.

- **Day 45 hotfix prod verification.** Bryan-confirmed in chat (was outstanding from Days 47-48 to avoid Vercel re-auth interruption). Dark-mode contrast on `https://dispatch-app-iota.vercel.app/` Activity Feed renders readable dark-on-cream rather than the pre-hotfix invisible near-white-on-cream. Day 45 hotfix `a287792` (delete `prefers-color-scheme: dark` `--foreground` swap + add `color-scheme: light` to `:root`) confirmed working in production.

**Day 49 process notes:**

- **DB-trigger-vs-UI-canonical-list drift audit codified.** Codified in Critical operating conventions → Process. Generalization: any UI list matched by title against DB-seeded rows needs cross-file pointer comments + lockstep updates in the same commit.
- **Mid-verification schema-change hotfix pivot codified.** Codified in Critical operating conventions → Process. Day 49 example took ~25 min from bug surface to verified fix without breaking verification flow. Single SQL block + saved to `docs/supabase/` archive + Supabase dashboard apply + browser hard-refresh + click confirms — clean five-step pattern.
- **Bonus visual confirmations during Day 40 verification.** Bryan's first Departures card screenshot showed (a) Day 47 manager-note `task.context.notes` plumb-through rendering on the staff card; (b) Day 48 Departures-specific relocation of `.manager-note` INTO the setstat container as a row between Room Spray and Status; (c) Day 48 `+` button in `topstrip-right` triggering the note compose modal — all three Day 47/48 polish items confirmed live in the wild without explicit verification chase. Free verification of the Day 49 STATE.md catch-up's "What ships today" updates.
- **Day 41 verification UX observations (small polish, not blockers, logged in standing tabled below).** (1) Severity chip on per-issue page writes inline via in-flight UPDATE + refetch but the `severityBusy` indicator doesn't render visibly — looks like nothing happened until navigate-back. (2) Resolved maintenance issues drop from BOTH Watchlist and master-table — there's no admin surface that lists "recently resolved" history; direct URL is the only way back to a resolved card. Fine for beta single-property scope.
- **Day 40 admin-side photo thumbnail design choice clarified.** Admin per-task activity panel surfaces the `image_attached` event row with `image_url` in detail JSON, but does NOT render a thumbnail. Per Day 40 closure, thumbnail render was scoped to staff cards only (staff need visual context for execution; admin gets audit trail). Could be revisited as v2 polish if Bryan wants admin-side thumbnails — small CSS lift on the `/admin/tasks/[id]` activity panel.

**Carry-forward still pending Bryan-solo (multiple-day):**

- **Stray-Lizzie data hygiene** unresolved — Open Jennifer question; orphan `fc2c4280-2be4-4ef8-a1ea-3a0b3dfbe3bc` 'Ops' vs canonical `8fb2f515-4df3-4835-b2e9-e01f2eff993d` 'Front of House'. Last remaining multi-day carry — all four UI verifications closed this session (Day 38, 40, 41, 45).

**Followups for next session:** chase queue still holds at 3 (all external-blocked: chase #1 stayover_status orchestrator pre-set on Jennifer's KB heuristic; chase #2 I.G sub-items 2+3 on chase #1; chase #3 V.C Cloudbeds on sales quote). **Vercel checklist rewrite** (lead with web UI flow, Day 45 codified follow-up) remains small-chase territory — not done this session, a clean ~15-20 min doc-only commit if Day 50 has the bandwidth. **Stray-Lizzie data hygiene** via Jennifer text/email is the only externally-actionable item Bryan can move on his own.

**Honest read on beta status (Day 49):** code-side beta is essentially shipped per CLAUDE.md's 5 ruthless beta-scope items. All four verification carry-forwards from Days 38/40/41/45 are now closed end-to-end. The remaining queue is documentation polish (Vercel checklist rewrite), data hygiene (Stray-Lizzie via Jennifer), and Jennifer-blocked KB authoring lane work. Production deploy at https://dispatch-app-iota.vercel.app/ is live and validated dark-mode-readable. The Wisconsin hotel can hit the URL on phones as soon as staff users are provisioned in Supabase.

---

## Last session close (Day 48, 2026-05-08)

One commit `e8dfd57` migrated note compose from inline forms across all 6 X-430 staff cards into a new cream-themed popout modal triggered by a `+` button in `topstrip-right`. The bucket-agnostic `.manager-note` block (introduced Day 47 to surface AddTaskModal's `task.context.notes` on staff cards) was simultaneously restyled to match the existing `setstat-row` aesthetic — white container, mono uppercase label, mint-pill value via `var(--accent-soft)` so each bucket inherits its own tone without per-card CSS. On Departures specifically, the manager-note was relocated INTO the `setstat` container as a row between Room Spray and Status per Bryan's spec; the other 5 cards keep `.manager-note` as a standalone block in its Day 47 position but now read visually like a setstat row. New file `app/staff/task/[id]/NoteComposeModal.tsx` wraps the existing `NoteComposeForm` component in a cream dialog with overlay + Esc-close + scroll-lock + mobile bottom-sheet / desktop centered. 8 files changed, 528 insertions / 162 deletions; build clean; no schema, no deps, no new task_events vocabulary. **No master plan items closed** — all polish on existing III.A notes compose path + Day 47's manager-note plumb-through.

**Diagnosis (plan-before-cut).** Two AskUserQuestion entries batched into one round (new Day 48 codification — multi-question batching when chase has >1 independent decision Bryan should make): (1) **scope** — modal vs in-place dialog vs slide-over drawer; Bryan picked popout modal for the discrete-action feel that mirrors AddTaskModal's interaction model on the admin side; (2) **button placement** — Bryan picked `+` in `topstrip-right` for thumb reach + visual continuity with the existing topstrip iconography. Single round constrained 11 files of downstream work in one Bryan response cycle.

**Implementation (`e8dfd57`).** New `NoteComposeModal.tsx` (~95 lines) shells the existing `NoteComposeForm` component — no form-level changes, just wrap-and-overlay. Modal anchors at root via portal pattern; Esc handler + scroll-lock follow standard dialog convention; mobile bottom-sheet / desktop centered split via media query. CSS lives in `app/globals.css` under new `.note-compose-modal-*` block. Each of the 6 staff card files (DeparturesCard / ArrivalsCard / StayoversCard / DailysCard / EODCard / StartOfDayCard): (a) imported `NoteComposeModal`, (b) added `noteModalOpen` local state, (c) wrapped the existing `NoteComposeForm` mount in modal trigger, (d) added `+` button in `topstrip-right` opening the modal. `.manager-note` block restyle in `app/globals.css` swaps the prior bucket-themed background for white container + mono uppercase label + mint-pill value (sourcing `var(--accent-soft)` so each bucket renders a distinct accent ribbon within the shared aesthetic). DeparturesCard additionally relocates the `.manager-note` div from above the brief into the setstat container's row stack between Room Spray and Status — single-card structural delta, others stay positionally unchanged.

**Day 48 process notes:**
- **Bucket-agnostic class restyle scales the cheap way.** When a multi-card UI change is purely visual (no structural moves), restyling a single bucket-agnostic class (like `.manager-note`) propagates across all 6 cards without per-card edits. Per-card edits stay reserved for structural moves (Departures relocating `.manager-note` into setstat). One shared class + per-card structural deltas = visual cohesion at minimum file churn.
- **Multi-question AskUserQuestion batching.** Two independent decisions (scope + button placement) batched into one AskUserQuestion call with two `questions[]` entries. Cost: same single round; benefit: constrained 11 files of downstream work in one Bryan response cycle. Forward rule (codified in Critical operating conventions → Process): any chase with >1 independent decision Bryan should make → batch into one AskUserQuestion call.
- **Day 47 codifications continued to apply.** Per-bucket-token collision audit passed (`.manager-note` doesn't derive colors from per-bucket palette tokens directly, just consumes `--accent-soft`). AddTaskModal-to-staff-card data-flow audit not triggered (no new context field this chase — only restyles existing `.manager-note`). Plan-before-cut for per-card patterns triggered (8 files this chase, well above threshold) — the AskUserQuestion round was driven by exactly this rule.

**Carry-forward still pending Bryan-solo (multiple-day):**
- **Day 41 II.H per-issue page UI verification** (4-step path).
- **Day 40 III.E + V.G photo pipeline UI verification** (4-step path).
- **Day 38 reshuffle UI verification** post-11 AM Central runtime check.
- **Day 45 hotfix verification** (~30 sec, dark-mode contrast on prod) — deferred Days 47-48 to avoid Vercel re-auth interruption. End-of-session sweep.
- **Stray-Lizzie data hygiene** unresolved — Open Jennifer question; orphan `fc2c4280-2be4-4ef8-a1ea-3a0b3dfbe3bc` 'Ops' vs canonical `8fb2f515-4df3-4835-b2e9-e01f2eff993d` 'Front of House'.

**Followups for next session:** chase queue holds at 3 (chase #1 `stayover_status` orchestrator pre-set still blocked on Jennifer's KB heuristic; chase #2 I.G sub-items 2+3 still blocked on chase #1; chase #3 V.C Cloudbeds still blocked on sales quote). **Inbound nav gap pattern (carry-forward standing observation):** Day 48 chase did not spawn any new admin surfaces. If a Day 49+ chase spawns ANY new admin surface, audit `/admin` tile + lane heads BEFORE the chase closes (per Day 42 II.H Phase C precedent — Maintenance tile re-route landed in follow-up commit after Bryan caught the gap mid-verification). Vercel checklist rewrite (lead with web UI flow, Day 45 codified follow-up) remains small-chase territory if Bryan wants it.

---

## Last session close (Day 47, 2026-05-08)

Three commits + two verification closes; **no master plan items closed** — two button-contrast hotfixes + one polish on AddTaskModal's existing manager-note path. Day 47's STATE.md catch-up commit was planned but never landed (Day 48 picked up STATE.md still showing Day 46 as current; Day 49 catch-up closed both Day 47 + Day 48 retroactively in one markdown-only commit).

**Hotfix `8faec3c` — Stayovers Save & Deploy contrast.** `BUCKET_THEME` stayovers row had `textOn: var(--shell-cream)` rendering the Save & Deploy button cream-on-cream invisible on `/admin/tasks/[id]` when the SOD/stayovers bucket was selected. Fix: `textOn` swapped to dark `#2C1608`. Same handler now `router.push('/admin/tasks')` after save instead of calling `loadTask()` (Bryan's preference — return to dashboard after save rather than re-render the same form). 1 file: `app/admin/tasks/[id]/page.tsx`.

**Hotfix `85b4c70` — SOD Dispatch button contrast.** `BUCKET_CONFIG.start_of_day` in `AddTaskModal` had `header=text=accent-ink` (same color), so the Dispatch CTA at the bottom rendered hollowed-out (dark-on-dark border-only) when the SOD chip was selected. Fix: added optional `buttonText?: string` field to the bucket config type; set to `var(--shell-cream)` for SOD only. Other buckets unchanged. 1 file: `components/admin/AddTaskModal.tsx`.

**Polish `c289f52` — Manager-note plumb-through to all 6 staff cards.** AddTaskModal writes `task.context.notes` (free-text manager note from the modal's Notes panel, shipped Day 28) but no staff card was reading it — the field had been silently set and forgotten for ~19 days. Fix: added a bucket-agnostic `.manager-note` block to all 6 X-430 staff cards + `page.tsx` prop threading. Per-card placement: SOD / Arrivals / Stayovers / Departures above brief; Dailys / EOD above first section (each card's structural shape decided per-card to read naturally given the existing layout). 8 files: `app/staff/task/[id]/page.tsx` + `app/globals.css` + 6 staff card files; 129 insertions / 0 deletions.

**Verifications closed:** (a) **Day 45 I.G sub-item 1 reader end-to-end** — Bryan opened an S-430 stayover staff card; brief 4th row read "Last status" with value "—" (will populate once setter chase ships); confirms the Day 45 forward-compat plumbing renders without regression. (b) **Day 44 II.A SOD chip end-to-end** — Bryan dispatched a SOD-bucket task via AddTaskModal; task landed with `card_type='start_of_day'` + `staff_home_bucket='start_of_day'`, surfaced on staff home in the SOD bucket as expected.

**Day 47 process notes:**
- **Per-bucket-token collision class.** When a button derives CSS vars from per-bucket palette tokens (e.g., `--dispatch-bg` from `header`, `--dispatch-color` from `text`), audit that the source tokens for THAT bucket actually resolve to distinct colors. SOD's `BUCKET_CONFIG` had `header=text=accent-ink`. Fix pattern: add an optional override field (e.g., `buttonText`) on the config type, apply only for buckets with collision. Same root cause as the Day 46-vintage stayovers Save & Deploy cream-on-cream that surfaced again as the first hotfix this session.
- **AddTaskModal-to-staff-card data-flow audit.** Any new top-level `task.context.<field>` written by AddTaskModal needs an explicit plumb-through plan covering `app/staff/task/[id]/page.tsx` (extract + thread to cards) + each of the 6 staff cards (prop + render slot) BEFORE considering the field "done." Bucket-specific fields document which card surfaces it; generic fields plan all six. The `task.context.notes` field had been silently written for ~19 days before Day 47 surfaced that no card was reading it.
- **Plan-before-cut for per-card patterns.** Any change touching all 6 staff card files merits an upstream placement question via AskUserQuestion. Day 47's `ctx.notes` change was 8 files; one question round avoided full rework. Generalization of CLAUDE.md's existing "plan before you cut code if the task touches more than one file" rule to per-card patterns specifically.

**Carry-forward still pending Bryan-solo (multiple-day):**
- **Day 41 II.H per-issue page UI verification** (4-step path).
- **Day 40 III.E + V.G photo pipeline UI verification** (4-step path).
- **Day 38 reshuffle UI verification** post-11 AM Central runtime check.
- **Day 45 hotfix verification** (~30 sec, dark-mode contrast on prod) — deferred to avoid Vercel re-auth interruption.
- **Stray-Lizzie data hygiene** unresolved — Open Jennifer question.

**Followups for next session:** chase queue holds at 3; STATE.md catch-up commit planned but did not land (Day 49 picked up the catch-up alongside Day 48 closes).

---

## Last session close (Day 46, 2026-05-08)

Chase #1 admin override leg closed end-to-end across two commits: `29173f2` mounted `<StayoverStatusPanel/>` on `/tasks/[id]` (manager card view); `016d19a` followed up to mount it on `/admin/tasks/[id]` (admin task view modal) after Bryan's first UI verification screenshot surfaced the surface gap. **Orchestrator pre-set leg deferred** per Day 46 plan-before-cut decision (heuristic undefined in Jennifer's KB; Bryan picked option 1 "Admin-only this chase, defer orchestrator pre-set"). Net work: ~1.5 hours including reading, plan-before-cut AskUserQuestion round, code cuts on both surfaces, two build verifies, two commits + pushes, two UI verification rounds. **Chase queue holds at 3 with chase #1 reshaped from "orchestrator pre-set + admin override" to "orchestrator pre-set only"** — admin override piece is shipped, the data path is open for admin-driven population, orchestrator side awaits Jennifer's KB.

**Diagnosis (chase #1, plan-before-cut).** Day 45's defensive plumbing meant `lib/stayover-history.ts` already reads `context.stayover_status` arrays from past stayover/housekeeping_turn rows; the only missing piece was a setter. Three shaping decisions surfaced via AskUserQuestion before any code touched: (1) **scope** — Bryan picked admin-only this chase, deferring orchestrator pre-set (carry-forward heuristic rejected because DND flips constantly with guest preference and would be wrong more often than right; Jennifer's KB hasn't authored the rule yet); (2) **placement** — separate panel above the form mirroring `<ReassignPanel/>` (not inline in the existing form, not below it) — gives a discrete audit trail and keeps the heavy form save path untouched; (3) **selection mode** — multi-select chips matching the underlying `string[]` data shape (single-dropdown rejected because future Sheet Change + Done combinations need stacking). Audit event vocabulary decided unilaterally: one new event type `stayover_status_overridden`, severity `info` (not added to `WARN_TASK_EVENT_TYPES` — admin overrides are routine, not warning signals).

**Implementation (`29173f2`, first commit).** 6 files changed, 306 insertions / 0 deletions. (1) NEW `components/admin/StayoverStatusPanel.tsx` (~170 lines) — discrete-action panel mirroring ReassignPanel shape: `parseStatuses` defensively reads `context.stayover_status` (returns `[]` for non-array or unknown keys), `arraysEqual` for the canSubmit equality check, `useEffect([context])` resets pending selection on parent reload (mirrors ReassignPanel's `useEffect([currentStaffId])` invalidation pattern), Save handler does merge-safe `{ ...context, stayover_status: pending }` spread + `logTaskEvent` with `withTaskEventSchema({ from: initial, to: pending })`. Mirrors `STAYOVER_STATUS_OPTIONS` from `app/staff/task/[id]/StayoversCard.tsx` line 36-45 exactly so admin writes match staff renders. (2) NEW `components/admin/StayoverStatusPanel.module.css` (~95 lines) — copies `ReassignPanel.module.css` chip / panel / submit conventions verbatim minus the textarea styles. (3) `lib/task-events.ts` — added `stayoverStatusOverridden: "stayover_status_overridden"` to the `taskEventType` constant. (4) `lib/activity-feed.ts` — added `[taskEventType.stayoverStatusOverridden]: "set stayover status"` to `TASK_EVENT_VERB`. (5) `app/tasks/[id]/manager-card-detail.tsx` — imported the panel + mounted it conditionally between `<ReassignPanel/>` and the big `card-panel` form, threading `taskId` / `userId` / `task.context` props plus the standard `onSuccess` reload+refresh. (6) `docs/TASK_EVENTS_CONTRACT.md` — added new vocabulary row documenting detail-key shape (`schema_version`, `from: string[]`, `to: string[]`) and severity classification.

**Implementation (`016d19a`, follow-up commit).** 2 files changed, 29 insertions / 2 deletions. After Bryan's first UI verification screenshot — the `/admin/tasks/[id]` admin task view modal — showed REASSIGN but no STAYOVER STATUS, surfaced the surface-gap omission. Day 35 ReassignPanel precedent line in the closure ledger: discrete admin panels mount on BOTH `/tasks/[id]` AND `/admin/tasks/[id]`. Three pieces: (a) `components/admin/StayoverStatusPanel.tsx` prop type relaxed `Record<string, unknown>` → `Record<string, unknown> | null` to match the admin page's `LiveTask.context` shape (the panel was already null-defensive at runtime via `context?.stayover_status` and merge-spread); (b) `app/admin/tasks/[id]/page.tsx` gained a `userId` state slot at line 272 hydrated inside the existing auth-resolve effect (`setUserId(user.id)` alongside `setReady(true)`) — the page didn't track user.id in state before, only used it during the auth check; (c) panel JSX mounted above ReassignPanel, gated on `task.card_type === "stayover"` AND `id && id !== "unknown"` (matches ReassignPanel's existing guard), threading `taskId` / `userId` / `task.context` plus an `onSuccess` calling both `loadTask()` and `loadActivity()` so the admin activity panel above refreshes inline when Save lands. Bryan re-verified on localhost — panel appears between ACTIVITY and REASSIGN, save round-trip clean, activity panel refreshes with the new entry.

**Day 46 process notes:**
- **AskUserQuestion ROI continued.** One round of 3 questions resolved scope + placement + selection mode before code touched. Q1 (scope) was the high-leverage one — the "carry-forward heuristic" option (orchestrator copies yesterday's status forward) was framed clearly enough that Bryan could see the operational problem (DND flips constantly with guest preference) and pick option 1 cleanly. Cut chase scope roughly in half (~45 min instead of 1.5–2 hr) — though the surface-gap follow-up added back ~30 min, ending at ~1.5 hr total. Net still positive vs. the original "orchestrator + admin override" framing.
- **Surface-gap lesson logged.** Day 35 ReassignPanel precedent documented in the closure ledger: "Mounted on BOTH `/tasks/[id]` ... AND `/admin/tasks/[id]` (II.G admin task view, between Activity and CTA pair, backed by a small live assignee fetch). Single component, two surfaces." I read the precedent during Day 46's reading pass but didn't operationalize it — only mounted on `/tasks/[id]` first. Bryan's UI verification screenshot caught the gap immediately. **Forward rule:** for any future discrete-action admin panel (anything that pattern-matches `<XxxPanel/>` mounted on a task surface), scan BOTH `app/tasks/[id]/manager-card-detail.tsx` AND `app/admin/tasks/[id]/page.tsx` during the placement plan; the closure-ledger precedent line is the canonical answer. The cost of this miss was a single follow-up commit (~30 min) — caught early enough that no design rework was needed.
- **Defensive forward-compat from Day 45 paid off Day 46.** `getLastStayoverStatusForRoom` was built Day 45 to handle null/empty contexts gracefully. Day 46's new panel writes the array shape exactly — zero rework on the read side. The Day 45 plumbing-no-op-until-data-flows pattern is now data-flowing without further edits to the lookup helper or the S-430 brief render.
- **Plan-before-cut gave a clean per-surface changeset.** Reading `interpret.ts` deriveContext + the existing `<ReassignPanel/>` shape + `task-events.ts` registry + `activity-feed.ts` verb list + `TASK_EVENTS_CONTRACT.md` entry shape before composing meant the implementation followed a single internal pattern (mirror ReassignPanel) and touched only the files genuinely needing edits. No exploratory commits, no rework loops on the per-surface code itself — only the surface-gap follow-up was rework, and that was a placement omission rather than a code design issue.

**Carry-forward still pending Bryan-solo (multiple-day):**
- **Day 41 II.H per-issue page UI verification** (4-step path).
- **Day 40 III.E + V.G photo pipeline UI verification** (4-step path).
- **Day 38 reshuffle UI verification** post-11 AM Central runtime check.
- **Day 44 II.A SOD chip UI verification** (~3 min, single-day carry).
- **Day 45 I.G sub-item 1 brief swap UI verification** (~30 sec).
- **Day 45 hotfix verification** (~30 sec, dark-mode contrast on prod — Bryan signed out of Vercel during Day 46 but did NOT tear down the deployment; https://dispatch-app-iota.vercel.app/ is still live and the verification still applies).
- **Stray-Lizzie data hygiene** unresolved — Open Jennifer question; orphan `fc2c4280-2be4-4ef8-a1ea-3a0b3dfbe3bc` 'Ops' vs canonical `8fb2f515-4df3-4835-b2e9-e01f2eff993d` 'Front of House'.

**Followups for next session:** chase queue holds at 3 with **`stayover_status` orchestrator pre-set only** as new chase #1 (admin override leg closed Day 46; orchestrator pre-set deferred until Jennifer's KB heuristic lands); **I.G sub-items 2 + 3** at chase #2 (now technically unblocked — admin can write status that triggers sub-item 2 auto-complete on card open; sub-item 3 status-pill skip semantics still need a sheet-change cadence system, post-beta); **V.C Cloudbeds** at chase #3 (pending sales quote). I.G sub-item 4 stays parked behind Jennifer's KB authoring + the new prerequisite chase, no chase queue slot. Updating `docs/deployment/vercel-checklist.md` to lead with web UI (Day 45 codified follow-up) remains small-chase territory if Bryan wants it. The latent `interpret.ts` line 42 `card_type=maintenance → staff_home_bucket=departures` vs `AddTaskModal` `maintenance → start_of_day` inconsistency stays as a dead-code flag, not a chase queue item — revisit if/when an orchestrator rule wires up to emit maintenance drafts.

---

## Last session close (Day 45, 2026-05-08)

Two chases closed end-to-end plus a hotfix surfaced via prod smoke test. **Item I / §VIII.A Vercel deploy** closed (production live at https://dispatch-app-iota.vercel.app/, magic-link auth round-trip verified in incognito; both `/admin` and `/staff` render). **I.G sub-item 1** Last Stayover Status lookup closed at `28eb2d5`. **Day 45 hotfix** at `a287792` removed the `prefers-color-scheme: dark` `--foreground` variable swap that was rendering admin Activity Feed text invisible against cream backgrounds when the OS was in dark mode (surfaced during prod smoke test). **I.G sub-items 2 + 3 + 4 deferred behind a new prerequisite chase** — orchestrator pre-set + admin override path for `task.context.stayover_status` — promoted to chase queue #1. Chase queue holds at 3 (Item I closed, replaced by the new prerequisite).

**Diagnosis (chase #1, I.G sub-items 1+2+3 plan-before-cut).** Bundled scope intent (1+2+3, defer #4 post-KB) confirmed via AskUserQuestion before reading code. Then code reality surfaced a blocker: **nothing in the codebase writes `task.context.stayover_status` today.** The staff-side toggle handler (`onToggleStayoverStatus` at StayoversCard.tsx:320) is preserved but `void`-suppressed (Day 21 lock: "status pill display-only"); pills are spans, not buttons. No admin or orchestrator setter exists. Sub-items 2 (status-driven auto-complete) and 3 (Sheet Change skip semantics) need a setter path to do anything visible. Bryan-confirmed via AskUserQuestion: Day 21 lock holds (staff cannot edit pills); proper architecture is orchestrator pre-set + admin override. Sub-item 3's "Sheet Change skip" further depends on a sheet-change cadence/scheduling system that doesn't exist today (master-plan I.G line 87 Open Assumption #5); after Bryan flagged "HELP" on the master-plan phrasing mid-question, rephrased in plain English (Sheet Change is a service action; "assigned" = today's card scheduled it; "completed unassigned" = staff did it anyway) and Bryan confirmed sub-item 3 is post-beta. Sub-item 4 (checklist variants) remains blocked on Jennifer's KB authoring + the new prerequisite chase. **Tightened scope to sub-item 1 only**, ship as forward-compat plumbing that no-ops until data flows.

**Implementation (`28eb2d5`, I.G sub-item 1).** 3 files changed, 181 insertions / 23 deletions. (1) New `lib/stayover-history.ts` — single helper `getLastStayoverStatusForRoom(roomNumber, excludeTaskId?)` queries last 20 `stayover` / `housekeeping_turn` tasks for the room ordered by `created_at` desc, filters in JS for rows whose `context.stayover_status` is a non-empty string array, returns `{ statuses, recorded_at } | null`. Defensive: graceful `console.warn`-and-return-null on Supabase error (matches V.A BR4 reservation-fallback pattern in `lib/reservations.ts`). (2) `app/staff/task/[id]/page.tsx` — 4 surgical inserts: import helper + `LastStayoverStatus` type; new `lastStayoverStatus` state slot; 6th promise in the existing `Promise.all` parallel fetch gated on `isStayover && room` (mirrors V.A BR4 reservation-fallback gating exactly); thread prop into StayoversCard render. (3) `app/staff/task/[id]/StayoversCard.tsx` — 5 surgical inserts: `LastStayoverStatus` type import; three display helpers (`statusBriefLabel` switch keyed by stayover_status keys → short label e.g. `dnd → "DND"`; `formatRelativeDate` with today / yesterday / N-days-ago semantics; `formatLastStatus` composing `<label> · <relative>` with `"—"` fallback for null); new optional prop `lastStayoverStatus?: LastStayoverStatus | null` with `= null` default; brief row 4 swapped from hardcoded `"Type: —"` to `"Last status: <formatLastStatus(lastStayoverStatus)>"`. **Forward-compat shape today**: no rows have `context.stayover_status` populated, helper returns null, brief renders `"—"` — same visible result as the prior hardcoded `"—"`. When the prerequisite chase ships, data flows automatically with no further edits here. No schema, no deps, no CSS, no new task_events vocabulary.

**Implementation (Item I / §VIII.A — Vercel deploy).** Browser-only path via vercel.com web UI — switched from CLI checklist (Steps 1-3 of `docs/deployment/vercel-checklist.md`) after the interactive `vercel login` + `vercel` flow tripped on the CC-vs-terminal context boundary. Web-UI flow: (1) Sign up at vercel.com → Continue with GitHub → authorize Vercel app → land on dashboard. (2) Add New Project → import `JBTheDispatchCobj/Dispatch-App` → autodetected as Next.js → Hobby plan (free tier). (3) Environment Variables (5 entries): `NEXT_PUBLIC_SUPABASE_URL` + `NEXT_PUBLIC_SUPABASE_ANON_KEY` + `SUPABASE_SERVICE_ROLE_KEY` from `.env.local`, plus `AGENT_KILL=true` + `AGENT_DRY_RUN=true` as production safety defaults. **`NEXT_PUBLIC_DEV_BYPASS` deliberately not added** (localhost-only role-switcher per CLAUDE.md; unset on prod is correct). (4) Click Deploy → build runs → deploys to https://dispatch-app-iota.vercel.app/. Login route renders. (5) Supabase auth config — Authentication → URL Configuration → Site URL set to https://dispatch-app-iota.vercel.app, Redirect URLs allowlist gains `/auth/callback` + `/**` for the Vercel domain (existing localhost entries preserved for dev). (6) Smoke test in incognito: magic-link round-trip → land on `/admin` (Bryan's admin role) → both admin home + staff role view render.

**Hotfix (`a287792`, dark-mode contrast bug).** Smoke test surfaced an Activity Feed contrast issue: text rendered nearly invisible against cream background when Bryan's OS was in dark mode. Triage via grep of `app/globals.css` lines 7-13 found the prefers-color-scheme: dark `@media` block flipping `--foreground: #171717` → `#ededed` (near-white). Admin Activity Feed text (`.activity-message`, `.activity-lede`, `.activity-muted`) inherits color from `--foreground`, but admin home backgrounds are hardcoded cream (Day 20 design tokens). Result: near-white text on cream when OS in dark mode → invisible until hover bumped contrast. Fix: deleted the dark-mode block at lines 7-13 entirely; added `color-scheme: light;` to `:root` as defense for browsers respecting the declarative property (tells the browser to use light-mode UA defaults for form controls / scrollbars). Block #2 at lines 144-148 (`.error` color override under dark mode → bright pink-red) left alone — single-property cosmetic, harmless on cream. Single CSS file touched, 8 insertions / 8 deletions. Vercel auto-redeploys from main; no manual deploy needed (verified the auto-redeploy path works as advertised). Build clean.

**New deferred prerequisite chase logged.** "Build `stayover_status` orchestrator pre-set + admin override path" — promoted to chase queue #1. Two-piece scope: (a) orchestrator-side, when generating a stayover task, pre-set `context.stayover_status` from prior-day's status / KB heuristics — wires the data path that Day 45's I.G sub-item 1 lookup helper expects (currently always returns null); (b) admin-side, on the manager card view at `/tasks/[id]`, surface a status-pill control letting admins override or set status. Day 21 lock holds — staff cannot edit pills (display-only on S-430). Once shipped, unblocks I.G sub-items 2 (auto-complete trigger), 3 (status-pill skip semantics — but only that interpretation; Sheet Change forward-scheduling is post-beta), and indirectly 4 (KB-driven checklist variants — also needs Jennifer's KB authoring in parallel).

**Day 45 process notes:**
- **AskUserQuestion ROI compounded.** Five plan-before-cut question rounds across the chase: scope (bundle 1+2+3 vs split), code-reality vs spec gap (no setter today — keep Day 21 lock or revert), sub-item 3 interpretation (status-pill / checklist-row / forward-scheduling), Last Stayover Status display layout, tightened scope confirmation. Each surfaced spec ambiguities Bryan resolved without code rework. Critical pivot: code-reality reading exposed that "sub-items 2+3 ship in 1-2 hr" wasn't accurate — they need a setter that doesn't exist. Without AskUserQuestion, this would have been a wrong-direction code cut.
- **Master-plan phrasing language gap.** Master plan §I.G line 87's "if assigned and skipped → admin note. If completed unassigned → skip next scheduled change for guest" used "assigned/unassigned" referring to the Sheet Change service action, not the task's assignment to a staff member. Bryan flagged "HELP" mid-question on the original framing; rephrased in plain English (service-action vs card-assignment) before he could answer. Good signal that master-plan terminology that's intuitive to spec authors can mystify even Bryan. **Forward rule**: rephrase master-plan terminology in plain English when surfacing AskUserQuestion options, especially around domain ops vocabulary (sheets, turns, assignments, holds).
- **Vercel deploy CLI vs web UI pivot.** Original checklist's CLI path (`vercel login` → interactive `vercel` → 6 questions) tripped on the OAuth browser flow plus interactive prompt context (CC bash can't proxy browser auth, and the prompt sequence is awkward through CC). Pivoted to vercel.com web-UI path mid-session — same outcome, dramatically lower friction for non-developer Bryan. Updating `docs/deployment/vercel-checklist.md` to lead with the web UI path (instead of CLI-first) is a small follow-up Bryan can request post-beta.
- **Don't paste my secrets to me, except when explicitly asked.** Bryan told CC to print `.env.local`, then asked me directly to give him the literal env var values to paste into Vercel. I did. The secrets routed through chat for one turn — same trust boundary as the CC-to-Vercel-dashboard hop he was about to make anyway. Worth keeping in mind for future deploy chases: when the user explicitly asks for the values, give them; don't add friction with "copy from CC's terminal" indirection unless the user prefers it.
- **Browser/OS dark-mode collision class.** Activity feed contrast bug was a Next.js scaffold default (`prefers-color-scheme: dark` block in globals.css) that survived the Day 20 cream-token lock. Cream is light-mode-only by design; dark-mode swap was harmless until prod surfaced it. Going forward: if any future CSS uses `var(--foreground)` against a hardcoded cream background, no dark-mode override fires (block deleted Day 45). Forward audit: if anyone ever wants to support dark mode, the block needs to come back AND the cream-token surfaces need their own dark-mode variants — that's a v2 design decision, not a hotfix.

**Carry-forward still pending Bryan-solo (multiple-day):**
- **Day 41 II.H per-issue page UI verification** (4-step path).
- **Day 40 III.E + V.G photo pipeline UI verification** (4-step path).
- **Day 38 reshuffle UI verification** post-11 AM Central runtime check.
- **Day 44 II.A SOD chip UI verification** (~3 min, single-day carry).
- **Day 45 I.G sub-item 1 brief swap UI verification** (~30 sec, NEW): open any S-430 stayover staff card; brief 4th row label was "Type" with value "—"; now reads "Last status" with value "—" (will populate once setter chase ships).
- **Day 45 hotfix verification** (~30 sec, NEW): hard-refresh https://dispatch-app-iota.vercel.app/ in browser/OS dark mode; admin Activity Feed entries should be readable dark-on-cream rather than invisible.
- **Stray-Lizzie data hygiene** unresolved — Open Jennifer question; orphan `fc2c4280-2be4-4ef8-a1ea-3a0b3dfbe3bc` 'Ops' vs canonical `8fb2f515-4df3-4835-b2e9-e01f2eff993d` 'Front of House'.

**Followups for next session:** chase queue holds at 3 with **`stayover_status` orchestrator pre-set + admin override** as new chase #1 (unblocks I.G sub-items 2/3/4); **I.G sub-items 2 + 3** waiting at chase #2 (resume once #1 ships); **V.C Cloudbeds** at chase #3 (pending sales quote). I.G sub-item 4 stays parked behind Jennifer's KB authoring + the new prerequisite chase, no chase queue slot. **Item I (Vercel deploy / §VIII.A) closed end-to-end Day 45; Dispatch is live on the public internet at https://dispatch-app-iota.vercel.app/.** Vercel auto-deploys from `main`; no manual redeploy needed for code commits going forward.
