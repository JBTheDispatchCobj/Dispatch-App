// Most maintenance is reactive (staff-reported). Rules here would be PROACTIVE / scheduled. [ASK JENNIFER] — any scheduled maintenance cadence?
import type { GenerationRule } from './types.ts';

export const maintenanceRules: GenerationRule[] = [];
